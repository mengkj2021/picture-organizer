# 路由设计

> **android 现状**：`android/.../navigation/`（`Routes.kt`、NavHost）。画面规格见 [`画面/`](画面/)；**全表清单**见 [画面/README.md](画面/README.md)。

## 1. 约定

- 单 Activity；Navigation Compose；route 字符串常量集中在 `Routes.kt`

## 2. route 命名约定

| 规则 | 示例 |
|---|---|
| 全小写，多词用连字符 | `import-images` |
| 传参用路径参数，`{}` 包裹 | `detail/{imageId}` |
| 根画面固定为 `splash` | `splash` |

## 3. 当前导航图

| route | 画面 | 参数 | 文档 |
|---|---|---|---|
| `splash` | 启动画面 | 无 | [README.md](画面/启动画面/README.md) |
| `main` | 主画面 | 无 | [README.md](画面/主画面/README.md) |
| `import-images` | 导入画面 | 无 | [README.md](画面/导入画面/README.md) |
| `image-detail/{imageId}` | 图片详细画面 | `imageId` | [README.md](画面/图片详细画面/README.md) |
| `settings` | 设置画面 | 无 | [README.md](画面/设置画面/README.md) |
| `tag-manage` | 标签管理画面 | 无 | [README.md](画面/标签管理画面/README.md) |
| `filter?tags=&untagged=&q=&sort=&dateFrom=&dateTo=&importFrom=&importTo=` | 筛选画面 | 可选 query 如上 | [README.md](画面/筛选画面/README.md) |
| `tutorial?fromSettings={fromSettings}` | 教程画面 | `fromSettings` 可选 | [README.md](画面/教程画面/README.md) |
| `rename-template-manage` | 重命名模板管理 | 无 | [README.md](画面/重命名模板管理画面/README.md) |
| `rename-template-edit` / `rename-template-edit/{templateId}` | 重命名模板编辑 | 可选 `templateId` | [README.md](画面/重命名模板编辑画面/README.md) |
| `default-tags` | 默认标签 | 无 | [README.md](画面/默认标签画面/README.md) |
| `export-zip` | 图片打包画面 | 无 | [README.md](画面/图片打包画面/README.md) |
| `export-manage` | 已打包文件管理画面 | 无 | [README.md](画面/已打包文件管理画面/README.md) |
| `oss-licenses` | 开源许可画面 | 无 | [README.md](画面/开源许可画面/README.md) |

## 4. 新增画面

按 [画面/README.md](画面/README.md)「新增画面」：复制模板 → 登记本表 §3 一行 → 更新画面清单 → 再实现代码。

## 5. 系统返回动作

| 层级 | 默认行为 |
|---|---|
| 非根画面 | 返回键 / 顶栏返回 = 上一画面。**安全 pop**：仅当前仍是目标 route 且 pop 后栈非空才执行（Bug6 / Bug10）。transition 未 RESUMED 时勿 pop；顶栏用 `BackNavIconButton`；NavHost 可再兜底 |
| 根画面（main） | 返回键 = 退出应用；**禁止**被误 pop 成 NavHost 空窗 |
| splash | 按教程完成状态 → `tutorial` 或 `main`，均不在 back stack 保留 |
| tutorial（首次） | 完成/跳过写入 `tutorial_completed` 后进 `main`，不回 splash |
| tutorial（`fromSettings=true`） | 结束仅 `pop` 回设置，不改写完成标记 |
| 带编辑 / 未保存 | `BackHandler` 拦截并确认，由各画面文档约定 |
| 导入中 | `BackHandler` 始终启用并吞掉返回（F10）；勿 `enabled=false` 把事件交回 Navigation |

过程见相关票复盘与 git；不另建开发日志。
