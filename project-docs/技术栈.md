# 技术栈

> **android 现状**：以 `android/gradle/libs.versions.toml` 与 `android/app/build.gradle.kts` 为准；改版本须同步本表。

**Kotlin** + **Jetpack Compose**；单 Activity + Navigation + **MVVM + Repository**（Room）。

## 版本矩阵

| 组件 | 版本 |
|------|------|
| Gradle / Wrapper | 9.7.1（`gradle-wrapper.properties` → 官方 bin.zip；F50；勿提交 zip） |
| AGP | 9.2.1（Built-in Kotlin） |
| Kotlin（compose 插件） | 2.4.10 |
| Compose BOM | 2026.08.00 |
| core-ktx | 1.19.0 |
| lifecycle-runtime-ktx / compose / viewmodel-compose | 2.11.0 |
| activity-compose | 1.13.0 |
| navigation-compose | 2.9.0 |
| room-runtime / room-ktx | 2.7.2 |
| KSP | 2.3.11 |
| coil-compose | 2.7.0 |
| exifinterface | 1.4.1 |
| datastore-preferences | 1.1.7 |
| material-icons-core | （Compose BOM） |
| ktlint（plugin） | 14.2.0 |
| junit / coroutines-test | 4.13.2 / 1.10.2 |
| aboutlibraries-compose-m3 | 15.0.4 |
| compileSdk / targetSdk | 37 |
| minSdk | 26 |
| Java | 17（AGP 9 默认） |

- Compose 由 `org.jetbrains.kotlin.plugin.compose` 管理；release：`minifyEnabled` + `shrinkResources`（R8）
- 改依赖或 bump `versionName` / `versionCode` 时同步本表与 toml

### 产品版本

| 口径 | 出处 | 当前值 |
|------|------|--------|
| versionName / versionCode | `app/build.gradle.kts` | `1.0.0` / `2`（F51；对外发包必增 code） |

无独立「文档版号」；规格以 `project-docs/` + 代码为准。无自动化版本脚本。

## 多语言

简中（默认）/ 日 / 英；**仅跟随系统**，无应用内开关；缺译回退简中。UI 三语对齐；用户数据（标签名、文件名等）不翻译。状态键库内为简中「待归档 / 已归档 / 回收站」（S5；旧键仍剥离）；UI 显示名随语言。工程文档与票库保持中文。
