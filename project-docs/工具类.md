# 工具类

> **android 现状**：跨画面无 UI 工具与少量共享模型。对应 `util/`、`ui/common/`、`model/`。

## `util/`

| 类 | 包 | 职责 |
|---|---|---|
| `AppFileManager` | `util/file/` | 三子目录、URI 复制、跨状态搬移、同目录重命名、删除、`exportsDir`、清 pending 孤儿 |
| `BatchRenamePlanner` | `util/file/` | 一览批量套模板规划（冲突检测；纯 JVM） |
| `PendingOrphanCleaner` | `util/file/` | 判定 `pending/` 无 DB 记录的孤儿名（纯 JVM） |
| `RenamePatternApplier` | `util/file/` | 模板 → 文件名。顺序固定：先 `{name}`/`{tags}`，再 `{n}`/`{tag}`，否则互相吞字 |
| `ZipExporter` | `util/file/` | 文件列表 → **一个** zip → `exports/`（S7；内容由上层 `ExportZipPack` 决定） |
| `ExportZipNames` | `util/file/` | F42/S7：默认 stem、非法名、冲突最终名；F46：`planZipRename`（事后改名冲突拒绝） |
| `ImageManager` | `util/image/` | `prepareForImport`：按开关压缩或原样写入 `pending/`。阈值常量 `MAX_BYTES`/`MAX_LONG_EDGE`/`JPEG_QUALITY`＝**现行临时**（2MB / 1920 / 85），是否定稿见 [阶段3 · 待对应](../project-status/阶段/阶段3-全面测试/待对应.md) |
| `ImageTagMetadata` | `util/image/` | 用户标签 ↔ Exif `UserComment` JSON。相册 Uri 优先 PFD；失败 / 非本应用 JSON → 空列表 |
| `ExifDateTaken` | `util/image/` | Exif `DateTimeOriginal` ↔ millis。无时区按设备默认区；blank / 错格式 → `null` |
| `ImportDuplicateLogic` | `util/image/` | 原图名规范化与批内/库内判重。`trim` + 忽略大小写；空串永不冲突 |
| `ListPaging` | `util/list/` | 一览切片 |
| `AppLog` | `util/log/` | F47：仅 `BuildConfig.DEBUG`；tag 前缀 `PO/` |

## `ui/common/`

| 类 / 组件 | 职责 |
|---|---|
| `LogScreenLifecycle` | F47：composition / Lifecycle 埋点 |
| `FabOverlayListContentPadding` | Bug9：有 FAB 的可滚列表底边净空（Scaffold 不含 FAB；约 56+16+16 dp） |
| `BackNavIconButton` | 顶栏返回 |
| `SnackbarHostStateExt` | F35：`showSnackbarReplacing` 等 |

## `model/`（纯逻辑为宜）

| 类 | 职责 |
|---|---|
| `ImageTagFilter` | 标签筛选匹配（主画面 / 打包共用） |
| `ExportZipPack` | F41/S7：`excludingIds` + `buildSingleExportPack` |
| `TagNameSaveFailure` | 标签 / 模板名保存失败分类 |
| `TagTemplateApply` | 详情套用标签模板规则 |

## 其它

FileProvider（Manifest / `res/xml`）分享 `exports/` 下 zip。
