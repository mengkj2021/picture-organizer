# 错题本（避免下一次犯同样错误）

> 汇集票验收（✅ / ❌）后「完结复盘」中提炼的**教训与规避方法**，供起票 / 接票前查阅，避免重蹈覆辙。
> 登记入口：票验收后补完结复盘（[`project-status/票库/README.md`](../project-status/票库/README.md) §6 状态机 · §7 生命周期）；每条含出处票号，可回查原票全文。
> 只记「下一次会因此受益」的内容，不做流水账；已固化为规则 / 模板 / 自动检查的教训标记「已落地」。

| 日期 | 出处（票号 / 事项） | 错题 / 教训（发生了什么） | 根因 | 规避方法 | 落地状态 |
|---|---|---|---|---|---|
| 2026-09-15 | Bug10（主画面标题点击白屏） | 导入等子画面返回后，点顶栏「图片整理」进白屏；三 Tab 同类 | 同 Bug6 族：二次/叠 `popBackStack` 把根 Main 弹出 → NavHost 空窗；此前仅 filter 收口，Import 等仍裸 pop | 全子页 `popRouteIfOnTop` + 空栈守卫；`dropUnlessResumed`；Import 返回防连点；标题本身保持无导航 | ☑️ `popRouteIfOnTop` 推广 |
| 2026-09-15 | Bug12 v3（导入画面返回键无效） | 11 个有顶栏返回的子画面有 10 个共用 `dropUnlessResumed { popRouteIfOnTop }` 同根 race；v1/v2 只修 ImportScreen 一个点，靠**用户报点驱动** | 接票后只修用户报的单个点；缺"扫描项目所有同类用法"环节；同类 API / 同族控件在多屏复用时风险被低估 | 同一类 bug 接票后**先全局扫描所有同类用法**（同类 API / 同族控件在多屏的复用位置）；抽项目级 helper（`BackNavIconButton`）取代屏内重复实现；helper 把 lifecycle 守卫收敛到一处，未来策略调整无需逐屏改 | ☑️ helper 落地 + 路由 §5 v3 增注 |
| 2026-09-14 | Bug6（筛选后回收站白屏） | 筛选返回后主画面全白；有列表数据仍白，系统返回直接 finish Activity | 连点/叠返回导致二次 `popBackStack` 把根 Main 弹出，NavHost 空窗 | 子页离开用「仅当前仍是该 route 才 pop」；应用/返回防连点；`BackHandler` 关闭中吞二次 back；打 backQueue size/route 链日志 | ☑️ `popFilterIfOnTop` + Filter 防抖 |
| 2026-09-14 | Bug9（列表末项被 FAB 遮挡） | 标签管理 / 重命名模板管理滚到底，末项被「+」FAB 盖住 | Scaffold FAB 叠在 content 上，`innerPadding` 不含 FAB | 有 FAB 的可滚列表用 `FabOverlayListContentPadding`；新画面模板 §5 勾 FAB 净空 | ☑️ 已修两处列表并写入画面模板 |
| 2026-09-10 | Bug7（详情移空返回异常） | 一览（筛选后）已移空，详情仍切到筛选条件外的图，而非返回 | 同一状态列表在详情与一览是两套口径：详情 `observeItems(status)` 全量 + 固定 `importedAt DESC`，一览为筛选 + 排序后结果 | 跨画面共用「还有没有下一项 / 顺序」这类判定时，以主画面最终可见集（或对等摘要，如可见总数）为唯一真源；判定抽纯函数 + 单测 | ☑️ 已修 `shouldReturnAfterRemoval` + `visibleSiblingCount` |
| 2026-09-10 | Bug8（编辑态标题栏间距） | 三一览点编辑后操作栏相对 TopAppBar「跳高」 | 编辑态同行塞入带 Icon 的 TextButton，工具栏未锁行高；菜单未 Box 锚定 | 主工具栏固定触控行高（如 48dp）；文案单行；Dropdown 与「更多」一样用 Box 包锚点 | ☑️ 已修 `TabActionBar` |
| 2026-09-07 | 结构1（pre-push 门禁） | hook 已设 `hooksPath` 仍可能静默不跑 | `.githooks/pre-push` 以 `100644` 入库，Unix 不执行非 +x | 入库须 `100755`；git-convention 写明 `git ls-files -s` 自检 | ☑️ 已修可执行位并写入 git-convention |
| 2026-09-05 | 原拆票流程票（已清理） | 进行中票已在当前分支提交过半时再切 `ticket/票号-…` 会分叉历史，半成品更难收 | 黄/红档「应当开分支」未写接票当时点；续接会话容易忽略已在主线 | 接票当时未开分支且已提交过半 → 本票继续当前分支；例外写入 git-convention / 接票开发 | ☑️ 已写入 git-convention「工作分支」与接票开发档位例外 |
| 2026-09-04 | F10（导入返回拦截） | `BackHandler(enabled = !isImporting)` 在导入中关掉 Handler，系统/预测性返回仍由 Navigation pop | 误以为 `enabled=false` = 禁用返回；实际是取消拦截、把事件交回系统 | 「禁止退出」场景应**始终启用** BackHandler 并在回调里吞掉；空闲才 `onBack()` | ☑️ 已写入导入画面 README §6 |
| 2026-09-04 | F7（导入 Exif 回读） | 若在 `prepareForImport` 之后读标签，开压缩时重编码已剥离 UserComment，回读永远空 | 压缩 / Bitmap 重写 JPEG 丢 Exif | 凡「从来源读元数据」必须在压缩/落盘**之前**对源 Uri 读；落盘文件以 Room 真源为准 | ☑️ 已写入架构 §3.2 / ImportViewModel |
| 2026-09-03 | Bug5（详情放大隐藏） | 捏合放大后编辑区有时不藏 / 动作怪异 | transform 与 tap 分两个 pointerInput，抬指被当成单击置 forceShow；强制显示只在回 1x 时清 | 变换后短时抑伪 tap；进缩放/捏合平移时清强制显示；回调用 rememberUpdatedState | ☑️ 已写入主图缩放.md |
| 2026-09-03 | F19（pre-push ktlint） | Git Bash 下 `sh ./gradlew` + 带空格的 `JAVA_HOME`（如 `Program Files\Android Studio\jbr`）会把 `-Xmx…` 拆成主类名，hook 假失败 | 路径空格 + 用 sh 调 wrapper | Windows hook 用 `cmd` + `gradlew.bat`，并 `cygpath -w` / 优先无空格 jdks | ☑️ 已写入 `.githooks/pre-push` |
| 2026-09-02 | Bug1（教程画面系统栏裁切） | 教程画面内容被系统栏裁切 | 新画面首屏未消费 SystemBars insets（M3 Scaffold） | 新画面按画面模板建 README 与区域文件，首屏验证 insets 表现 | ☑️ 已纳入画面模板 |
| 2026-09-02 | Bug2（详情 IME 顶栏） | 软键盘弹出后详情顶栏消失 | Scaffold 把 IME 计入整体高度 / 缺 adjustResize，顶栏被顶出 | 含输入的画面：`adjustResize` + Scaffold 排除 IME、内容区 `imePadding` | ☑️ 已落地 |
| 2026-09-02 | Bug3（筛选底栏） | 自定义 bottomBar 被导航条裁切 | bottomBar 槽位未自动等同 BottomAppBar 的 inset | 自定义 bottomBar 必加 `navigationBarsPadding` | ☑️ 已写入错题本 |
| 2026-08-27 | git 提交纪律（历史教训） | 中文 commit 信息经 PowerShell 直传变成**不可逆乱码** | 终端编码 GBK / UTF-8 混用，`-m "中文"` 直传 | commit 一律 `-F` 文件；杜绝 `-m "中文"`；中文路径用 `git add .` 之外的方式处理 | ☑️ 已规则化 git-convention.md |
