# 文档分层约定（宪法）

> **定位**：以后生成任何文档都按本文。无特殊情况，**两大世界与已冻结的目录名不再改**。  
> **关联**：落位四象限见 [AI配置落位.md](AI配置落位.md)；常驻 rules 必须短（**规则瘦身**）。  
> **改第一层**：须起票（常用 `FN`、节点 `P`），票内写「改宪法」。日常只在已有目录内加文件或按门槛拆分册。
> **已落地结构要点**（原结构改修票已删，以下为主题纪要，**不是**现行产品 F 号）：  
> - 仓库根旧 `docs/`、临时 `待整理/` 已删除  
> - AI 闭环、宪法进 workbench、`prompts/` 在仓库根、▶ include 壳化  
> - `ai-workbench/` 四分区：指南 / 宪法 / 细则 / 模板  
> - 曾有 `logs/` 过程目录已删——**过程只留票复盘 + git**  
> - **结构4** 曾建仓库根 `测试/` 效果验收清单；**已删**。原「画面验证清单」亦已撤；**全面测试公开后再起正式验证票**（阶段 ③）

## AI 阅读入口

1. 先懂闭环（下节 §0）与 [仓库结构总览](../指南/仓库结构总览.md)；**人读整体系**见 [AI开发体系总览](../指南/AI开发体系总览.md)
2. 项目真相：[仓库根 README.md](../../README.md) · [project-status/](../../project-status/) · [project-docs/](../../project-docs/)
3. 如何用真相：[如何使用-project-docs](../指南/如何使用-project-docs.md) · [如何使用-project-status](../指南/如何使用-project-status.md)
4. AI 怎么干活：[`ai-workbench/README.md`](../README.md) · [`prompts/`](../../prompts/) · [`shared/README.md`](../../shared/README.md)
5. 式样细则：先 [项目概述.md](../../project-docs/项目概述.md)；是否实现见 [阶段3 · 待对应](../../project-status/阶段/阶段3-全面测试/待对应.md) 与票内状态
6. 架构 §2 只在 [架构设计.md](../../project-docs/架构设计.md)

---

## 0. AI 开发闭环

`prompts/` + `shared/` **连接** `ai-workbench/`，以项目真相为输入、以 `android/` 为改动目标，改完再回写真相：

```
prompts/         拉入对话的编排壳
shared/          rules · skills 编排壳（同步到工具）
    ↕ ▶ include
ai-workbench/    指南 / 宪法 / 细则 / 模板 / 错题本
    ↓ 读取
project-status/  票库 · 状态 · 阶段
project-docs/    android 现状规格
    ↓ 变更
android/         唯一代码工程
    ↓ 写回
project-status/ · project-docs/
```

| 环节 | 目录 | 职责 |
|---|---|---|
| 怎么干 | `prompts/` · `shared/` | 编排壳；rules / skills（同步 `.cursor` / `.codebuddy`） |
| 干活材料 + AI 元文档 | `ai-workbench/` | 指南 · 宪法 · 细则 · 模板 · 错题本（**不是**产品规格正文） |
| 读什么 | `project-status/` · `project-docs/` | 票与是否实现、画面/架构等现状；全面测试公开后再起票 |
| 改什么 | `android/` | Kotlin / Compose / Gradle |
| 写回什么 | `project-status/` · `project-docs/` | 状态与规格对齐；**过程写在票内**（开发记录 / 完结复盘），不另建 logs |

禁止颠倒：不把产品规格写进 `prompts/` / `shared/` / `ai-workbench/`；不把 AI 纪律写成 `project-docs` 正文。

---

## 1. 两大世界（第一层 · 冻结）

| 世界 | 根路径 | 写什么 | 不写什么 |
|---|---|---|---|
| **项目相关**（真相） | [`project-docs/`](../../project-docs/)、[`project-status/`](../../project-status/)；代码在 [`android/`](../../android/) | android 现状、票与阶段、验证进度 | rules、prompts、skills、工作台材料 |
| **AI 开发** | [`prompts/`](../../prompts/)、[`shared/`](../../shared/)、[`ai-workbench/`](./) | 流程模板 + rules/skills + 材料与元文档 | 式样条文、画面规格正文、票正文 |

仓库根 [`README.md`](../../README.md) 只做门面：指向这两个世界与闭环，不堆分层细则。

不新增第三世界。不把项目真相搬进 AI 目录，不把 AI 规范写成产品规格正文。

特殊情况（才允许改第一层）：仓库形态变了（例如拆出第二个 App、公开文档必须分仓）。

---

## 2. 第二层（目录名冻结）

### 2.1 项目相关

| 目录 | 写什么 |
|---|---|
| [`project-docs/`](../../project-docs/) | **`android/` 现状文档**：架构 / 路由 / 画面规格 / 核心行为与数据层（与代码冲突以源码为准） |
| [`project-status/`](../../project-status/) | **项目状态**：票库、阶段（`待对应.md` / 下一号；全面测试公开后再起票） |

### 2.2 AI 开发 · `ai-workbench/` 内部分区 + `prompts/` + `shared/`

| 路径 | 写什么 |
|---|---|
| [指南/](../指南/) | 仓库结构总览 · 如何用 project-docs / project-status |
| 本文所在 [宪法/](./) | 分层 · include · 落位 |
| [细则/](../细则/) | 接票 · prompts · rules · skills 正文（供 ▶ include） |
| [模板/](../模板/) | 画面文档（含区域模板文件）· 票 |
| [错题本.md](../错题本.md) | 跨票教训 |
| [`prompts/`](../../prompts/) | 拉入才跑的编排壳 |
| [`shared/rules/`](../../shared/rules/) · [`shared/skills/`](../../shared/skills/) | 常驻纪律 / 技能编排壳 |

`prompts` / `shared` **▶ include** `ai-workbench/细则/`；用法见 [include约定.md](include约定.md)。

---

## 3. 先选世界再落目录

新文档先判世界，再进对应目录。禁止「先写再找地方」。

| 问自己 | → |
|---|---|
| 这是票 / 阶段待对应 / 排期 / **是否实现**？ | `project-status/`（状态看票内 + `阶段/*/待对应.md`；票正文在 `票库/`；全面测试公开后再起票） |
| 这是错题本 / 阶段复盘 / 画面·票模板 / **分层宪法·落位**？ | `ai-workbench/` |
| 这是架构 / 路由 / **画面规格** / 已实现业务与数据层说明？ | `project-docs/`（= android 现状镜像；画面用 `project-docs/画面/`） |
| 这是过程 / 开发流水？ | **票内**开发记录与完结复盘（+ git）；**不**单独建 logs 目录 |
| 这是拉入对话的**提示词流程**？ | `prompts/` |
| 这是 AI **纪律 / 技能**？ | `shared/` |
| 两者都沾？ | 拆成两份：真相在 `project-docs/` / `project-status/` 等，流程在 `prompts/`，纪律/技能在 `shared/`，材料与元文档在 `ai-workbench/`，互相链接 |

AI 元文档变更须回写 [AI配置落位.md](AI配置落位.md) 盘点表。

---

## 4. 拆 / 不拆门槛

命中 **≥2 条**才把一篇长文拆成「短索引 + 分册」：

1. 单文件明显变长，且含 ≥3 个可独立读的主题
2. 接票经常只需要其中一章
3. 已有自然章节边界

**不拆**：

- 单主题或短文（如 [路由设计.md](../../project-docs/路由设计.md)）
- 时间线：开发日志、单张票、开发记录附录
- 已是一目录一主题（画面、票）
- 高频 AI 锚点必须留在权威路径（架构 **§2** 在 `project-docs/架构设计.md`）

不删内容，只搬家。权威入口文件名不改。搬家时旧路径**默认可**留短跳转——权威已迁且用户明确要求删除跳转时，可不留（如曾在 `shared/` 的宪法跳转）。**整目录已清空且无读者依赖时（如曾删仓库根 `docs/`）可不留跳转。**

---

## 5. 索引页与分册写法

### 5.1 索引（长文变短文）

1. 文首「AI 阅读入口」3～7 条（路径 + 节号）
2. 每章：2～5 行摘要 + **位置**（相对链接到分册）
3. 高频锚点（如架构 §2、式样 §5 画面表）可全文留在索引

### 5.2 分册

- 保留原节号（`### 4.1 导入` 仍叫 4.1），旧票写「式样 §4.1」仍能对上
- 文件名：`project-docs/` 用直白名（如 `核心流程.md`）
- 分册开头一行：所属索引路径（整理期可省略）

---

## 6. 各类生成模板

| 类型 | 怎么生成 |
|---|---|
| 式样 / 设计说明 | 索引 + 按门槛分册；落 `project-docs/` |
| 画面 | 复制 [模板/画面文档/](../模板/画面文档/) → `project-docs/画面/<名>/`；技能 `create-screen-doc` |
| 票 | 复制 [模板/票/](../模板/票/) → `project-status/票库/<分区>/<票号>-简述.md` |
| 过程记录 | 写在票「开发记录 / 完结复盘」；**勿**新建 `logs/` 或按日散文件 |
| AI 元文档（宪法/指南类） | 落 `ai-workbench/宪法/` 或 `指南/`，并回写落位盘点表 |
| 提示词 / rules / skills **编排壳** | 落 `prompts/` 或 `shared/`；细则用 ▶ include（见 [include约定.md](include约定.md)） |
| 细则正文 | 落 `ai-workbench/细则/` |

---

## 7. 读法（给人 / 给 AI）

1. 先票或当前 prompt 填写区 → 再按 §0 闭环选读
2. 打开该世界入口 README，**不通读**整库
3. 打开权威索引，按「位置」只开需要的分册
4. 禁止把式样 / 架构 / 本宪法全文粘进 alwaysApply rules
