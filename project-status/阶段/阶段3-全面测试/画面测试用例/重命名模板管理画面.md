# 重命名模板管理画面

> [docs](../../../../project-docs/画面/重命名模板管理画面/README.md)

## 列表与入口

- [ ] **RT-01** 列表显示名称、pattern、默认徽标
- [ ] **RT-02** 占位符说明含 `{name}` `{date}` `{yyyy}` `{mm}` `{dd}` `{time}` `{tag}` `{tags}` `{n}`
- [ ] **RT-03** FAB → 编辑画面（添加，无 templateId）
- [ ] **RT-04** 点行 / 编辑 → 编辑画面（带 templateId）
- [ ] **RT-05** 从编辑保存返回 → Snackbar 成功（替换式）
- [ ] **RT-06** 列表滚到底末项不被 FAB 挡（Bug9）

## 删除与默认

- [ ] **RT-07** 删除确认 → 列表移除；取消不删
- [ ] **RT-08** 多模板「设为默认」互斥，仅一个默认
- [ ] **RT-09** 无模板时详情/主画面批量重命名入口禁用（交叉抽测）
