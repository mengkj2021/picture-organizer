# Bug8 票：V · 三一览编辑态标题栏间距异常

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | Bug8 |
| 类型 | Bug 票 |
| 标题 | V · 三一览点编辑后第二层标题栏与上方间距变大 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-08 |
| 所属节点 | V |
| 涉及节点 | V |
| 提出 | 用户 |
| 关联 | [主画面](../../../project-docs/画面/主画面/)；`TabActionBar` / `ImageListTab`；近源 [F5](../功能体验/F5-三一览工具栏溢出菜单.md) / [T2](../调查/T2-三一览标题栏优化.md)（已完结，本票为新视觉回归） |
| 档位 | 绿 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/主画面/`（待处理 / 已确认 / 回收站一览 · 菜单）
- **实际改动**：
  - 无需改文档（仅 `TabActionBar` 行高与锚定收紧）

## 现象（用户可观察）

1. 复现步骤：三个一览任一 Tab → 点「编辑」
2. 实际结果：第二层标题栏（操作栏）与上方间距变大
3. 期望结果：进入/退出编辑态时纵向间距稳定，无明显跳动变大

## 影响范围

- 涉及画面 / route：`main` 三 Tab
- 涉及数据或文件：无

## 根因（调查后填）

- 初步判断：编辑态额外行（如「移动到」）或 padding / inset 叠加导致
- 定位证据：编辑态在**同一行**插入「移动到▼」「重命名▼」带 Icon 的 `TextButton`，且 DropdownMenu 未用 `Box` 锚定；窄屏下文案易折行 / 行高被抬高，表现为操作栏相对 TopAppBar「跳高」。并非新开第二行。

## 已锁定（接票时确认；绿档可写「见现象/期望」或省略本节）

- 见现象 / 期望；不改交互语义（不把重命名收进「更多」）

## 实施步骤（接票后填；绿档可写「一步完成」）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | `TabActionBar` 主行固定 48dp、单行文案、移动/重命名 `Box` 锚定 | ✅ | 无纯逻辑，未加测试 |

## 方案（接票后填）

- 文档改动（画面文档 / 式样 / 架构，若涉及）：无需
- 代码改动点：`TabActionBar.kt`
- 验证方式：三 Tab 进退编辑态，操作栏高度稳定；移动到 / 重命名 / 更多菜单可用

## 验收标准

- [x] 三 Tab 编辑态间距不再异常变大
- [x] 非编辑态与编辑操作（移动到等）回归正常
- [x] 票内开发记录（带本票号）已写

### 验收对照自评（Bug8）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 三 Tab 编辑态间距不再异常变大 | ☑ | `TabActionBar` 主行 `height(48.dp)` + 文案 `maxLines=1`；编辑态不再因折行/Icon 抬高行高 |
| 非编辑态与编辑操作（移动到等）回归正常 | ☑ | 移动/重命名改为 `Box` 锚定 DropdownMenu（同「更多」）；按钮与菜单逻辑未改 |
| 票内开发记录（带本票号）已写 | ☑ | 本表开发记录 |

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-10 | 固定工具栏行高；编辑态菜单 Box 锚定；单行文案 | 59b0812 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **此 bug 的产生原因**（根因链，非表面现象）：F14 在编辑态同行塞入「重命名」后，工具栏未锁死行高；带 ArrowDropDown 的 TextButton + 未 Box 锚定的菜单在窄宽下把行撑高
- **为什么此前没被发现 / 防不住**：F5 只解横向过挤；未对编辑态增量控件做纵向约束
- **修改是否带来新风险**（若有，转新票或备注）：极窄屏文案可能 Ellipsis；功能仍可达
- **错题本登记**：已登记 [ai-workbench/错题本.md](../../../ai-workbench/错题本.md)
