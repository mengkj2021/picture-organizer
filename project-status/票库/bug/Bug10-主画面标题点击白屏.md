# Bug10 票：V · 主画面顶栏标题点击后白屏

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | Bug10 |
| 类型 | Bug 票 |
| 标题 | V · 导入等子画面返回后点顶栏「图片整理」进入白屏 |
| 状态 | ✅ 已完成（2026-09-15；用户真机主路径 + 三 Tab 全部通过；标题无导航保留） |
| 起票日期 | 2026-09-15 |
| 所属节点 | V |
| 涉及节点 | V |
| 提出 | 用户（画面验证点验） |
| 关联 | [主画面](../../../project-docs/画面/主画面/README.md)；近源 [Bug6](Bug6-筛选后回收站白屏.md)（Nav 二次 pop / 空窗白屏）；埋点 [F47](../功能体验/F47-Debug专用AppLog.md) |
| 档位 | 黄 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/主画面/README.md`（顶栏「图片整理」行为；三 Tab 共用壳）
  - `project-docs/路由设计.md`（`main` 与 `import-images` 等子栈）
  - 可能波及：导入 / 设置 / 筛选 / 详情等从主画面 push 再返回的路径（待确认）
- **实际改动**：
  - `project-docs/画面/主画面/README.md`（标题无导航；安全 pop）
  - `project-docs/画面/导入画面/README.md`（返回防连点 + 安全 pop）
  - `project-docs/路由设计.md` §5（安全 pop / 勿空栈）
  - （原 `测试/画面/主画面.md` Bug10 测项；细测册已删）
  - 代码：`PictureOrganizerNavHost.kt`、`ImportScreen.kt`

## 现象（用户可观察）

1. 复现步骤：
   1. 主画面（待归档一览）冷启动或稳定态：点顶栏标题「图片整理」→ **无动作**（可接受）
   2. 点「导入图片」进入导入画面，再返回主画面
   3. 再点顶栏标题「图片整理」→ **进入白屏**
   4. 用户反馈：已归档 / 回收站一览有**类似**问题（接票时三 Tab × 常见子画面往返一并点验）
2. 实际结果：子画面往返后，点顶栏标题会白屏（疑似 NavHost 空窗）
3. 期望结果：
   - 顶栏标题「图片整理」**始终无导航动作**（与式样：仅设置齿轮进设置）
   - 任意子画面返回后，主画面内容区仍为列表 / 空态，不白屏
   - 修法覆盖三 Tab；接票时全面检查同类往返，整理根因并写入错题本，避免再犯

## 影响范围

- 涉及画面 / route：`main`（三 Tab）；子画面离开路径（Import / Settings / Filter / Detail 等）
- 涉及数据或文件：无（导航栈，非 Room）

## 根因（调查后填）

- **根因链（同 Bug6 族）**：子画面离开时连点 / 叠返回导致二次 `popBackStack`，把根 `main` 弹出 → NavHost 空窗白屏。标题本身无 `onClick`；表面「点标题白屏」是往返后栈已脆 / 二次 pop 的表现，非标题导航。
- **为何仅 filter 修过仍复发**：Bug6 只收口 `popFilterIfOnTop`；Import 等仍裸 `popBackStack`，无空栈守卫。
- 定位证据：代码对照 Bug6 + `compileDebugKotlin` 通过；真机 F47 `PO/Nav` 栈摘要待点验补记。

## 已锁定（接票时确认；绿档可写「见现象/期望」或省略本节）

- 顶栏「图片整理」**永不导航**（与式样一致；标题无 `onClick` 须保持）
- 任意子画面返回后，主画面不得被 pop 出栈 → 禁止 NavHost 空窗白屏
- 修法覆盖三 Tab；至少点验：导入 / 设置 / 筛选（Bug6 回归）往返后再点标题
- 先测后写：**否**（纯导航 / UI，无 JVM 纯逻辑可抽）
- 拆票：未达门槛；档位 **黄**；合入 `direct-main`

## 实施步骤（接票后填；绿档可写「一步完成」）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | F47 / 代码对照定根因为二次 pop 空栈（同 Bug6） | ☑ | 标题无 onClick；Import 等裸 pop |
| 2 | `popRouteIfOnTop` + 空栈守卫；全子页 `onBack` / `dropUnlessResumed`；Import `closing` | ☑ | 主修 |
| 3 | 标题保持无 `onClick`（无证据表明标题另有导航） | ☑ | |
| 4 | 文档 + 错题本；真机三 Tab 点验 | ✅ | 用户真机点验 2026-09-15 通过（主路径 + 三 Tab） |

## 方案（接票后填）

- 文档：主画面 / 导入 / 路由 §5（原测试主画面测项已随细测册删除）
- 代码：`popRouteIfOnTop`；子页 `dropUnlessResumed`；Import 防连点
- 验证：三 Tab × 导入/设置/筛选往返后点标题；Bug6 回归；`PO/Nav`

## 验收标准

- [x] 子画面返回后点顶栏「图片整理」不再白屏；标题本身无导航
- [x] 待归档 / 已归档 / 回收站三 Tab 同类往返回归通过
- [x] 根因写入票复盘；有复用价值则登记错题本
- [x] 票内开发记录（带本票号）已写

### 验收对照自评（Bug10）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 子画面返回后点顶栏「图片整理」不再白屏；标题本身无导航 | ☑ | 代码：`popRouteIfOnTop` + Import `closing`；标题仍无 onClick。**用户真机主路径已确认** |
| 待归档 / 已归档 / 回收站三 Tab 同类往返回归通过 | ☑ | **用户真机三 Tab 全通过**（待归档 / 已归档 / 回收站 × 导入 / 设置 / 筛选 往返） |
| 根因写入票复盘；有复用价值则登记错题本 | ☑ | 本票根因/复盘；`ai-workbench/错题本.md` Bug10 行 |
| 票内开发记录（带本票号）已写 | ☑ | 本票开发记录 |

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-15 | 接票：黄档不拆；方案确认后落地 `popRouteIfOnTop`、空栈守卫、子页 `dropUnlessResumed`、Import 防连点；文档+错题本 | f36f55e |
| 2026-09-15 | Bug10 收口：用户真机确认主路径 + 三 Tab 全通过；台账 / 改动清单 §0§1§2 同步 | （本收口 commit） |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **此 bug 的产生原因**（根因链，非表面现象）：子页离开二次/叠 `popBackStack` 卸掉根 Main → NavHost 空窗；标题无导航，表象易误判为「点标题进白屏」
- **为什么此前没被发现 / 防不住**：Bug6 只修 filter；其它子页仍裸 pop；缺空栈守卫
- **修改是否带来新风险**：非当前 route / 将空栈时 skip pop（可能吞多余返回，属预期）；根 Main 系统返回退出应用逻辑未改
- **错题本登记**：已登记 [ai-workbench/错题本.md](../../../ai-workbench/错题本.md)
