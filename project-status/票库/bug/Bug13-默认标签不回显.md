# Bug13 票：V · 默认标签再次进入不回显

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | Bug13 |
| 类型 | Bug 票 |
| 标题 | V · 默认标签画面保存后再进不回显已勾选 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-16 |
| 所属节点 | V |
| 涉及节点 | V |
| 提出 | 用户（画面验证点验） |
| 关联 | [默认标签画面](../../../project-docs/画面/默认标签画面/)；近源联动 [F48](../功能体验/F48-标签改名联动.md) · [F49](../功能体验/F49-删标签清默认标签.md) |
| 档位 | 绿 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/默认标签画面/README.md`（§3/§4 回显与保存语义若需写明）
- **实际改动**：
  - 无需改文档（回显为既有规格实现缺口）

## 现象（用户可观察）

1. 复现步骤：设置 → 默认标签 → 勾选若干库标签 → 点保存返回 → 再次进入默认标签
2. 实际结果：看不到当前已保存的默认集合（表现为空选或无法辨认上次勾选）
3. 期望结果：再次进入时 Checkbox **回显** DataStore 中已保存的勾选（不是空选）

## 影响范围

- 涉及画面 / route：`default-tags`
- 涉及数据或文件：`UserPreferencesRepository.defaultTagNames`（DataStore）

## 根因（调查后填）

- **根因**：`collectAsStateWithLifecycle(emptySet())` 占位触发 `LaunchedEffect`，`selected == null` 仅首次生效，被锁死在初始空集，DataStore 真实值到达后不再同步。
- 定位证据：`DefaultTagsScreen.kt` 草稿同步逻辑

## 已锁定（接票时确认；绿档可写「见现象/期望」或省略本节）

- 见现象 / 期望；保存语义以点「保存」写入为准（画面文档 §6）

## 实施步骤（接票后填；绿档可写「一步完成」）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | `userEdited` 标志：未编辑前持续跟 `stored` Flow | ✅ | 一步完成 |

## 方案（接票后填）

- 文档改动（画面文档 / 式样 / 架构，若涉及）：无
- 代码改动点：`DefaultTagsScreen.kt` 草稿同步
- 验证方式：保存后再进勾选完整回显；未保存返回不写

## 验收标准

- [x] 保存后再进：上次勾选完整回显
- [x] 未点保存直接返回：不写入；再进仍为进入前已存集合
- [x] 票内开发记录（带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-16 | `userEdited` 修复 Flow 初值锁死草稿 | （本次提交） |

### 验收对照自评（Bug13）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 保存后再进：上次勾选完整回显 | ☑ | `DefaultTagsScreen`：`!userEdited` 时 `draft = stored` |
| 未点保存直接返回：不写入；再进仍为进入前已存集合 | ☑ | 仅保存按钮写 DataStore；返回不写 |
| 票内开发记录（带本票号）已写 | ☑ | 本票开发记录 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **此 bug 的产生原因（根因链，非表面现象）**：Compose 初值 `emptySet()` + `selected == null` 一次性初始化，错把占位当真实数据。
- **为什么此前没被发现 / 防不住**：单测未覆盖 Compose 草稿与 Flow 初值竞态；真机点验才暴露。
- **修改是否带来新风险（若有，转新票或备注）**：用户编辑后不再跟 Flow 变化（符合本屏「进屏加载、保存写入」语义）。
- **错题本登记**：已登记 [ai-workbench/错题本.md](../../../ai-workbench/错题本.md) ／ 不适用
