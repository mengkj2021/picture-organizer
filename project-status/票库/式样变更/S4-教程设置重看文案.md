# S4 票：V · 教程设置重看：跳过→关闭、开始使用→返回

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | S4 |
| 类型 | 式样变更票 |
| 标题 | V · 从设置进入教程时，「跳过」改「关闭」、「开始使用」改「返回」 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-07 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [教程画面](../../../project-docs/画面/教程画面/README.md)（`fromSettings`）；设置→查看教程 |
| 档位 | 绿 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/教程画面/README.md`
- **实际改动**：
  - `project-docs/画面/教程画面/README.md`（§3 / §5 双文案）

## 变更内容（已确认的式样）

- 现状：教程顶栏「跳过」、末页「开始使用」在首启与设置重看两条入口文案相同（行为已按 `fromSettings` 分支：设置路径 `popBackStack`）
- 变更为：当 `fromSettings=true` 时，顶栏「跳过」→「关闭」；末页「开始使用」→「返回」（行为不变，仍回设置、不改 `tutorial_completed`）
- 变更理由：设置内重看是查阅而非首次引导，「跳过 / 开始使用」语义不当
- 是否涉及「待定」项澄清：否

## 已锁定 / 实施步骤 / 档位

- **档位**：绿
- **步骤**：
  1. 画面文档 §3/§5 对齐双文案；三语新增 `tutorial_close` / `tutorial_return`
  2. `TutorialScreen` 接收 `fromSettings`，顶栏/末页按分支选文案；NavHost 传入已有参数
- **先测后写**：无纯逻辑，未加测试

## 验收标准

- [x] 设置→查看教程：顶栏为「关闭」、末页为「返回」；点后回设置且不改完成标记
- [x] 首启教程仍为「跳过 / 开始使用」
- [x] 三语文案齐全；画面文档与代码对齐
- [x] 票内开发记录已写

### 验收对照自评（S4）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 设置→查看教程：顶栏为「关闭」、末页为「返回」；点后回设置且不改完成标记 | ☑ | `TutorialScreen` 按 `fromSettings` 用 `tutorial_close`/`tutorial_return`；NavHost 设置路径仍只 `popBackStack`、不写 `tutorial_completed` |
| 首启教程仍为「跳过 / 开始使用」 | ☑ | `fromSettings=false`（默认）仍用 `tutorial_skip`/`tutorial_start` |
| 三语文案齐全；画面文档与代码对齐 | ☑ | `values` / `values-ja` / `values-en` 已加键；`教程画面/README.md` §3/§5 已改 |
| 票内开发记录已写 | ☑ | 本表开发记录 |

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-08 | 设置重看文案「关闭/返回」；三语 + 画面文档；`fromSettings` 传入 UI | 2648159 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **变更的好处**：设置内重看语义正确，不再像「跳过首次引导」
- **坏处与代价**：多两个 string 键；UI 多一布尔分支（极小）
- **若重新选择，是否仍会做**：会
- **错题本登记**：不适用
