# S7 票：V · 打包改为单 zip

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | S7 |
| 类型 | 式样变更票 |
| 标题 | V · 打包导出每次只打一个 zip（默认可筛的全部已归档） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-16 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [图片打包画面](../../../project-docs/画面/图片打包画面/)；近源 [F41](../功能体验/F41-打包预览排除.md) · [F42](../功能体验/F42-打包zip文件名.md) · [F43](../功能体验/F43-已打包文件管理.md) |
| 档位 | 黄 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/图片打包画面/README.md`（职责 / 功能清单：取消「每标签一包」）
  - `project-docs/画面/图片打包画面/预览区.md`、`输出文件名.md`（单包命名与预览）
  - 教程 / 核心流程若写「按标签分包」一并改
- **实际改动**：
  - `project-docs/画面/图片打包画面/README.md`
  - `project-docs/画面/图片打包画面/输出文件名.md`
  - `project-docs/画面/图片打包画面/预览区.md`
  - `project-docs/工具类.md`

## 变更内容（已确认的式样）

- 现状：对已归档按标签筛选后 **每标签一包**（无筛选时每个出现过的用户标签各一包 + 未打标签一包）；F42 支持**每包**改 stem。
- 变更为：**每次只打一个 zip**；默认范围 = 当前筛选下的全部已归档图片（无筛选则全部已归档）；仍可用筛选收窄，仍可用预览叉掉排除（F41）；文件名改为**单个**可编辑主名（F42 语义收缩为单 stem）。
- 变更理由：用户点验认为「每次只打一个包」才是期望行为；多包按标签拆分不符合当前用法。
- 是否涉及「待定」项澄清：否（单包范围与默认集合已由用户确认；接票时锁定默认文件名规则）

## 已锁定（接票时确认）

- 默认主名 `export_{yyyyMMdd}.zip`；筛选 / 预览排除语义不变。

## 实施步骤（接票后填）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | 文档先行：打包画面 README / 输出文件名 / 预览区 | ✅ | |
| 2 | `buildSingleExportPack` 替换 `buildExportGroups` | ✅ | |
| 3 | ViewModel / Screen / 三语文案 | ✅ | |
| 4 | 单测更新 | ✅ | ExportZipPackTest / ExportZipNamesTest |

## 落地范围（接票后填）

- 式样书 / 画面文档章节：图片打包画面 README、输出文件名、预览区
- 涉及的画面 / route / 数据模型：`export-zip`；`ExportZipPack.kt`；`ExportZipViewModel` / `ExportZipScreen`
- 代码改动点：见上
- 旧数据 / 迁移影响（若有）：无（历史多包 zip 仍可在 `export-manage` 管理）

## 验收标准

- [x] 文档先行：打包画面不再写「每标签一包」；单包 + 筛选 + 排除语义一致
- [x] 开始打包只生成 **一个** zip；内容为筛选∩预览剩余的已归档图
- [x] 单文件名编辑可用；非法名拦截行为保留
- [x] 票内开发记录（带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-16 | 单 zip 导出；`defaultSingleZipStem`；文档与单测对齐 | （本次提交） |

### 验收对照自评（S7）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 文档先行：打包画面不再写「每标签一包」；单包 + 筛选 + 排除语义一致 | ☑ | 打包画面 README / 输出文件名 / 预览区 / 工具类 |
| 开始打包只生成 **一个** zip；内容为筛选∩预览剩余的已归档图 | ☑ | `ExportZipViewModel.startExport` 单包；`buildSingleExportPack` |
| 单文件名编辑可用；非法名拦截行为保留 | ☑ | 单 `OutlinedTextField`；`ExportZipNames.validateZipStem` 不变 |
| 票内开发记录（带本票号）已写 | ☑ | 本票开发记录 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **变更的好处（已达成 / 预期）**：符合用户「一次导出一个包」预期；UI 简化为单文件名。
- **坏处与代价（迁移成本、回归面、使用习惯变化等）**：无法再按标签自动分包；历史多包 zip 仍保留在管理列表。
- **若重新选择，是否仍会做（理由）**：会；点验确认多包非期望行为。
- **错题本登记**：已登记 [ai-workbench/错题本.md](../../../ai-workbench/错题本.md) ／ 不适用
