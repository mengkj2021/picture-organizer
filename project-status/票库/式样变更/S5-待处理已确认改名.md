# S5 票：V · 主画面「待处理 / 已确认」显示名改优雅正式名

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | S5 |
| 类型 | 式样变更票 |
| 标题 | V · 待处理 / 已确认 → 待归档 / 已归档（键 + 显示名） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-07 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [主画面](../../../project-docs/画面/主画面/README.md)；[F28 状态显示名本地化](../功能体验/F28-状态显示名本地化.md)；S3 回收站改名先例 |
| 档位 | 黄 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/主画面/`（三 Tab、空态、移动到等）
  - 教程 / 详情保留字提示等凡展示「待处理」「已确认」处
- **实际改动**：
  - `project-docs/画面/主画面/`（README；区域目录改名 `待归档一览` / `已归档一览`；回收站区域交叉引用）
  - `project-docs/画面/教程画面/README.md`
  - `project-docs/画面/导入画面/README.md`
  - `project-docs/画面/图片详细画面/状态操作.md`
  - `project-docs/画面/图片打包画面/README.md`
  - `project-docs/核心流程.md` / `图片存储管理.md` / `数据流.md` / `技术栈.md` / `架构设计.md` §2 / `画面/README.md`

## 变更内容（已确认的式样）

- 现状：主画面 Tab 等处显示「待处理」「已确认」（库内键亦为中文，见 F28）
- 变更为：显示层与库内键改为更优雅、正规的名称
- 变更理由：现名偏口语 / 流程感，希望更正式
- 是否涉及「待定」项澄清：**是**——已锁定（见下）

## 需求确认（接票前）

- [x] 新中文名（待处理 → 待归档；已确认 → 已归档）
- [x] 日 / 英显示名（未分類 / 分類済み；Unfiled / Filed）
- [x] 仅显示层 vs 改库内键 → **键 + 显示名都改**；枚举 / 目录 `pending/` `confirmed/` 不动

## 已锁定（接票时确认）

- 中：待归档 / 已归档；日：未分類 / 分類済み；英：Unfiled / Filed
- Tab 与回收站同构：中「名+一览」、日「名+一覧」、英无后缀
- `STATUS_TAGS` = 待归档 / 已归档 / 回收站
- 旧中文键「待处理 / 已确认」继续 strip + 禁止当标签（新旧日英别名均拦输入）
- `ImageStatus` / 目录名不动

## 实施步骤（接票后填）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | 先测：STATUS_TAGS / 旧键 strip / 别名 | ✅ | ImageListItemTest 等 |
| 2 | 实现键 + 三语文案 + Exif strip | ✅ | |
| 3 | 画面文档目录改名与交叉引用 | ✅ | |
| 4 | 验收自评 + 台账 | ✅ | `testDebugUnitTest` 全绿 |

## 落地范围（接票后填）

- 式样书 / 画面文档章节：无独立式样书；画面 / 核心流程 / 技术栈 / 架构 §2
- 涉及的画面 / route / 数据模型：`main` 三 Tab；教程；详情 / 标签保留字；打包；`ImageListItem.STATUS_TAGS`
- 代码改动点：`ImageListItem.kt`；`ImageTagMetadata`；三语 `strings.xml`；相关单测；Mock 走 `userTagsOf`
- 旧数据 / 迁移影响：无 DB 迁移；历史 tags / Exif 旧中文键仍剥离

## 验收标准

- [x] 待定项已锁定
- [x] 用户可见「待处理 / 已确认」按新显示名；筛选 / 移动 / 保留字行为不坏
- [x] 文档与三语对齐；票内开发记录已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-09 | 锁定待归档/已归档；先测后写；键+三语+文档目录改名 | 9ff24a3、434580f |

### 验收对照自评（S5）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 待定项已锁定 | ☑ | 票内「已锁定」：待归档/已归档；未分類/分類済み；Unfiled/Filed；键+显示都改；Tab 同回收站 |
| 用户可见「待处理 / 已确认」按新显示名；筛选 / 移动 / 保留字行为不坏 | ☑ | `status_name_*` / Tab / 空态 / 教程 / 保留字提示已换新名；`userTagsOf` 剥新键+旧键；`isReservedStatusName` 拦新旧中日英；`testDebugUnitTest` 全绿 |
| 文档与三语对齐；票内开发记录已写 | ☑ | 主画面区域目录改名；教程/导入/详情/打包/核心流程/技术栈；`values` `values-ja` `values-en` |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **变更的好处**：Tab 用语更正式，与「整理 / 归档」产品语义对齐；三语同步
- **坏处与代价**：内部枚举仍 Pending/Confirmed、目录仍 `pending/`/`confirmed/`，需对照显示名；旧键必须长期留在 strip/别名，比 S3 丢掉「不修改」更重
- **若重新选择，是否仍会做**：会——用户明确要更正式名；旧键兼容成本可接受
- **错题本登记**：不适用
