# S6 票：V · 教程正文排版优化（分段 / 重点 / 序号）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | S6 |
| 类型 | 式样变更票 |
| 标题 | V · 教程三页长文改为分段、有重点与序号的可读排版 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-14 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [教程画面](../../../project-docs/画面/教程画面/README.md)；近源 [F12](../功能体验/F12-教程内容丰富.md)（文案内容已落地，本票只改呈现） |
| 档位 | 黄（三语正文结构 + Compose 呈现） |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/教程画面/README.md`（§5 文案呈现约定）
- **实际改动**：
  - `project-docs/画面/教程画面/README.md`（§5 正文呈现约定 + 三页要点表）
  - （原 `测试/画面/已通过/教程画面.md` 测项对齐；细测册已删）
  - 代码：`ui/tutorial/TutorialBody.kt`、`TutorialScreen.kt`；三语 `tutorial_page*_body`

## 变更内容（已确认的式样）

- 现状：F12 后每页一大段长文本，可读性差、「有点丑」
- 变更为：在**不改信息量主题骨架**前提下，优化呈现——分段、突出重点、有序号/条目层级（三语同步）
- 变更理由：长文已够用，缺视觉层次
- 是否涉及「待定」项澄清：**部分**——具体分段层级与重点样式（纯字符串换行 vs `AnnotatedString` / 小标题）接票时锁定一版即可，不另开调查票

## 已锁定（接票时确认；绿档可写「见变更内容」或省略本节）

- **呈现技术**：不引入 `AnnotatedString` / Markdown；三页仍各一条 `tutorial_pageN_body`
- **正文格式**：`\n` 分段；条目行 `N. 短标题`，随后行为说明（条目间空行可选）
- **渲染**：同包 `parseTutorialBody` → 引言 `bodyLarge`；序号短标题 `titleSmall`（重点）；说明 `bodyMedium`
- **信息量**：F12 主题骨架与要点不增不减；三语结构对齐（页 1：引言+三步；页 2：引言+四条；页 3：五条无引言）
- **先测后写**：解析为可测纯逻辑；壳层 / 首启·重看行为不改

## 实施步骤（接票后填；绿档可写「一步完成」）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | `parseTutorialBody` 单测 | ☑ | `TutorialBodyParserTest` 绿灯 |
| 2 | 解析器 + `TutorialScreen` 块渲染 | ☑ | |
| 3 | 三语 strings + 画面文档 §5 + 测试清单 | ☑ | |

## 落地范围（接票后填）

- 式样书 / 画面文档章节：`project-docs/画面/教程画面/README.md` §5
- 涉及的画面 / route / 数据模型：`tutorial`；无新 route / 无数据模型
- 代码改动点：`ui/tutorial/TutorialBody.kt`、`TutorialScreen.kt`；`values*` `tutorial_page*_body`
- 旧数据 / 迁移影响（若有）：无

## 验收标准

- [x] 教程三页呈现有分段 / 重点 / 序号（或等价条目层级），明显优于单墙文本
- [x] 三语文案结构对齐；画面文档 §5 与实现一致
- [x] 首启 / 设置重看行为与 S4 / F12 不变
- [x] 票内开发记录（带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-14 | 锁定：单 body 字符串 + `parseTutorialBody`；三语分段；`TutorialBodyText` 渲染；画面 §5 / 测试清单对齐 | 4fdc61e |

### 验收对照自评（S6）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 教程三页呈现有分段 / 重点 / 序号（或等价条目层级），明显优于单墙文本 | ☑ | 三语 `tutorial_page*_body` 改为引言+`N. 短标题`；`TutorialBodyText` 用 `titleSmall`/`bodyMedium`；验证：真机构建资源格式 + 解析单测 |
| 三语文案结构对齐；画面文档 §5 与实现一致 | ☑ | 页 1=引言+3、页 2=引言+4、页 3=5 条；`教程画面/README.md` §5 已写约定与表 |
| 首启 / 设置重看行为与 S4 / F12 不变 | ☑ | 仅换正文渲染；`Scaffold` / Pager / `fromSettings` 分支未改 |
| 票内开发记录（带本票号）已写 | ☑ | 本表 + 完结复盘 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **变更的好处**：三页可扫读；短标题突出重点；解析可单测，后续改文案不必动 Compose 结构
- **坏处与代价**：`strings.xml` 依赖 `N. ` 行约定；格式写错会回落成普通段
- **若重新选择，是否仍会做**：会；比 AnnotatedString / 多 key 拆条更轻
- **错题本登记**：不适用
