# F33 票：V · 回收站一览清空全部

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F33 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 新功能 |
| 标题 | V · 回收站 title 右上增加「清空」，删除回收站内全部图片 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-07 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [主画面/回收站一览](../../../project-docs/画面/主画面/回收站一览/)；删除语义对齐现有「删除」（真删文件+DB） |
| 档位 | 黄 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/主画面/回收站一览/菜单.md`（或一览.md）
  - `project-docs/画面/主画面/README.md`
- **实际改动**：
  - `project-docs/画面/主画面/回收站一览/菜单.md`（补 F33「清空」）

## 用户诉求 / 场景

- 要解决的问题：回收站图多时，只能编辑态勾选分页删除，无法一次清掉全部
- 预期效果：回收站 Tab 标题栏右上方「清空」→ 确认后删除**回收站内全部**图片（不受当前页 / 当前筛选限制，除非接票时另定）
- 是否已有式样依据：否（本票确认）

## 需求确认（接票前若为「待定」先在此澄清）

- 清空范围默认：**该状态全部**（非仅当前页、非仅当前筛选）——与用户原文一致
- 是否二次确认弹窗：**要**（2026-09-08 锁定）
- 空列表时按钮：**禁用**（2026-09-08 锁定）
- 入口位置：回收站 Tab 操作栏**右侧**（对位导入 / 导出）（2026-09-08 锁定）

## 已锁定 / 实施步骤 / 档位

- **档位**：黄
- **已锁定**：确认弹窗要；空列表禁用；右侧；范围全部
- **实施步骤**：
  1. 文档：`回收站一览/菜单.md` 补「清空」
  2. UI：`TabActionBar` 右侧「清空」+ `MainScreen` 确认弹窗
  3. VM：`EmptyTrash` → 删全部 NoModify（文件+DB）
  4. 三语文案
- **先测后写**：无纯逻辑可抽，未加单测

## 验收标准

- [x] 回收站 title 右上有清空入口；确认后清空全部回收站项（文件+DB）
- [x] 不影响待处理 / 已确认
- [x] 文档与代码对齐；票内开发记录已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-08 | 锁定：确认弹窗 / 空列表禁用 / 右侧 / 范围全部；实现 EmptyTrash + 三语 + 菜单文档；`compileDebugKotlin` 通过 | 281dd37 |

### 验收对照自评（F33）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 回收站 title 右上有清空入口；确认后清空全部回收站项（文件+DB） | ☑ | `TabActionBar` 右侧「清空」；`MainScreen` 确认弹窗后发 `EmptyTrash`；`MainViewModel.emptyTrash` 取未筛选 NoModify 全量 id → `deleteItems`；空列表 `canEmptyTrash=false` 禁用 |
| 不影响待处理 / 已确认 | ☑ | `emptyTrash` 仅 `selectedTab == NoModify`；按钮仅 `MainTab.NoModify` 分支；删除走既有 `deleteItems` 按 id |
| 文档与代码对齐；票内开发记录已写 | ☑ | `回收站一览/菜单.md` 已补 F33；本票开发记录 / 自评已写 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：回收站一览右侧可一键清空全部（含确认），不必分页勾选删除
- **代价与遗留**：无；大批量清空时串行删文件，未做进度条（与现有 `deleteItems` 一致）
- **错题本登记**：不适用
