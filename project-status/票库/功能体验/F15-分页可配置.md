# F15 票：列表「是否分页」与「每页数量」改为可配置

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F15 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | 设置新增「列表分页」：分页开关 + 每页数量（默认 30，硬编码改为可配） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 完成日期 | 2026-09-05 |
| 关联 | [设置画面](../../../project-docs/画面/设置画面/README.md)；[主画面](../../../project-docs/画面/主画面/README.md)（C3 分页）；三一览「一览 / 菜单」；`UserPreferencesRepository`（DataStore） |
| 关联画面·预估 | `docs/画面/设置画面/README.md`；`docs/画面/主画面/README.md`；`docs/画面/主画面/*/一览.md`、`*/菜单.md` |
| 关联画面·实际改动 | 设置 README；主画面 README；待处理/已确认/回收站「一览.md」「菜单.md」；式样 4.3；架构 2 |
| 档位 | 黄 |
| 拆票门槛 | 未达；票内步骤完成 |

## 用户诉求 / 场景

- 要解决的问题或场景：一览列表每页固定 30 条、分页不可关；图多时翻页烦，图少时「每页 30」也要翻页，用户希望自己定
- 预期效果：设置里可「关闭分页」（一次看全当前筛选结果）或调整每页数量；默认保持现状（开分页、30 条），老用户无感
- 是否已有式样依据：否（C3 硬编码每页 30 的体验调整）

## 需求确认（接票前若为「待定」先在此澄清）

### 已锁定（接票确认）

- **存放**：`UserPreferencesRepository` 加 `list_paging_enabled`（默认 `true`）+ `list_page_size`（默认 `30`；允许值 `20 / 30 / 50 / 100`，非法值回落 30）
- **设置页 UI**：「列表」相关两行——Switch「分页显示」；每页数量单选（20 / 30 / 50 / 100，仅分页开启时可选，关闭时置灰）
- **主画面**：读偏好——关分页 = 不切片、隐藏页码栏、全选 = 当前筛选全部；开分页 = 切片 + 页码 + 全选当前页
- 排序 / 筛选不变；开关或每页数量变更 → **回到第 1 页**；仅主画面三一览，打包 / 筛选结果集不变
- **F20**：抽出 `ListPaging`，先测后写

### 实施步骤

| # | 步骤 | 状态 |
|---|---|---|
| 1 | 文档：设置 / 主画面 / 式样书分页相关段 | ✅ |
| 2 | `ListPaging` + 单测 → `UserPreferencesRepository` 两键 | ✅ |
| 3 | 设置画面 UI（开关 + 每页数量） | ✅ |
| 4 | `MainViewModel` / 页码栏读偏好；菜单文案「全选」语义 | ✅ |

## 方案（接票后填）

- 文档先行：设置 / 主画面 / 式样 / 架构已对齐
- 代码：`ListPaging` + Test；`UserPreferencesRepository`；`SettingsViewModel` / `SettingsScreen`；`MainViewModel` + `ImageListTab`；`NavHost` Factory
- 验证：单测 + ktlint 绿灯；默认行为同现状

## 验收标准

- [x] 场景达成，行为符合式样（默认与现状一致）
- [x] 文档与代码对齐
- [x] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-05 | 实现分页可配 + F20 先测后写；见开发日志 | （待用户提交） |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：设置可关分页 / 改每页 20·30·50·100；默认开+30 与 C3 一致；关分页隐藏页码栏、全选=筛选全部
- **代价与遗留**：超大列表关分页可能一次加载较多项（Compose LazyColumn 仍按需组合，可接受）；未做「每页自定义任意数」
- **错题本登记**：不适用
