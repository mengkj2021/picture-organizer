# F36 票：V · 重命名模板添加改为独立画面

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F36 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | V · 添加重命名模板改为跳转新画面（取消 Dialog） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-07 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [重命名模板管理画面](../../../project-docs/画面/重命名模板管理画面/)；[重命名模板编辑画面](../../../project-docs/画面/重命名模板编辑画面/)；建议与 [F37](F37-重命名规则按钮插入.md) 协调（新画面上放规则按钮） |
| 档位 | 红 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/重命名模板管理画面/README.md`
  - `project-docs/画面/重命名模板编辑画面/README.md`
  - `project-docs/路由设计.md`
- **实际改动**：
  - `project-docs/画面/重命名模板编辑画面/README.md`（新建）
  - `project-docs/画面/重命名模板管理画面/README.md`
  - `project-docs/路由设计.md` §3
  - `project-docs/画面/README.md` 清单

## 用户诉求 / 场景

- 要解决的问题：Dialog 内两个输入框切换焦点时弹层闪烁，体验差
- 预期效果：添加与编辑改为全屏独立 route 画面，稳定编辑
- 是否已有式样依据：否（本票新建画面文档）

## 需求确认

- [x] 编辑一并迁出 Dialog（同一编辑画面，有无 `templateId` 区分）
- [x] 画面名 / route：`重命名模板编辑画面`；`rename-template-edit` / `rename-template-edit/{templateId}`
- [x] F37 规则按钮不在本票实现
- [x] 未保存返回：直接 `popBackStack`，不二次确认

## 已锁定 / 实施步骤 / 档位

- **档位**：红（`direct-main` 当前分支）
- **步骤**：文档 → Edit Screen/VM + Routes/NavHost → 管理去 Dialog → 收尾（已完成）

## 验收标准

- [x] 添加不再用会闪的 Dialog；新画面可保存返回列表
- [x] 画面文档 + 路由登记齐全；票内开发记录已写

### 验收对照自评（F36）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 添加不再用会闪的 Dialog；新画面可保存返回列表 | ☑ | 管理画面已移除 `RenameTemplateEditDialog`；FAB/编辑 → `Routes.renameTemplateEdit`；`RenameTemplateEditScreen` 保存成功 `popBackStack` + `NotifySaved` Snackbar；`:app:compileDebugKotlin` 通过 |
| 画面文档 + 路由登记齐全；票内开发记录已写 | ☑ | `画面/重命名模板编辑画面/README.md`；路由表与画面清单已登记；本票开发记录与复盘已写 |

**先测后写**：本票为 UI 迁出 Dialog，校验仍在 `RoomRenameTemplateRepository`；无新增可测纯逻辑，未加单测。

**壳层核对**：A Scaffold+insets ☑；B imePadding ☑；C bottomBar N/A；D 直接 pop ☑；E N/A；F 与管理画面一致 ☑。

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-09 | 锁定需求；文档先行；实现编辑全屏画面并接线；管理去 Dialog | 47fcb6a |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：添加/编辑共用 `rename-template-edit` 全屏表单，消除 Dialog 焦点闪烁；保存回列表有 Snackbar
- **代价与遗留**：规则按钮插入留给 F37；真机未在本会话点按验收，需用户实机确认
- **错题本登记**：不适用
