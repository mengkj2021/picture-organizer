# F30 票：详情对齐一览的移动与删除

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F30 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | 图片详细画面增加与一览一致的「移动到」；删除仅当前图在回收站时可操作 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-06 |
| 关联 | [图片详细画面](../../../project-docs/画面/图片详细画面/README.md)；[状态操作.md](../../../project-docs/画面/图片详细画面/状态操作.md)；三一览菜单语义；S3 |
| 档位 | 黄 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/图片详细画面/README.md`
  - 三一览「菜单.md」仅对照
- **实际改动**：
  - `project-docs/画面/图片详细画面/README.md`
  - `project-docs/画面/图片详细画面/状态操作.md`（新建）
  - `project-docs/画面/README.md`（清单说明）

## 用户诉求 / 场景

- 要解决的问题或场景：详情目前主要只有重命名、加标签；在详情里想把当前图**移到其他分类**，或在回收站里**删除**，还得退回一览再编辑勾选，路径绕。
- 预期效果：详情提供与一览对应的操作——**移动到**；**删除**仅回收站；移动成功跳下一张，无剩余回一览。
- 是否已有式样依据：一览侧已确认；详情本票补齐。

## 需求确认

- 范围：单张；不含批量/打包。
- 移动目标：另两状态。
- 删除：仅回收站；真删文件 + DB。
- **入口（锁定）**：TopAppBar「更多」溢出（对齐 F5；目标需菜单，避免挤标题栏）。
- **移动成功导航（锁定）**：同状态剩余图优先下一张（末张则前一张）；无剩余 → `popBackStack`。
- **删除成功**：确认后删 → `popBackStack`。

## 已锁定

- 入口 = TopAppBar「更多」；移动成功 = 下一张 / 无则回一览；删除仅回收站 + 确认。

## 实施步骤

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | 画面文档（状态操作 + README） | ✅ | |
| 2 | `nextSiblingIdAfterRemoval` 单测 | ✅ | |
| 3 | ViewModel move/delete + Effect | ✅ | |
| 4 | TopAppBar「更多」+ 删除确认 | ✅ | |
| 5 | 三语文案 | ✅ | |

## 方案

- 文档先行：`状态操作.md` + 详情 README
- 代码：`ImageDetailUi` / `ViewModel` / `Screen`；复用 `ImageRepository.moveItems` / `deleteItems`
- 验证：`NextSiblingAfterRemovalTest` + compileDebugKotlin

## 验收标准

- [x] 详情可「移动到」另两状态；库 + 文件落点正确；缩略条随新当前图刷新
- [x] 仅回收站详情可见并可用「删除」；确认后清除并返回主画面
- [x] 待处理 / 已确认详情无删除入口
- [x] 画面文档与代码对齐
- [x] 开发记录（带 F30）已写

### 验收对照自评（F30）

| 验收项 | 结果 | 证据 |
|---|---|---|
| 详情可移动到另两状态；落点正确；缩略条刷新 | ☑ | `ImageDetailViewModel.moveCurrentTo` → `repository.moveItems`；成功后 `currentId` 切下一张；`NextSiblingAfterRemovalTest` 绿灯 |
| 仅回收站可删；确认后回主画面 | ☑ | Screen 菜单仅 `NoModify` 显示删除；`AlertDialog` 后 `DeleteCurrent` → `NavigateBack` |
| 待处理/已确认无删除入口 | ☑ | `detailMoveTargets` + `if (status == NoModify)` 门控 |
| 文档对齐 | ☑ | `状态操作.md`、详情 README、画面清单 |
| 开发记录 | ☑ | 本表 |

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-06 | 详情「更多」移动/删除；移动后下一张；单测 NextSibling | （待 commit） |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：详情可对齐一览移动；回收站可删并回一览；移动后连续浏览同状态剩余图。
- **代价与遗留**：删除成功不弹 Snackbar（避免挂起挡住返回）；移动失败有文案。
- **错题本登记**：不适用
