# F17 票：技能 create-screen-doc（新建画面文档 SOP）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F17 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 工程质量（AI 技能落地） |
| 标题 | 落地技能 `create-screen-doc`：新增画面自动走文档 SOP |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 关联 | `shared/skills/`；docs/README「新增画面流程」；画面 `_模板` |

## 用户诉求 / 场景

- 要解决的问题或场景：`shared/skills/README.md` 框架自建起就标注"高优先级建议补充 create-screen-doc / implement-screen"，至今 0 技能落地。业界共识：把重复 SOP 编码为可复用技能，让每个 agent 同一套打法，不靠个人纪律与每次重述提示词。新增画面的流程（模板 → 路由表 → 式样书第 5 章）目前依赖 prompt 每次重复，容易漏步（Bug1 教训：壳层约束没随模板带出）。
- 预期效果：调用 `create-screen-doc` 技能 → 按固定 SOP 一步不落新建画面文档（复制 `_模板/` → 登记路由 → 更新式样书画面清单 → 填 10 节模板）。
- 是否已有式样依据：否（工程资产）。

## 需求确认（接票前若为「待定」先在此澄清）

- 范围：只做 `create-screen-doc` 一个技能（`implement-screen` 见 F18，拆票便于各自验收）。
- 落地形态：按 `shared/skills/README.md` 的 SKILL.md 规范（name/description frontmatter + 触发条件 + SOP + 注意事项）；同步到工具技能目录（CodeBuddy `.codebuddy/skills/`；Cursor `.cursor/skills/`）。
- 技能内容须覆盖 Bug1 教训：SOP 里强制含"insets/edge-to-edge 壳层约束从模板带出"检查（可引画面 `_模板` 或错题本）。
- 与本项目"新画面流程"（docs/README 摘要）保持一致；流程文档有变更时技能需同步（可备注）。

## 已锁定 / 实施步骤 / 档位（F29）

| 项 | 内容 |
|---|---|
| 档位 | **黄**（多路径技能同步 + 模板补丁 + 演练；步骤 3–4） |
| 分支 | `main`（`direct-main`，未开 `ticket/`） |
| 先测后写 | **否**（工程资产 / SOP，无 JVM 纯逻辑） |

### 实施步骤

| # | 步骤 | 状态 |
|---|---|---|
| 1 | 写 `shared/skills/create-screen-doc/SKILL.md`（触发 + SOP 四步 + insets） | ☑ |
| 2 | 同步 `.codebuddy` / `.cursor` skills；更新 README / 落位表；模板 5 补 insets | ☑ |
| 3 | 演练四步后撤回产品登记；样例留 `assets/` | ☑ |
| 4 | 收尾：验收自评、开发日志、台账 | ☑ |

## 方案（接票后填）

- 文档先行：补 `_模板` 5 insets；技能正文；无业务画面长期留档
- 代码改动点：无 Android 代码
- 验证方式：演练 `F17-drill-demo` 四步齐全后撤回；对照验收表

## 验收标准

- [x] `create-screen-doc` SKILL.md 就位（触发条件 + SOP + 注意事项，含 insets 检查）
- [x] `shared/skills/README.md` 登记；工具目录同步可用
- [x] 演练一次：SOP 四步（模板→路由→式样书→填写）无漏，日志记录
- [x] 开发日志（文件名带本票号）已写

## 关联画面·预估

| 路径 | 说明 |
|---|---|
| `docs/画面/_模板/README.md` | 补 5 insets 检查 |
| （无业务新画面） | 技能工程资产 |

## 关联画面·实际改动

- `docs/画面/_模板/README.md`（5 insets）
- 演练临时触碰路由/式样/画面索引后已撤回；样例：公开前已删演练 assets（原 create-screen-doc/assets）

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-05 | 落地 create-screen-doc；双工具同步；模板 insets；演练后撤回；`.gitignore` 放行 `.codebuddy/skills/` | `116abf9` |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：首个项目技能就位；Cursor 同步路径订正为 `.cursor/skills/`；模板与错题本「已纳入模板」对齐。
- **代价与遗留**：`implement-screen` 仍待 F18；流程文档变更时须人工同步技能（技能注意事项已写）。
- **错题本登记**：不适用（未新登记；复用 Bug1 既有条目）
