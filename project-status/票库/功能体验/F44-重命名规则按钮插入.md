# F44 票：V · 重命名规则按钮插入

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F44 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | V · 命名规则以按钮插入光标处（可选手改） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-09 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | 母题 [F37](F37-重命名规则按钮插入.md)；姊妹 [F45](F45-重命名规则扩充.md)；[重命名模板编辑画面](../../../project-docs/画面/重命名模板编辑画面/)；F36 |
| 档位 | 黄 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/重命名模板编辑画面/`
- **实际改动**：
  - `project-docs/画面/重命名模板编辑画面/README.md`（§3 / §4 / §9 / §10）

## 用户诉求 / 场景

- 不要主要靠手敲 `{name}` 等；点击按钮插入到 pattern 输入框**当前光标处**
- 仍允许手改 pattern 全文

## 需求确认

- [x] 仍允许手改 pattern 文本 → **允许**
- [x] 本票规则集合 → **接票时已支持的占位符**（F45 未合入前为 `{name}` `{date}` `{tag}`；F45 合入后按钮条随总清单扩展，由 F45 收尾对齐）

## 已锁定

- 落点：重命名模板**编辑**画面（非管理列表内联）
- 交互：规则按钮 → 在 `TextField` 选区 / 光标处插入对应 `{…}`；可选手改
- 文案：按钮旁或下方保留简短占位符说明（三语；本票至少覆盖既有三规则）
- 不改 `RenamePatternApplier` 语义（扩充归 F45）

## 实施步骤

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | 画面文档：编辑画面功能清单补「规则按钮插入」 | ✅ | |
| 2 | Compose：pattern 区按钮条 + 光标插入；保留手改 | ✅ | `TextFieldValue` + AssistChip |
| 3 | 三语 strings（按钮 label / 说明） | ✅ | `rename_template_insert_rules`；hint 沿用 |

## 方案

- 文档先行：改编辑画面 README §3 / 待定
- 代码：`RenameTemplateEditScreen`；`TextFieldValue` 管选区；`insertAtSelection` 纯函数（先测后写）
- 验证：单测 `PatternTokenInsertTest`；手动点插入到光标 / 选区替换；手改仍可保存

## 验收标准

- [x] 可对已支持规则点按钮插入到光标（或替换选区）
- [x] 仍可手改 pattern 并保存生效
- [x] 说明文案三语齐全；票内开发记录已写

### 验收对照自评（F44）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 可对已支持规则点按钮插入到光标（或替换选区） | ☑ | `PatternTokenInsert.kt` + `RenameTemplateEditScreen.kt`；`PatternTokenInsertTest` 6 例绿灯（光标插入 / 选区替换 / 逆序选区 / 越界） |
| 仍可手改 pattern 并保存生效 | ☑ | pattern 仍走 `OutlinedTextField` + `PatternChanged` → 既有 `Save`；未改保存路径 |
| 说明文案三语齐全；票内开发记录已写 | ☑ | `values` / `values-en` / `values-ja`：`rename_template_insert_rules` + 既有 `rename_template_pattern_hint`；本票开发记录已填 |

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-12 | 文档 + `insertAtSelection` 先测后写 + 编辑画面按钮条 + 三语；验收自评 ✅ | 4140d29 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：编辑画面 pattern 可用 `{name}` `{date}` `{tag}` 芯片插入光标/选区，仍可手改；说明三语保留
- **代价与遗留**：占位符清单仍三枚；扩充与 Applier 对齐归 F45（改 `RenamePatternInsertTokens`）
- **错题本登记**：不适用
