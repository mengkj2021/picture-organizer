# F49 票：V · 删标签时清理默认标签集合

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F49 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | V · 删除库标签时从默认标签 DataStore 去掉该名（F8 遗留） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-16 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | 母题遗留 [F8](F8-删除标签联动.md)；同主题 [F48](F48-标签改名联动.md)；[默认标签画面](../../../project-docs/画面/默认标签画面/)；[标签管理画面](../../../project-docs/画面/标签管理画面/) |
| 档位 | 绿 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/标签管理画面/`（删除联动范围补默认标签）
  - `project-docs/画面/默认标签画面/README.md`
- **实际改动**：
  - 无需改文档（删除侧 DataStore 联动为 F8 边界内补全）

## 用户诉求 / 场景

- 要解决的问题或场景：F8 删除有引用标签时会清图片，但 **默认标签** DataStore 仍可能残留已删名 → 之后导入继续把幽灵名打上。
- 预期效果：词表删除成功路径上，同步从 `defaultTagNames` 去掉该名；默认标签画面再进不再出现无效勾选。
- 是否已有式样依据：F8 完结复盘写明「模板 / 默认标签快照不联动」为边界，本票补默认标签一侧。

## 需求确认（接票前若为「待定」先在此澄清）

- 模板 Chip 内同名文本：仍按 F8 **不做**（与 F48 一致）；需要另票。
- 与 F8 二层确认文案：可在有引用确认中附带「并将从默认标签移除」，或静默清默认（接票锁定）。

## 已锁定（接票时确认）

- 静默清默认（不额外改确认文案）；模板内 tagNames 不联动。

## 实施步骤（接票后填）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | `UserPreferencesRepository.removeDefaultTagName` + 删标签两路径调用 | ✅ | 对齐 F48 rename |

## 方案（接票后填）

- 文档先行：无（行为补全 F8 边界）
- 代码改动点：
  - `UserPreferencesRepository.removeDefaultTagName`
  - `TagManageViewModel.confirmDeleteTag` / `confirmCascadeDeleteTag`
- 验证方式：删库标签后 DataStore 不含该名；新导入不打幽灵标签

## 验收标准

- [x] 删除库标签后，默认标签集合不再含该名；再进默认标签画面无该勾选
- [x] 新导入不会因残留默认名打上已删标签
- [x] 文档与代码对齐；票内开发记录（带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-16 | `removeDefaultTagName`；无引用直删与 cascade 删均清 DataStore | （本次提交） |

### 验收对照自评（F49）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 删除库标签后，默认标签集合不再含该名；再进默认标签画面无该勾选 | ☑ | `UserPreferencesRepository.removeDefaultTagName`；`TagManageViewModel` 两删路径调用 |
| 新导入不会因残留默认名打上已删标签 | ☑ | 删后 `defaultTagNames` 同步减名 |
| 文档与代码对齐；票内开发记录（带本票号）已写 | ☑ | 本票开发记录 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果（达成了什么 / 与预期差距）**：F8 删除路径补全默认标签 DataStore 联动；与 F48 改名对称。
- **代价与遗留（新增复杂度、未覆盖场景 → 转新票或备注）**：模板 tagNames 仍不联动（与 F8/F48 一致）。
- **错题本登记**：已登记 [ai-workbench/错题本.md](../../../ai-workbench/错题本.md) ／ 不适用
