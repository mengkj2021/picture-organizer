# F38 票：V · 设置分页每页数量支持自定义

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F38 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | V · 分页每页数量增加「自定义」并可输入 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-07 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [F15 分页可配置](F15-分页可配置.md)（复盘遗留：未做任意数）；[设置画面](../../../project-docs/画面/设置画面/) |
| 档位 | 绿 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/设置画面/README.md`
  - 主画面分页消费处（一般只读偏好，可能无需改 UI）
- **实际改动**：
  - `project-docs/画面/设置画面/README.md`（式样同步）
  - 主画面：无需改 UI（仍读 `list_page_size`）

## 用户诉求 / 场景

- 现状：每页仅 20 / 30 / 50 / 100
- 预期：增加「自定义」，点击后自行输入数量

## 需求确认

- ✅ 合法范围：1～500（含）
- ✅ 非法输入：不写入；对话框内短提示；保留上次有效值
- ✅ 交互：保留预设单选；末尾「自定义」+ 数字输入；非预设值时高亮「自定义」

## 已锁定 / 实施步骤 / 档位

- **档位**：绿
- **步骤**：
  1. 先测后写：`ListPaging` 合法范围 1～500 + 单测
  2. 设置对话框：预设 + 自定义输入（三语文案）+ 设置画面文档同步

## 验收标准

- [x] 可选自定义并持久化；主画面分页按该数切片
- [x] 与 F15 开关共存；票内开发记录已写

### 验收对照自评（F38）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 可选自定义并持久化；主画面分页按该数切片 | ☑ | `ListPaging.kt`：1～500 归一化；`SettingsScreen` 自定义输入写入 DataStore；`ListPagingTest`：`normalize`/`slice` 自定义与越界用例通过（`testDebugUnitTest` · ListPagingTest 绿灯） |
| 与 F15 开关共存；票内开发记录已写 | ☑ | 分页 Switch 未改；关分页时每页数量仍置灰；本票开发记录已写 |

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-08 | 锁定范围/非法处理；`ListPaging` 归一化扩至 1～500；设置对话框加自定义；三语文案；设置画面文档已对齐；单测绿灯 | bd6eea0 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：设置「每页数量」支持预设 + 自定义（1～500），非法输入不落盘；主画面仍经 `ListPaging.slice` 消费偏好。
- **代价与遗留**：真机点验设置对话框与翻页切片建议用户确认（公开后全面测试再系统回归）。
- **错题本登记**：不适用
