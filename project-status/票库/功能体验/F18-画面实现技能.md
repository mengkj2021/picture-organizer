# F18 票：技能 implement-screen（画面实现 SOP）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F18 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 工程质量（AI 技能落地） |
| 标题 | 落地技能 `implement-screen`：读画面文档 → 实现 Kotlin + 路由注册 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 关联 | `shared/skills/`；F17（create-screen-doc，姊妹技能，各自独立）；dev-convention 文档先行 |

## 用户诉求 / 场景

- 要解决的问题或场景：画面实现（读画面文档 → 按包结构写 Kotlin → 路由注册 → 对照文档收尾）每次依赖 prompt 重述，易漏：UI 壳层 inset 消费（Bug1/Bug2 教训）、路由表与代码同步、数据依赖对齐画面文档。业界把"实现"也编码为技能，与 `create-screen-doc` 配对，保证文档产物与代码产物用同一套规范。
- 预期效果：调用 `implement-screen` 技能 → 以画面 README 为唯一输入，按固定 SOP 实现 + 注册路由 + 收尾对照，UI 壳层约束自动带上。
- 是否已有式样依据：否（工程资产）。

## 需求确认（接票前若为「待定」先在此澄清）

- 范围：只做 `implement-screen`（F17 是 create-screen-doc，两者拆票独立实施）。
- 落地形态：按 `shared/skills/README.md` SKILL.md 规范；同步工具目录；README 表补登记。
- 技能内容须覆盖历史 UI 教训：M3 Scaffold/inset 消费、IME 顶栏（Bug2）、底栏 inset（Bug3）、系统返回键（Bug4）、F4/F5 可见性联动——SOP 里固化为"壳层与交互对照画面文档逐项核对"。
- 输入约定：本技能以画面文档为准，文档与代码冲突时先回查（docs/README 对齐约定），不擅自改文档。

## 已锁定 / 实施步骤 / 档位（F29）

| 项 | 内容 |
|---|---|
| 档位 | **黄**（多路径技能同步 + 对照演练；步骤 3–4） |
| 分支 | `main`（`direct-main`，未开 `ticket/`） |
| 先测后写 | **否**（工程资产 / SOP，无 JVM 纯逻辑） |

### 实施步骤

| # | 步骤 | 状态 |
|---|---|---|
| 1 | 写 `shared/skills/implement-screen/SKILL.md`（输入约定 + SOP 五步 + 壳层清单） | ☑ |
| 2 | 同步 `.codebuddy` / `.cursor`；更新 README / 落位表 | ☑ |
| 3 | F21 对照演练（不改产品码）；清单留 `assets/` | ☑ |
| 4 | 收尾：验收自评、开发日志、台账 | ☑ |

## 方案（接票后填）

- 文档先行：无业务新画面；技能正文 + 演练资产
- 代码改动点：无 Android 业务代码；新建技能并双工具同步
- 验证方式：以 F21 开源许可画面走 SOP 对照演练，核对壳层清单可强制执行

## 验收标准

- [x] `implement-screen` SKILL.md 就位（输入约定 + SOP + 壳层/交互核对清单）
- [x] `shared/skills/README.md` 登记；工具目录同步可用
- [x] 至少一次真实画面票实施走技能流程（日志记录，UI 约束无回归）
- [x] 开发日志（文件名带本票号）已写

## 关联画面·预估

| 路径 | 说明 |
|---|---|
| （无业务新画面） | 技能工程资产；演练对照开源许可画面文档 |

## 关联画面·实际改动

- 无需改文档（演练只读 `docs/画面/开源许可画面/README.md`）
- 样例：公开前已删演练 assets（原 implement-screen/assets）

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-05 | 落地 implement-screen；双工具同步；F21 对照演练 | `743087b` |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：与 F17 配对的实现技能就位；SOP 五步 + A–F 壳层清单固化 Bug1–3 / F10 / F4–F5 核对点；F21 对照演练证明清单可走通。
- **代价与遗留**：后续真实「 greefield 写码」票接票时应点名本技能；流程文档变更须同步技能（注意事项已写）。
- **错题本登记**：不适用（未新登记；复用既有 UI 错题条目）
