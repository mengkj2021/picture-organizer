# F46 票：V · 已打包文件删除与重命名

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F46 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 新功能 |
| 标题 | V · 已打包 zip：删除与事后重命名 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-11 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | 母题 / 前置 [F43](F43-已打包文件管理.md)（列表 + 再分享）；同主题 [F41](F41-打包预览排除.md) · [F42](F42-打包zip文件名.md)；画面 [已打包文件管理画面](../../../project-docs/画面/已打包文件管理画面/) |
| 档位 | 黄（既有管理画面上的变更操作；含先测后写） |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/已打包文件管理画面/`
  - 必要时交叉引用 `project-docs/画面/图片打包画面/README.md`
- **实际改动**：
  - `project-docs/画面/已打包文件管理画面/README.md`
  - `project-docs/画面/已打包文件管理画面/删改名.md`（新建）
  - `project-docs/工具类.md`、`数据流.md`、`画面/README.md`、`核心流程.md`

## 用户诉求 / 场景

- 要解决的问题或场景：已能列出并再分享历史 zip，但缺少删除与事后改名
- 预期效果：在管理画面上可删除（确认后）、重命名（合法 stem + 同目录冲突规则）
- 是否已有式样依据：否（依赖 F43 画面文档；规则对齐 F42 stem / 冲突约定）

## 需求确认（接票前若为「待定」先在此澄清）

- 无（范围已由 F43 拆票锁定）
- 事后改名冲突：**方案 A** — 目标已存在则 Snackbar 拒绝（不自动 `_2`）

## 已锁定（接票时确认；绿档可写「见诉求」或省略本节）

- 仅管理 `filesDir/exports/` 内 zip；不含 Downloads 外拷
- F42 = 打包前改名；本票 = 历史文件事后改名 / 删除
- 须先有 F43 管理画面（列表 + 再分享）再接本票
- 重命名合法 stem：对齐 F42 `ExportZipNames`；**冲突拒绝**（异于打包落盘 `_2`）；**先测后写**
- 档位：黄；`direct-main`

## 实施步骤（接票后填；绿档可写「一步完成」）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | 文档：删改名区域 + README / 工具类 / 数据流 | ☑ | |
| 2 | 先测：`planZipRename` 校验 / 冲突 / 未变 | ☑ | ExportZipNamesTest |
| 3 | VM / Screen 删确认 + 改名对话框 + 三语 | ☑ | `ui/exportmanage/` |
| 4 | 跑相关单测 + 收尾 | ☑ | test / ktlint 绿 |

## 方案（接票后填）

- 文档先行：`画面/已打包文件管理画面/删改名.md` + README；工具类 / 数据流 / 核心流程
- 代码改动点：`ExportZipNames.planZipRename` + Test；`ui/exportmanage/`；三语 strings
- 验证方式：单测绿灯；删确认后刷新；非法名 / 冲突 Snackbar

## 验收标准

- [x] 管理画面可删除 `exports/` 内 zip（有确认，删后列表刷新）
- [x] 可事后重命名；非法名 / 冲突行为有明确规则且与文档一致
- [x] 纯逻辑单测绿灯（先测后写）；票内开发记录（带本票号）已写

### 验收对照自评（F46）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 管理画面可删除 `exports/` 内 zip（有确认，删后列表刷新） | ☑ | `ExportManageScreen` 删除确认对话框；`ConfirmDelete` → `deleteExportZip`；成功后 `refresh()`；路径须在 `exportsDir` 下 |
| 可事后重命名；非法名 / 冲突行为有明确规则且与文档一致 | ☑ | 改名对话框 + `planZipRename`；空/非法复用 F42 文案；冲突 `export_manage_name_collision` 拒绝不自动 `_2`；见 `删改名.md` |
| 纯逻辑单测绿灯（先测后写）；票内开发记录（带本票号）已写 | ☑ | `ExportZipNamesTest.planZipRename_*`；`testDebugUnitTest` + `ktlintCheck` 绿；本票开发记录 |

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-11 | 自 F43 拆出：删 / 改名；列表 + 再分享留 F43 |  |
| 2026-09-11 | 接票锁定方案 A（冲突拒绝）；文档先行；`planZipRename` + 管理画面删/改名 UI；单测/ktlint 绿 | 78f1922 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：`export-manage` 可确认删除与事后改 stem；非法名拦截；同名冲突拒绝（异于 F42 打包 `_2`）
- **代价与遗留**：真机点验列表三按钮 / 对话框 / IME 未在本环境做；窄屏 trailing 三 TextButton 可能偏挤（可后续再调布局）
- **错题本登记**：不适用
