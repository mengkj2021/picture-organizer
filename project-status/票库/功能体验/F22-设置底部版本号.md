# F22 票：设置画面底部显示版本号（versionName）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F22 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | 设置画面底部加 versionName（版本号）显示 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 关联 | 画面文档（设置画面）；[改动清单](../../阶段/阶段3-全面测试/待对应.md) F20 版本矩阵（发布 / 公开配套） |
| 关联画面·预估 | `docs/画面/设置画面/README.md` |
| 关联画面·实际改动 | `docs/画面/设置画面/README.md`（功能清单 / UI / 职责） |

## 用户诉求 / 场景

- 要解决的问题或场景：`android/app/build.gradle.kts` 已定义 `versionCode = 1`、`versionName = "0.0.1"`，但 UI 无从查看当前版本，用户 / 测试 / 发版对照不便。
- 预期效果：设置画面底部显示 app 版本号（如「版本 0.0.1」），与 build 配置一致。
- 是否已有式样依据：否（体验增强，位置与样式需确认）

## 需求确认（接票前若为「待定」先在此澄清）

- **读取方式**：已锁定 **C** — `PackageManager.getPackageInfo(…).versionName`（不改 gradle / 不开 buildConfig）
- **显示样式 / 位置**：已锁定 — 列表底部 caption 灰字居中「版本 {versionName}」；无点击复制、不露 versionCode
- **与 F20 关系**：本票只读展示，不硬编码；F20 版本矩阵仍独立

## 方案（接票后填）

- 文档先行：设置画面 README 功能清单 / UI 描述补「底部版本号」。
- 代码改动点：`strings.xml`（`settings_version`）+ `SettingsScreen` LazyColumn 末项
- 验证方式：`compileDebugKotlin` / `ktlintCheck`；真机看设置底部与 `versionName = "0.0.1"` 一致
- **档位**：绿；**步骤**：文档 → 文案+UI → 收尾（已锁定）

## 验收标准

- [x] 设置画面底部显示版本号，与 `build.gradle.kts` 的 versionName 一致
- [x] 文档与代码对齐（设置画面 README）
- [x] 开发日志（文件名带本票号 F22）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-05 | PackageManager 读 versionName；设置底 caption；文档/台账/日志 | 4b899e2 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：设置底部只读显示「版本 0.0.1」，与 build 配置同源（APK PackageInfo）
- **代价与遗留**：未展示 versionCode、无复制；F20 统一版本矩阵仍待办
- **错题本登记**：不适用
