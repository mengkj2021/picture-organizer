# F20 票：统一版本矩阵（文档号 ↔ App 版本 ↔ versionCode）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F20 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 工程质量（版本口径） |
| 标题 | 建立统一版本矩阵：式样书文档号 / App versionName / versionCode 单一出处 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 完成日期 | 2026-09-05 |
| 关联 | 式样书 3.1（版本矩阵）；`android/app/build.gradle.kts`（versionName/versionCode）；阶段一复盘 3.5/4.2；项目计划 5（发版语义拍板） |
| 关联画面·预估 | 无画面改动（仅式样 / 计划文档） |
| 关联画面·实际改动 | 无需改文档（无画面） |

## 用户诉求 / 场景

- 要解决的问题或场景：阶段一复盘实证——文档版本（v0.9）、App 版本（0.0.1 / 曾 0.1.0）、versionCode（1）三套号互不参照、无统一出处；发版语义待定（项目计划 5）。公开日临近，三套口径若不自洽会露怯，且后续每次发版缺"谁增谁不变"的权威依据。
- 预期效果：式样书建立版本矩阵表（文档号 ↔ 产品节点 ↔ versionName ↔ versionCode 语义），发版时按语义递增 versionCode / 演进 versionName；文档修订与代码改版各自有出处可查。
- 是否已有式样依据：阶段一复盘 3.5（已列候选调查/变更）。

## 需求确认（接票前若为「待定」先在此澄清）

- 范围：建矩阵与语义规则（哪档动哪、何时 +1）入式样书 3.1；与项目计划 5「版本 0.1.0 首个公开 tag」对齐拍板；落地时把 `versionName` 调整到拍板值。
- 不改动：不引入自动化版本脚本（语义规则文档化即可，够用）。
- 引用基础：App 版本号权威处 = `android/app/build.gradle.kts`；文档号权威处 = 式样书自身标题行——矩阵表给出对照与演进规则，代码/文档各自改各自出处，矩阵只做登记不反客为主。
- **接票拍板（2026-09-05）**：方案 **A**——本票只建矩阵 + 语义；代码保持 `0.0.1` / `versionCode = 1`；矩阵写明公开日再升 `0.1.0` / 预期 Code `2`。

## 已锁定 / 实施步骤 / 档位（F29）

| 项 | 内容 |
|---|---|
| 档位 | 绿 |
| 已锁定 | 方案 A；不 bump Gradle；无自动化脚本 |
| 实施步骤 | ① 式样书 3.1 产品版本矩阵 + 演进语义 ② 项目计划对齐 ③ 票 / 台账 / 日志 |

## 方案（接票后填）

- 文档先行：式样书 3.1 增「产品版本」对照表 + 演进规则表；项目计划 1/线 2/5 对齐 F20 口径
- 代码改动点：**无**（保持 `0.0.1` / `1`）
- 验证方式：模拟演进 `0.0.1`/`1` → 公开 `0.1.0`/`2` → 热修 `0.1.1`/`3` 写入式样书自检行

## 验收标准

- [x] 式样书 3.1 有统一版本矩阵 + 演进语义（含 versionCode 递增规则）
- [x] 与项目计划 5 发版拍板（0.1.0 首个 tag）口径一致
- [x] 落地时 versionName/versionCode 与实际一致（若调整，日志记录）
- [x] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-05 | 式样书 3.1 产品版本矩阵；项目计划对齐；方案 A 不改 Gradle | （待用户提交） |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：三套口径有对照表与演进规则；公开前不假封板，与计划「可选 tag v0.1.0」一致。
- **代价与遗留**：公开日须手动 bump Gradle 并回写表「当前值」；无自动化。**F51（2026-09-16）** 公开首版实际定为 **`1.0.0` / code `2`**，覆盖本票原文「公开日 0.1.0」；权威当前值见 [技术栈.md](../../../project-docs/技术栈.md)。
- **错题本登记**：不适用（文档口径类，无新教训）
