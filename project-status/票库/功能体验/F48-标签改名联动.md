# F48 票：V · 标签改名联动图片与默认标签

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F48 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | V · 标签库改名时同步图片标签与默认标签（有引用先确认） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-16 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [标签管理画面](../../../project-docs/画面/标签管理画面/)；[默认标签画面](../../../project-docs/画面/默认标签画面/)；近源 [F8](F8-删除标签联动.md)（删标签清图片）；同主题拆票 [F49](F49-删标签清默认标签.md)；点验 [Bug13](../bug/Bug13-默认标签不回显.md) |
| 档位 | 黄 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/标签管理画面/`（改名规则 / 确认文案）
  - `project-docs/画面/默认标签画面/README.md`（库改名后本屏名同步）
  - 必要时详情 / 筛选交叉说明
- **实际改动**：
  - `project-docs/画面/标签管理画面/README.md` §8
  - `project-docs/画面/标签管理画面/标签列表.md`

## 用户诉求 / 场景

- 要解决的问题或场景：标签管理里改名后，图片上的标签名与默认标签集合仍停在旧名；用户希望「标签跟着改」。
- 预期效果：
  1. 改名时若有图片仍挂旧名 → **先通知**（类似 F8 删标签有引用确认），确认后再改；
  2. 确认后：词表改名 + 引用图片 `tagsJson` / Exif 同步为新名 + DataStore 默认标签集合中旧名替换为新名；
  3. 取消则词表与引用均不动。
- 是否已有式样依据：否（现状 `updateTag` 只改词表）；需求以本票为准。近重复：F8 仅覆盖**删除→清图片**，明确不含默认标签 / 模板，且不含改名。

## 需求确认（接票前若为「待定」先在此澄清）

- 模板（`tag_templates` 内名字快照）是否一并改名：建议本票**先不做**（与 F8 边界一致）；需要则另票。
- 无图片引用、仅默认集合含旧名：是否仍弹确认 → 建议轻量提示或直接替换默认集合（接票锁定）。
- Exif 失败策略：对齐 F8（DB 主、Exif 失败汇总提示、不回滚词表改名——接票确认）。

## 已锁定（接票时确认）

- 模板内 tagNames 快照不联动（与 F8 一致）。
- 无图片引用时：直接改词表 + 默认集合替换，不弹确认。
- Exif 失败：DB 仍成功，Snackbar 汇总失败张数，不回滚词表改名。

## 实施步骤（接票后填）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | `ImageListItem.renameTagName` + 单测 | ✅ | 先测后写 |
| 2 | `ImageRepository.renameTagInAllImages` + `UserPreferencesRepository.renameDefaultTagName` | ✅ | |
| 3 | `TagManageViewModel` 改名确认流 + 三语文案 | ✅ | 对齐 F8 二层确认 |
| 4 | 画面文档对齐 | ✅ | 标签列表 / README §8 |

## 方案（接票后填）

- 文档先行（画面文档 / 式样书 / 架构 / 路由，按需）：标签列表 + README §8
- 代码改动点：
  - `ImageListItem.renameTagName` / `ImageListItemTest`
  - `ImageRepository.renameTagInAllImages`（Room + Mock）
  - `UserPreferencesRepository.renameDefaultTagName`
  - `TagManageViewModel` / `TagManageUi` / `TagManageScreen`
- 验证方式：`ImageListItemTest`；真机改名有/无引用 / 取消 cascade

## 验收标准

- [x] 改名有图片引用：确认文案含影响张数；确认后图片与默认集合均为新名；取消全部不变
- [x] 改名无图片引用：默认集合若含旧名则替换为新名；词表为新名
- [x] 文档与代码对齐；票内开发记录（带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-16 | 先测后写 renameTagName；二层确认 + 批量改名 + 默认标签联动；文档对齐 | e414f91 |
| 2026-09-16 | 点验补丁：有引用改名确认框误复用删除 Dialog（标题/按钮均为「删除」）→ `ConfirmActionDialog` + `action_rename` | 4cbcf3a |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：标签库改名后图片标签与默认标签集合同步更新；有引用时用户知情后一键联动（对齐 F8 体验）。
- **代价与遗留**：模板快照不联动（票内边界）；删标签侧默认集合清理由 F49 补；Exif 失败不回滚 DB。
- **错题本登记**：不适用

### 验收对照自评（F48）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 改名有图片引用：确认文案含影响张数；确认后图片与默认集合均为新名；取消全部不变 | ☑ | `TagManageViewModel.saveTag` / `confirmCascadeRenameTag` + `tag_manage_confirm_rename_tag_in_use`；`performTagRename` 联动图片 + 默认标签；取消清 `cascadeRenameTag` |
| 改名无图片引用：默认集合若含旧名则替换为新名；词表为新名 | ☑ | `saveTag` 无引用走 `performTagRename`；`UserPreferencesRepository.renameDefaultTagName` |
| 文档与代码对齐；票内开发记录（带本票号）已写 | ☑ | 标签列表 / README §8；本票开发记录 |
