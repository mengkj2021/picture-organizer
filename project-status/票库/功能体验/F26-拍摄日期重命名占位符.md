# F26 票：重命名 `{date}` 优先拍摄日期

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F26 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | `RenamePatternApplier`：`{date}` 拍摄日期优先，无则回退导入日期 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-05 |
| 关联 | **母题 [F13](F13-按拍摄日期.md)**；**依赖 [F24](F24-拍摄日期数据底座.md)**；[重命名模板管理画面](../../../project-docs/画面/重命名模板管理画面/README.md)；图片详细画面·重命名；F16/`RenamePatternApplier` 单测 |
| 档位 | **绿**（纯逻辑 + 文案/文档；无迁移/无新画面；步骤 ≤2） |

## 关联画面（路径级）

- **预估**：
  - `docs/画面/重命名模板管理画面/README.md`（占位符说明：拍摄日优先）
  - `docs/画面/图片详细画面/重命名.md`（语义同步）
- **实际改动**：
  - `docs/画面/重命名模板管理画面/README.md`
  - `docs/画面/图片详细画面/重命名.md`
  - `docs/画面/图片详细画面/README.md`
  - 文案：`rename_template_pattern_hint`（非 docs，与画面对齐）

## 用户诉求 / 场景

- 要解决的问题：模板 `{date}` 目前按导入日期展开，与拍摄时间不一致
- 预期效果：有 `dateTakenMillis` 则用拍摄日；否则回退现有导入日期；不产生空文件名段
- 是否已有式样依据：否（F13；占位符说明需同步）

## 需求确认（已锁定，源自 F13）

- 展开规则：拍摄日期优先 → 无则导入日期（既有行为）
- 格式：沿用现有 `{date}` 日期格式，本票不另开格式选项
- **依赖**：须 F24 ✅
- **F20**：含可测纯逻辑 → **先写/补单测再改实现**，收尾跑相关 `testDebugUnitTest`
- **不纳入**：筛选（F25）；导入解析（F24）

## 已锁定（接票时确认）

- 展开：有 `dateTakenMillis` → `{date}` = 拍摄日 `yyyyMMdd`；否则回退 `item.date`（导入日，既有）；再空则今日
- 格式：沿用 `yyyyMMdd`，不另开选项
- 时区：与导入日一致（`ZoneId.systemDefault()`）
- F20：先补 `applyForItem` 单测再改实现
- 不改：筛选（F25）、导入解析（F24）、`apply(...)` 底层签名（仍吃已算好的 `dateYmd`）

## 实施步骤（接票后填）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | 单测：`applyForItem` 有/无拍摄日；再改 `RenamePatternApplier` + 文案/画面文档/式样一句 | ✅ | 绿档 |

## 方案（接票后填）

`applyForItem` 优先 `dateTakenMillis` → `yyyyMMdd`，否则既有导入日回退；`ImageDetailViewModel` 已传完整 `ImageListItem`，调用方未改。

## 验收标准

- [x] 有拍摄日期时 `{date}` 展开为拍摄日；无则回退导入日；不出现空档文件名
- [x] 相关单测覆盖上述规则且通过（F20）
- [x] 重命名模板管理 / 详情重命名文档与代码对齐
- [x] 开发日志（文件名带 F17）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-05 | 自 F13 拆出；依赖 F24；约定先测后写 |  |
| 2026-09-05 | 接票实施：先测后写落地；验收自评全部 ☑ | 待用户 commit |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：`{date}` 与拍摄时间对齐；无拍摄日行为与改前一致；F20 在本票回证
- **代价与遗留**：无；母题 F13 可关
- **错题本登记**：不适用
