# F27 票：三语资源 + 系统跟随（简中 / 日 / 英）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F27 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 新功能 |
| 标题 | `values` / `values-ja` / `values-en` 对齐；跟随系统语言；缺译回退简中；式样多语言章节 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-05 |
| 关联 | **母题 [F23](F23-文言国际化.md)**；后续 [F28](F28-状态显示名本地化.md)、[F29](F29-教程长文三语.md)；[values/strings.xml](../../../android/app/src/main/res/values/strings.xml)；设置 / 各画面壳层 |
| 档位 | 黄（多资源文件 + 硬编码收口；`direct-main` 分支可选） |

## 关联画面（路径级）

- **预估**：
  - 凡使用 `stringResource` / `R.string` 的画面文档注明文言来源（可批量脚注，不必逐画面大改）
  - `docs/式样/项目式样说明书.md`（新增多语言章节）
  - 教程：**仅壳层**标题/按钮等；正文长文 → F29
- **实际改动**（收尾填）：
  - `docs/画面/README.md`（统一文言脚注）
  - `docs/画面/教程画面/README.md`（壳层 vs F29）
  - `docs/式样/项目式样说明书.md` 3.4
  - 代码层：`values*` + ViewModel 文案收口（画面文档无需逐屏改）

## 用户诉求 / 场景

- 要解决的问题：UI 仅简中；需日/英资源并随系统语言切换
- 预期效果：系统语言为中/日/英时主要画面文言正确；键集合对齐；缺键回退 `values`（简中）无崩溃
- 是否已有式样依据：否（本票补式样）

## 需求确认（已锁定，源自 F23）

- **语言来源 A**：仅系统跟随，无应用内开关
- **资源**：`values`（简中）+ `values-ja` + `values-en`；键名对齐
- **回退**：缺译 → 简中 `values`
- **纳入**：现有 `strings.xml` 全量 UI；Kotlin 中误硬编码的用户可见串收口到资源；教程等**壳层**文案
- **不纳入**：用户数据；`STATUS_TAGS`（→ F19）；教程长文（→ F29）；应用内语言开关（明确不做）
- **翻译**：首版 AI 起草日/英，用户抽检；专业校对非阻断

## 已锁定（接票时确认）

- 见需求确认；新增 string 纪律：本票后 ja/en 须同步补齐（可写进式样章节）

## 实施步骤（接票后填）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | 式样多语言章节 + 画面索引文言脚注 | ✅ | |
| 2 | `values-ja` / `values-en` 键对齐；简中补通用错误键 | ✅ | 180 键 |
| 3 | ViewModel / ExportZip / ImageManager 硬编码收口 | ✅ | 不改 STATUS_TAGS |
| 4 | 验收自评 + 票/台账/日志 | ✅ | |

## 方案（接票后填）

- 文档先行：式样新增多语言节；`docs/画面/README.md` 统一约定文言来源；教程 README 注明壳层 vs F29
- 代码改动点：三语 `strings.xml`；各 `*ViewModel` 失败回退改 `ShowMessage(R.string…)`；`ExportZip` 未打标签标签走 `filter_untagged`；`unnamed_image`
- 验证方式：键集合对齐检查；ktlint；系统语言切换抽检（中/日/英）

## 验收标准

- [x] 系统语言为中 / 日 / 英时，主要画面 UI 文言正确切换
- [x] `values` / `values-ja` / `values-en` 键集合对齐；缺键回退简中且无崩溃
- [x] 用户数据（标签名、文件名等）不被错误翻译或破坏
- [x] 式样书补多语言章节；相关画面文档注明文言来源（或统一约定）；开发日志（文件名带 F18）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-05 | 自 F23 拆出；待定已由母题锁定 |  |
| 2026-09-05 | 落地三语资源 + 硬编码收口 + 式样 3.4；验收自评全部 ☑ | （待提交） |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：系统跟随简中/日/英 UI；键对齐 180；缺译回退简中；壳层与错误回退可本地化
- **代价与遗留**：Repository / AppFileManager 深层 `error()` 中文仍可能在少数路径经 `e.message` 冒出（ExportZip / 批量重命名明细）；STATUS→F28；教程长文→F29；日/英首版 AI 译待用户抽检
- **错题本登记**：不适用（常规 i18n 落地）
