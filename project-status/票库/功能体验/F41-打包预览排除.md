# F41 票：V · 打包预览可单张排除

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F41 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | V · 打包画面筛选预览小图可叉掉排除 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-08 |
| 所属节点 | V |
| 涉及节点 | V |
| 关联 | [图片打包画面](../../../project-docs/画面/图片打包画面/)；同主题拆票 [F42](F42-打包zip文件名.md) · [F43](F43-已打包文件管理.md) |
| 档位 | 黄 |

## 关联画面（路径级）

- **预估**：
  - `project-docs/画面/图片打包画面/README.md`（预览区；若尚无区域文件则接票补）
- **实际改动**：
  - `project-docs/画面/图片打包画面/README.md`
  - `project-docs/画面/图片打包画面/预览区.md`（新建）
  - `project-docs/核心流程.md`、`数据流.md`、`架构设计.md` §2、`工具类.md`

## 用户诉求 / 场景

- 要解决的问题或场景：打包前按筛选得到的预览集合无法去掉个别不想进包的图
- 预期效果：预览小图上有叉，可针对某张去掉；去掉后不进入本次打包
- 是否已有式样依据：否（本票补画面预览区）

## 需求确认（接票前若为「待定」先在此澄清）

- 排除是会话临时态（离开画面清空）还是需持久 → **会话临时态**
- 无预览 UI 时是否先补「待打包预览」再加叉 → **本票一并补网格 + 叉**

## 已锁定（接票时确认；绿档可写「见诉求」或省略本节）

- 排除为 ViewModel 内存 `Set<id>`；pop 出打包画面清空；进筛选再回保留；不写 Room / DataStore
- 本票补待打包预览网格 + 小图叉；叉掉后从预览消失、不进本次任何标签包；无单独撤销
- 改筛选不自动恢复已排除 id；全部排除后「开始打包」不可用
- 先测后写：抽出 `excludingIds` + `buildExportGroups`
- 档位：黄；`direct-main`

## 实施步骤（接票后填；绿档可写「一步完成」）

| # | 步骤 | 状态 | 备注 |
|---|---|---|---|
| 1 | 文档：预览区 + README / 核心流程 / 架构 | ☑ | |
| 2 | 先测：排除 id + 分组不进包 | ☑ | ExportZipPackTest |
| 3 | VM / 预览 UI / 三语文案 | ☑ | |
| 4 | 跑相关单测 + 收尾 | ☑ | testDebugUnitTest / ktlintCheck 绿 |

## 方案（接票后填）

- 文档先行：`画面/图片打包画面/预览区.md` + README；核心流程 / 数据流 / 架构 §2 / 工具类
- 代码改动点：`model/ExportZipPack.kt` + Test；`ExportZipViewModel` / `ExportZipUi` / `ExportZipScreen`；三语 strings
- 验证方式：单测绿灯；开始打包只含预览剩余项

## 验收标准

- [x] 预览小图可叉掉排除；被排除图不进本次 zip
- [x] 未排除项与筛选逻辑回归正常
- [x] 票内开发记录（带本票号）已写

### 验收对照自评（F41）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 预览小图可叉掉排除；被排除图不进本次 zip | ☑ | `ExportZipScreen` 预览网格 + 叉；`ExcludeImage` → `excludingIds`；`startExport` 用排除后列表再 `buildExportGroups`；`ExportZipPackTest.excludeThenGroup_removedFromAllTagPacks` / `excludeAll_yieldsEmptyGroups` 通过 |
| 未排除项与筛选逻辑回归正常 | ☑ | 分组规则原样抽出；筛选仍 `matchesFilter`；`ImageTagFilterTest` + `ExportZipPackTest` 选中标签 / 未打标签 / 多标签分包用例通过 |
| 票内开发记录（带本票号）已写 | ☑ | 本票 |

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-11 | 预览网格 + 会话排除；抽出 `ExportZipPack`；三语文案；`testDebugUnitTest` / `ktlintCheck` 绿 | 0c8b2cc |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：打包画面有待打包预览；叉掉的图不进本次任何标签包；离开画面后排除集随 ViewModel 销毁。
- **代价与遗留**：排除无单张撤销（改筛选也不恢复 id）；真机点验预览触控 / 大量缩略图滚动未在本环境做。zip 文件名、已打包管理仍属 F42 / F43。
- **错题本登记**：不适用
