# F14 票：一览菜单「批量重命名」（选中多张套模板）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F14 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 新功能 |
| 标题 | 主画面三一览编辑态批量重命名：选中多张 → 套重命名模板 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 关联 | [主画面](../../../project-docs/画面/主画面/README.md) 三一览「菜单」（编辑态 / 移动到 行内操作）；图片详细画面·重命名（`RenamePatternApplier`、后缀锁定）；[重命名模板管理画面](../../../project-docs/画面/重命名模板管理画面/README.md)；[导入画面](../../../project-docs/画面/导入画面/README.md) P1 失败明细弹窗（参考交互） |
| 关联画面·预估 | `docs/画面/主画面/` 三 Tab「菜单.md」；`docs/画面/图片详细画面/重命名.md`（语义对齐，一般不改）；式样书 4.3 |
| 关联画面·实际改动 | `docs/画面/主画面/待处理一览/菜单.md`；`已确认一览/菜单.md`；`回收站一览/菜单.md`；`主画面/README.md`；式样 4.3（详情重命名文档未改，语义已对齐） |
| 档位 | 黄（多文件 + 新交互 + 失败明细；步骤 4；`direct-main` 下分支可选） |
| 已锁定 | 是（2026-09-05） |

## 用户诉求 / 场景

- 要解决的问题或场景：目前重命名只能进详情逐张操作（手输或套模板）；批量改名（如按模板统一日期 / 前缀整理一批）无入口
- 预期效果：主画面一览「编辑」勾选多张后，可一次套用重命名模板批量改名，不用逐张进详情
- 是否已有式样依据：部分（详情单张套模板为 C9；批量是新增）

## 需求确认（接票前若为「待定」先在此澄清）

已锁定（接票确认）：

- **入口**：三一览编辑态 + 选中 ≥ 1 时，工具栏「移动到」旁行内「重命名」（显式可见，不进「更多」）
- **交互**：点「重命名」→ DropdownMenu 选模板（`rename_templates`；无模板时按钮禁用）→ 应用到全部选中项
- **展开规则**：逐张 `RenamePatternApplier.applyForItem`（与详情 / F26 一致）；扩展名锁定（F2）
- **冲突处理**：不整体回滚；逐张跳过；失败明细弹窗（仿 P1：单张 / 全部重试）
- 范围不含：手输统一新名的批量替换

## 实施步骤

| # | 步骤 | 状态 |
|---|---|---|
| 1 | 文档：三 Tab「菜单.md」+ 式样书 4.3 补批量套模板 | ✅ |
| 2 | F20：先测批量展开/批内冲突纯逻辑（`BatchRenamePlanner`） | ✅ |
| 3 | 代码：Main VM/事件 + TabActionBar + 失败明细 UI；逐张调现有 `rename` | ✅ |
| 4 | 收尾：验收自评 + 日志 + 台账 | ✅ |

## 方案（接票后填）

- **文档**：三 Tab「菜单.md」+ 主画面 README；式样 4.3
- **纯逻辑（F20）**：`util/file/BatchRenamePlanner` + 单测（先测后写）
- **代码**：`MainViewModel` + `TabActionBar` 行内重命名；失败明细仿 P1；逐张 `ImageRepository.rename`
- **验证**：单测绿灯；用户验收后提交

## 验收标准

- [x] 场景达成，行为符合式样（与详情套模板规则一致）
- [x] 文档与代码对齐
- [x] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-05 | 接票：读票定边界；拆票门槛未达；档位黄；方案待确认 |  |
| 2026-09-05 | 实现：BatchRenamePlanner + 主画面批量套模板 + 失败明细；自评完成；用户验收后提交 | 见本提交 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：三一览编辑态可批量套重命名模板，规则与详情一致；失败明细可重试
- **代价与遗留**：批内交叉换名链（A→B、B→C）仍依赖运行时顺序，极端场景可能需多次重试；不含手输统一新名
- **错题本登记**：不适用
