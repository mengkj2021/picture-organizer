# 拉取最新代码

**用法**：把本文件拉进对话 → 填写下方「本次拉取」→ 发送。  
**约定**：先看清本地未提交改动再拉；禁止擅自 `--force` / 硬重置丢本地。

▶ include `ai-workbench/宪法/include约定.md`

## AI 执行流程（不用改）

▶ include `ai-workbench/细则/prompts/拉取最新代码.md`

---

## 本次拉取（每次只改这里）

```
远程与分支：（可空 = origin + 当前分支）
策略：merge（默认） / rebase
本地有未提交改动时：停下来说明 / 先 stash 再拉 / 直接拉（仅当无冲突风险且用户写明）
额外说明：
```
