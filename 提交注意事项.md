# 提交注意事项

本仓库为「图片整理」Android 项目（PictureOrganizer）。任何克隆、提交、推送请遵循以下约定，避免提交无关文件、账号混乱与中文乱码。

## 1. 账号配置

本项目托管于 GitHub：**mengkj2021/picture-organizer**。

推荐使用专用 SSH 别名（与笔记库等其它仓库隔离）。若已为其它仓库生成过专用 key，可复用，无需重复生成。

### 克隆地址

```
git clone git@github-mengkj2021:mengkj2021/picture-organizer.git
```

### 首次配置（仅需一次）

1. 若无专用 key，生成（无 passphrase）：
   ```
   ssh-keygen -t ed25519 -C "mengkj2021-github" -f ~/.ssh/id_ed25519_mengkj2021 -N ""
   ```
2. 将公钥 `~/.ssh/id_ed25519_mengkj2021.pub` 添加到 GitHub 账号 → Settings → SSH and GPG keys
3. 在 `~/.ssh/config` 追加（Windows 路径为 `C:\Users\<用户名>\.ssh\config`）：
   ```
   Host github-mengkj2021
     HostName github.com
     User git
     IdentityFile ~/.ssh/id_ed25519_mengkj2021
     IdentitiesOnly yes
   ```
4. 验证：`ssh -T git@github-mengkj2021` 应返回 `Hi mengkj2021!`

### git 身份（仓库级，勿设全局）

在仓库目录内执行：

```
git config user.name "mengkj2021"
git config user.email "mengkj2021@163.com"
```

## 2. 提交编码（防乱码，重要）

本项目文档、提交信息均为中文。历史教训：Windows 上编码错误会导致中文变成乱码且不可逆丢失。**务必**：

- 提交信息一律使用 **UTF-8（无 BOM）**
- 在 Windows 上额外设置：
  ```
  git config --global core.quotepath false
  git config --global i18n.commitEncoding utf-8
  git config --global i18n.logOutputEncoding utf-8
  ```
- 提交前用 `git log -1` 检查中文显示正常再 push
- 若发现提交信息显示乱码，**不要继续 push**，先修正再提交

## 3. 提交流程

1. `git status` 查看改动
2. `git add <文件>`（不要用 `git add .`，避免误提本地文件）
3. `git commit -m "中文描述"`（说明改了什么、为什么）
4. `git push`（正常推送即可；**禁止** `--force`，除非明确确认要改写历史）

## 4. 本项目提交约定

### 4.1 文档与规则维护

| 内容 | 维护入口 | 说明 |
|------|----------|------|
| 项目说明 | [`shared/README.md`](shared/README.md) | AI 双工具共用，无需同步 |
| 规则 | `shared/rules/<name>.md` | 复制到 `.codebuddy/rules/<name>/RULE.mdc` 与 `.cursor/rules/<name>.mdc` |
| 式样 / 路由 / 画面 | [`docs/`](docs/) | 式样书、路由设计、各画面文档 |
| 仓库入口 | [`README.md`](README.md) | 指向上述文档与首次构建说明 |

规则改动流程详见 [`shared/README.md`](shared/README.md)。

### 4.2 不提交的内容（已在 `.gitignore`）

**构建与 IDE 本地产物**（可随时删除，下次构建会重建）：

| 目录 / 文件 | 说明 |
|-------------|------|
| `build/`、`app/build/` | Gradle / Android 构建输出（含 APK、中间文件） |
| `.gradle/` | Gradle 缓存 |
| `.kotlin/` | Kotlin 编译插件本地缓存 |
| `.idea/`、`*.iml` | Android Studio 配置 |
| `local.properties` | 本机 SDK 路径 |

**其它不提交项**：

- `gradle/wrapper/*.zip`（本地 Gradle 发行包，约 130MB，超过 GitHub 单文件 100MB 限制）
- `.codebuddy/` 只提交 `rules/`，其余（`plans/` 等）已忽略
- `.DS_Store`、临时文件、签名文件（`*.jks` / `*.keystore`）、日志（`*.log`）

### 4.3 Gradle 发行包（克隆后注意）

[`gradle/wrapper/gradle-wrapper.properties`](gradle/wrapper/gradle-wrapper.properties) 中 `distributionUrl=gradle-9.7.1-bin.zip` 为**相对路径**（zip 不提交 Git）。

克隆到其它机器后任选其一：

1. 将 `gradle-9.7.1-bin.zip` 放入 `gradle/wrapper/` 目录（与 `gradle-wrapper.properties` 同级）
2. 临时改用官方 URL：`https://services.gradle.org/distributions/gradle-9.7.1-bin.zip`

详见 [`README.md`](README.md)「首次构建」。

## 5. push 前自检

- [ ] `git status` 无预期外文件（`build/`、`app/build/`、`.gradle/`、`.kotlin/`、`.codebuddy/plans/`、zip 等）
- [ ] `git log -1` 中文显示正常
- [ ] 提交信息说明改了什么、为什么
- [ ] 规则改动已从 `shared/rules/` 同步到 `.codebuddy/` 与 `.cursor/`
