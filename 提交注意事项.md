# 提交注意事项

本仓库为「图片整理」Android 项目（PictureOrganizer）。任何克隆、提交、推送请遵循以下约定，避免提交无关文件、账号混乱与中文乱码。

## 1. 账号配置

> 本项目计划托管于 GitHub，账号信息以实际连接时为准，先用占位符 `<...>` 标记，连接后补充。

推荐使用专用 SSH 别名（与笔记库等其它仓库隔离）。若已为其它仓库生成过专用 key，可复用，无需重复生成。

### 克隆地址

```
git clone git@github-mengkj2021:<GitHub账号>/<仓库名>.git
```

### 首次配置（仅需一次）

1. 若无专用 key，生成（无 passphrase）：
   ```
   ssh-keygen -t ed25519 -C "<账号>-github" -f ~/.ssh/id_ed25519_<账号> -N ""
   ```
2. 将公钥 `~/.ssh/id_ed25519_<账号>.pub` 添加到对应 GitHub 账号 → Settings → SSH and GPG keys
3. 在 `~/.ssh/config` 追加（Windows 路径为 `C:\Users\<用户名>\.ssh\config`）：
   ```
   Host github-mengkj2021
     HostName github.com
     User git
     IdentityFile ~/.ssh/id_ed25519_<账号>
     IdentitiesOnly yes
   ```
4. 验证：`ssh -T git@github-mengkj2021` 应返回 `Hi <账号>!`

### git 身份（仓库级，勿设全局）

在仓库目录内执行：

```
git config user.name "<GitHub账号>"
git config user.email "<注册邮箱>"
```

## 2. 提交编码（防乱码，重要）

本项目文档、提交信息均为中文。历史教训：Windows 上编码错误会导致中文变成乱码且不可逆丢失。**务必**：

- 提交信息一律使用 **UTF-8（无 BOM）**
- 在 Windows 上额外设置：
  ```
  git config --global core.quotepath false
  git config --global i18n.commitEncoding utf-8
  git config --global i18n.logOutputEncoding utf-8
  ```
- 提交前用 `git log -1` 检查中文显示正常再 push
- 若发现提交信息显示乱码，**不要继续 push**，先修正再提交

## 3. 提交流程

1. `git status` 查看改动
2. `git add <文件>`（不要用 `git add .`，避免误提本地文件）
3. `git commit -m "中文描述"`（说明改了什么、为什么）
4. `git push`（正常推送即可；**禁止** `--force`，除非明确确认要改写历史）

## 4. 本项目提交约定

- `.codebuddy/` 只提交 `rules/`，其余（`plans/` 等）已在 `.gitignore` 忽略
- `build/`、`.gradle/`、`local.properties`、`.idea/` 均为本地生成内容，不提交
- `gradle/wrapper/*.zip`（本地 Gradle 发行包，约 151MB）不提交；`distributionUrl=gradle-9.7.1-bin.zip` 为相对路径，**克隆到其它机器后需自行放置该 zip 才能构建**，或临时改用官方 https URL
- 规则与文档：先改 `shared/`（唯一维护入口），再同步到 `.codebuddy/rules/` 与 `.cursor/rules/`（见 `shared/README.md`）
- 不提交 `.DS_Store`、临时文件、签名文件（`*.jks` / `*.keystore`）

## 5. push 前自检

- [ ] `git status` 无预期外文件（`build/`、`.codebuddy/plans/`、zip 等）
- [ ] `git log -1` 中文显示正常
- [ ] 提交信息说明改了什么、为什么
