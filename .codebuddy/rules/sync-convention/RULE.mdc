---
description: 手动双目录维护约定，CodeBuddy 与 Cursor 规则需同时更新保持一致
alwaysApply: true
enabled: true
updatedAt: 2026-08-25
provider:
---

# 手动双目录维护约定

本项目由 CodeBuddy 与 Cursor 共同维护，规则文件分别存在于两个目录中，需手动保持同步。

项目说明与维护流程见 `shared/README.md`。

## 规则文件位置

| 工具 | 文件位置 |
|------|----------|
| CodeBuddy | `.codebuddy/rules/<name>/RULE.mdc` |
| Cursor | `.cursor/rules/<name>.mdc` |

## 强制约定

1. **新增规则时**：在两个目录中分别创建规则文件，正文内容保持一致。
2. **修改规则时**：必须同时更新 CodeBuddy 与 Cursor 两份文件，禁止只改一处。
3. **删除规则时**：同时删除两个目录中的对应文件。
4. **frontmatter 字段差异**：两工具字段要求不同，按各自格式填写：
   - CodeBuddy：`description`、`alwaysApply`、`enabled`、`updatedAt`、`provider`
   - Cursor：`description`、`globs`、`alwaysApply`
5. **CodeBuddy 规则生效时机**：规则在会话开始时注入，创建或修改规则后需新建对话才生效。
