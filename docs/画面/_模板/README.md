# 画面文档模板（目录）

> 新建画面时：复制本 `_模板/` 目录 → 重命名为 `画面名/` → 填写 `README.md`，按需增加区域文件（可参考 [区域模板.md](区域模板.md)）→ 更新 [`docs/设计/路由设计.md`](../../设计/路由设计.md) 与 [`docs/式样/项目式样说明书.md`](../../式样/项目式样说明书.md) 第 5 章。
>
> 约定：一层目录 = 一个 route 画面；区域内多文件，从文件名定位（如 `菜单.md`、`一览.md`）。总览始终为目录下 `README.md`。

## 1. 画面概述

| 项 | 内容 |
|---|---|
| 画面名称 | （如：导入画面） |
| route | （如：`import-images`，见路由设计） |
| 职责 | （一句话） |

## 2. 路由定义

- 路由字符串：
- 必选参数：
- 可选参数：
- 参数说明：

## 3. 功能清单（总览）

- [ ] …
- 区域明细见同目录其它 `.md`

## 4. 用户操作（总览）

1. …

## 5. UI 壳层

- 顶部：
- 主体分区索引：（链到各区域文件）
- 底部：

## 6. 系统返回动作

| 场景 | 返回键 / 返回箭头行为 |
|---|---|
| 有上级画面 | 返回上一画面 |
| 根画面 | 退出应用 |

## 7. 进入与退出

| 方向 | 说明 |
|---|---|
| 从哪里进入 | |
| 可前往 | |
| 退出去向 | |

## 8. 数据依赖

有 ViewModel 的画面请写明：`*UiState` / `*UiEvent` / `*UiEffect`、Repository。架构见 [架构设计.md](../../设计/架构设计.md)。

## 9. 待定事项

- [ ] …

## 10. 开发记录

| 日期 | 变更 | 关联提交 |
|---|---|---|
| （日期） | 画面创建 | |
