# 启动画面

## 1. 画面概述

| 项 | 内容 |
|---|---|
| 画面名称 | 启动画面 |
| route | `splash` |
| 职责 | App 启动后的首个画面，当前仅展示应用名称，后续作为功能入口 |

## 2. 路由定义

- 路由字符串：`splash`
- 必选参数：无
- 可选参数：无

## 3. 功能清单

- [x] 显示应用名称（当前为静态文字，对应现有 `MainActivity.MainScreen`）
- [ ] （规划）功能入口导航

## 4. 用户操作

1. 打开 App 进入本画面
2. （当前无其他操作）

## 5. UI 描述

- 主体：居中显示应用名称（`headlineMedium` 标题样式）
- 背景：Material 3 主题默认背景，edge-to-edge 全屏

## 6. 系统返回动作

| 场景 | 行为 |
|---|---|
| 根画面（当前唯一画面） | 返回键退出应用（系统默认） |

## 7. 进入与退出

| 方向 | 说明 |
|---|---|
| 从哪里进入 | App 启动（MainActivity） |
| 可前往 | 暂无（后续画面待定） |
| 退出去向 | 退出应用 |

## 8. 数据依赖

- 无（当前静态展示）

## 9. 待定事项

- [ ] 后续功能入口（导入 / 浏览 / 导出）导航按钮
- [ ] 启动过渡动画（如需要）

## 10. 开发记录

| 日期 | 变更 | 关联提交 |
|---|---|---|
| 2026-08-26 | 建立画面文档（对应现有 MainActivity 模板界面） | `f600abb` |
