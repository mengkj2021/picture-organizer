# 画面文档模板

> 本文件是「图片整理」App 每个画面的文档模板。
> **新增画面时**：复制本文件 → 重命名为 `画面名.md`（如 `导入画面.md`）→ 按章节填写 → 更新《路由设计.md》与《项目式样说明书》第 5 章画面清单。
> 所有画面文档统一放在 `docs/画面/` 目录。

## 1. 画面概述

| 项 | 内容 |
|---|---|
| 画面名称 | （如：导入画面） |
| route | （如：`import`，见《路由设计.md》） |
| 职责 | （一句话说明本画面做什么） |

## 2. 路由定义

- 路由字符串：
- 必选参数：
- 可选参数：
- 参数说明：

## 3. 功能清单

- [ ] 功能一
- [ ] 功能二

## 4. 用户操作

1. 操作步骤一
2. 操作步骤二

## 5. UI 描述

（简单描述布局与主要组件，不追求像素级）
- 顶部：
- 主体：
- 底部 / 浮动按钮：

## 6. 系统返回动作

| 场景 | 返回键 / 返回箭头行为 |
|---|---|
| 有上级画面 | 返回上一画面 |
| 根画面 | 退出应用 |
| 特殊场景 | （如：表单未保存时的拦截确认） |

## 7. 进入与退出

| 方向 | 说明 |
|---|---|
| 从哪里进入 | 如：主界面点击「导入」 |
| 可前往 | 如：图片详情画面 |
| 退出去向 | 返回上一画面 / 退出应用 |

## 8. 数据依赖

（本画面读写的数据、状态、持久化位置，如：图片列表、标签集合、私有目录路径）

有 ViewModel 的画面请写明：`*UiState` / `*UiEvent` / `*UiEffect`、Repository 接口与实现。架构总览见 [架构设计.md](../架构设计.md)。

## 9. 待定事项

- [ ] 待定项一

## 10. 开发记录

| 日期 | 变更 | 关联提交 |
|---|---|---|
| （日期） | 画面创建 | （提交 hash） |
