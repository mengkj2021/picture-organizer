# 待处理一览 · 菜单

## 1. 区域概述

| 项 | 内容 |
|---|---|
| 所属画面 | 主画面 · 待处理 Tab |
| 区域名称 | 菜单 |
| 对应 UI | `TabActionBar` + 筛选条件摘要（`MainTab.Pending`） |

## 2. 功能清单

- [x] 编辑 / 完成
- [x] 全选（编辑模式下；**仅当前页**可见项）
- [x] 「移动到」→ 已确认 / 不修改（有选中时）
- [x] 「导入图片」→ `import-images`
- [x] **筛选**：跳转 [`filter`](../../筛选画面/README.md)；返回后展示条件摘要 Chip；可清除

## 3. 用户操作

1. 点「编辑」进入编辑模式；「完成」退出并清空选中
2. 「全选」勾选**当前页**列表项
3. 「移动到」选择目标状态，批量改库 + 搬文件
4. 「导入图片」进入导入画面
5. 「筛选」→ `filter`；应用后该 Tab 回到第 1 页并清空选中；摘要 Chip /「清除」展示当前条件

## 4. UI 描述

- 水平工具栏；导入在右侧；筛选 icon 进筛选画面
- 有筛选时：工具栏下方 Chip 摘要（标签名 +「未打标签」）+ 清除

## 5. 数据 / 事件

- `ToggleEditMode` / `SelectAll` / `MoveSelectedTo` / `ImportImages`
- `SetTagFilter` / `ClearTagFilter`（由筛选画面回传触发）

## 6. 待定事项

- 无
