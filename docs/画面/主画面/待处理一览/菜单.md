# 待处理一览 · 菜单

## 1. 区域概述

| 项 | 内容 |
|---|---|
| 所属画面 | 主画面 · 待处理 Tab |
| 区域名称 | 菜单 |
| 对应 UI | `TabActionBar`（`MainTab.Pending`） |

## 2. 功能清单

- [x] 编辑 / 完成
- [x] 全选（编辑模式下）
- [x] 「移动到」→ 已确认 / 不修改（有选中时）
- [x] 「导入图片」→ `import-images`

## 3. 用户操作

1. 点「编辑」进入编辑模式；「完成」退出并清空选中
2. 「全选」勾选当前列表全部
3. 「移动到」选择目标状态，批量改库 + 搬文件
4. 「导入图片」进入导入画面

## 4. UI 描述

- 水平工具栏；导入按钮在右侧

## 5. 数据 / 事件

- `ToggleEditMode` / `SelectAll` / `MoveSelectedTo` / `ImportImages`

## 6. 待定事项

- 无
