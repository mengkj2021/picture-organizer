# 已确认一览 · 菜单

## 1. 区域概述

| 项 | 内容 |
|---|---|
| 所属画面 | 主画面 · 已确认 Tab |
| 区域名称 | 菜单 |
| 对应 UI | `TabActionBar` + 筛选条件摘要（`MainTab.Confirmed`） |

## 2. 功能清单

- [x] 编辑 / 完成
- [x] 「移动到」→ 待处理 / 回收站（编辑态行内）
- [x] **打包导出** → `export-zip`（C7）
- [x] **筛选**：跳转 [`filter`](../../筛选画面/README.md)；返回展示摘要；可清除
- [x] **F5**：全选收入「更多」溢出菜单

## 3. 用户操作

1. 编辑 →「更多」全选当前页 →「移动到」改状态
2. 「完成」退出编辑
3. 「筛选」同待处理一览 · 菜单
4. 「打包导出」进入打包画面

## 4. UI 描述

- 工具栏 + 筛选 + 更多；右侧「打包导出」

## 5. 数据 / 事件

- `ToggleEditMode` / `SelectAll` / `MoveSelectedTo` / `SetTagFilter` / `ClearTagFilter`
- 导航：`export-zip`

## 6. 待定事项

- 无
