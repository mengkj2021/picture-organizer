# 导入画面 · 进度与结果

## 1. 区域概述

| 项 | 内容 |
|---|---|
| 所属画面 | 导入画面（`import-images`） |
| 区域名称 | 进度与结果 |
| 对应 UI | 全屏 Loading 遮罩 + Snackbar |

## 2. 功能清单

- [x] 导入前按阈值压缩（>2MB 或长边 >1920 → JPEG 85）
- [x] 写入 `images/pending/` + Room（Pending）
- [x] 进度文案「正在导入 x/y」
- [x] 全部成功：返回主画面；部分/全部失败：Snackbar

## 3. 用户操作

1. 选择完成后等待；成功自动返回；失败可再选或返回

## 4. UI 描述

- 半透明遮罩 + `CircularProgressIndicator` + 进度文字

## 5. 数据 / 事件

- `ImageManager.prepareForImport`；`repository.insert`；`ImportUiEffect`

## 6. 待定事项

- [ ] 失败明细列表与重试
