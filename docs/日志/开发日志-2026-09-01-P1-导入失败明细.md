# 开发日志 · 2026-09-01 P1 导入失败明细

## 完成内容

- **P1 导入失败明细**：导入结束有失败时弹出 AlertDialog 明细，逐条显示文件名与原因分类（无法读取 / 无法解码 / 未知 + 原始异常信息）；支持单张重试、全部重试；全部成功后回主画面
- 失败项保留原 `Uri` 供重试；`ImageManager` 新增 `displayNameOf(uri)` 查询显示名（失败回退路径末段）
- 错误分类：`SecurityException` / `FileNotFoundException` / `IOException` → 读取失败；`IllegalStateException` → 解码失败；其余 → 未知
- 移除原「仅计数 + Snackbar」逻辑及 `import_fail_all` / `import_fail_partial` 文案

## 关联文档

- [改动清单.md](../工程/改动清单.md)（P1 ✅）
- [导入画面](../画面/导入画面/README.md)、[进度与结果](../画面/导入画面/进度与结果.md)
