# 开发日志 · 2026-09-05 · F29

## 摘要

**F29**（续接）：落地拆票门槛、票内步骤/已锁定、绿黄红档与 `ticket/` 分支；母题–子票写入票 README；用 F11（不拆 + 步骤）与 F28（建议拆）试跑；不引入 `.planning/`。无 Android 代码，无纯逻辑测试。

## 改动

- 四类票 `_模板/`：档位、已锁定、实施步骤（绿档可极简）；`_模板/README` 起票说明
- `shared/prompts/功能开发.md`：步骤 1 对照门槛、步骤 6 先确认步骤/档位；专节「拆票门槛与档位」
- `shared/prompts/起票分析.md`：起票时对照门槛；落盘子票 ↔ 母题
- `shared/rules/git-convention.md`：工作分支节；同步 `.cursor` / `.codebuddy`
- `shared/rules/dev-convention.md`：强制约定第 10、11 条短句；三处同步（hash 一致）
- `docs/工程/票/README.md`：「母题与子票」权威说明
- 试跑：`F11` 档位黄、不拆、四步步骤表；`F28` 门槛命中 4 条、建议切分（未起子票）
- `shared/AI配置落位.md` 矩阵勾选 F29 已落地

## 验证

- 模板四类均含已锁定 / 实施步骤 / 绿档极简说明
- 功能开发门槛为 5 条、≥2 建议拆；档位表含中途不分叉例外
- git 三处、dev 三处 hash 分别一致
- 无 `.planning/` 目录；无强制四窗口

## 字数（dev-convention，UTF-8 含 frontmatter）

| | 字符数 |
|---|---|
| F24 瘦身后（参考） | 1604 |
| 本票收尾后 | 2575（含其后 F19/F30/F29 累加；本票仅增第 10/11 条短句） |

## 验收对照自评（F29）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 票 `_模板/`（四类）含「关联画面 · 预估 / 实际改动」；起票分析填预估；功能开发收尾回写实际 | ☑ | 上一会话已落地（130dafe）；本会话未回退 |
| 票 `_模板/` 含「已锁定」与「实施步骤」用法（标明绿档可极简） | ☑ | `_模板/bug票.md` 等四类 + `_模板/README.md` 填写说明 |
| `功能开发.md` 含拆票门槛（≥2 条建议拆）、步骤确认后再写码、档位/分支提示 | ☑ | 步骤 1/6 + 「拆票门槛与档位」专节 |
| git 约定含 `ticket/票号-简述` 命名与绿/黄/红策略；明确 merge/push 由用户决定、禁擅自 force | ☑ | `shared/rules/git-convention.md`「工作分支」；提交流程仍禁 `--force` |
| dev-convention（shared + CodeBuddy + Cursor 三处同步）含拆票/步骤短约定，且不明显回潮膨胀 | ☑ | 第 10、11 条；三处 hash 一致；细则在 prompts 不进常驻长文 |
| 母题-子票关联写法有一处权威说明（票 README 或改动清单旁注） | ☑ | `docs/工程/票/README.md` §3「母题与子票」 |
| 用 1 张未做待办票试跑（拆票或票内步骤+分支至少一种）；别扭点记入本票开发记录 | ☑ | F11 票内步骤（不拆/黄）；F28 拆票评估；别扭点见 F29 票开发记录 |
| 开发日志（文件名带 F29）已写；不引入 `.planning/` 或强制四窗口 | ☑ | 本文件；功能开发专节写明不引入 |

## 关联提交

见本票开发记录（用户验收后提交）。
