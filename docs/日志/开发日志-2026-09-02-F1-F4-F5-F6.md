# 开发日志 · 2026-09-02 F1 / F4 / F5 / F6（含 T1–T3 转票）

## 范围

- `.gitignore` 忽略 `android/gradle/gradle-daemon-jvm.properties`
- **T1–T3** 调查结案并转 **F4–F6**；**F1** 确认方案 B 并落地

## 完成内容

### F1 导入缩短落盘名
- `ImageManager.prepareForImport`：DISPLAY_NAME → 清洗截断主名≤40 → 保留/推断扩展名；冲突追加短 id
- 压缩仍输出 `.jpg`，主名同样截断

### F4 详情放大收起编辑区（← T1）
- scale>1 默认隐藏重命名/标签；单击主图切换强制显示；双击重置仍可用；缩略条保留

### F5 三一览溢出菜单（← T2）
- `TabActionBar`：行内保留编辑、筛选、导入/打包、编辑态移动到；全选/删除进「更多」

### F6 筛选文件名 + 排序（← T3）
- `TagFilterCriteria`：`nameContains` + `ImageListSort`
- 主画面 / 打包 / 筛选 route（`q`/`sort`）共用；默认导入日期倒序

## 验收

- `compileDebugKotlin` 通过

## 关联票

F1 / F4 / F5 / F6 ✅；T1 / T2 / T3 ✅（已转票）
