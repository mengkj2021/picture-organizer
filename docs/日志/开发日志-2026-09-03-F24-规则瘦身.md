# 开发日志 2026-09-03 · F24 规则瘦身

## 范围

- 票：F24（`dev-convention` 常驻 vs 按需）
- 类型：功能 / 体验（工程质量 · 规则组织）

## 改动

1. **`shared/rules/dev-convention.md`**（alwaysApply）
   - 包结构细则移出常驻正文，改为引用 `docs/设计/架构设计.md` §2
   - 「权威文档」改为「全量见 docs/README + 高频入口表」
   - **不新增约束**；语义与原先一致
2. **`docs/设计/架构设计.md` §2**
   - 收纳原常驻「放置约定」为 §2.2；§2 为包结构权威源
   - 文首引用方向改为规则 → 架构（不再反向）
3. **三处同步**：`.cursor/rules/dev-convention.mdc`、`.codebuddy/rules/dev-convention/RULE.mdc`

## 字数（规则正文，UTF-8，含 frontmatter）

| | 字符数 |
|---|---|
| 瘦身前 | 2086 |
| 瘦身后 | 1604（约 -23%） |

## 验收自检

- [x] 常驻变短；核心纪律（票驱动 / 文档先行 / route / 依赖 / 票号）仍在
- [x] 包结构可从架构 §2 定位；规则第 7 条有引用
- [x] 三处文件 hash 一致
- [ ] 新会话注入验证（需用户新建对话确认）

## 关联提交

见本票开发记录。
