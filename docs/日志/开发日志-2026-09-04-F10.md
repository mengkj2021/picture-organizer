# 开发日志 · 2026-09-04 · F10

## 摘要

**F10**：导入中返回全程拦截；成功 / 重试清完后停留导入画面并 Snackbar；进入画面清理 `pending/` 孤儿。**F20**：孤儿判定先测后写。

## 改动

- `PendingOrphanCleaner` + `PendingOrphanCleanerTest`（5 用例）
- `AppFileManager.cleanupPendingOrphans`
- `ImportViewModel`：`ImportCompleted(n)`；`init` 清孤儿；注入 `fileManager`
- `ImportScreen`：BackHandler 始终启用、导入中吞事件；Snackbar；失败「完成」仅关弹窗；去掉 `onImportFinished`
- `NavHost`：导入 destination 仅 `onBack`
- 文档：导入 README / 进度与结果、式样 §4.1、架构 §3.2、票与台账、错题本

## 验证

```
export JAVA_HOME=…/jbr-17.0.14/Contents/Home
$GRADLE_9_6_1/bin/gradle :app:testDebugUnitTest --tests '…PendingOrphanCleanerTest' …
```

- `testDebugUnitTest`（本票相关 +既有）：**BUILD SUCCESSFUL**
- `PendingOrphanCleanerTest`：5/5 通过

## 验收对照自评（F10）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 导入中：UI 返回按钮、系统返回键、返回手势均无响应，画面保持 | ☑ | `ImportScreen`：`BackHandler { if (!isImporting) onBack() }`；顶栏 `enabled = !isImporting`。验证：逻辑上导入中回调空操作，事件被消费 |
| 导入完成后不自动返回；可再次选图导入下一批 | ☑ | 去掉 `ImportFinished`→`popBackStack`；`ImportCompleted` 仅 Snackbar；按钮在 `!isImporting` 可再点 |
| 完成有明确反馈；空闲态可正常返回 | ☑ | `R.string.import_completed` Snackbar；空闲 `BackHandler` / 顶栏走 `onBack` |
| pending 目录无 DB 记录孤儿文件被清理 | ☑ | `PendingOrphanCleaner` + `cleanupPendingOrphans`；单测 5/5；VM `init` 调用 |
| 文档与代码对齐；开发日志（文件名带 F10）已写 | ☑ | 进度与结果 / README / 式样 / 架构已改；本文件 |

## F20 实测

本票含可测纯逻辑（孤儿文件名判定）→ 先写 `PendingOrphanCleanerTest`，再实现 cleaner 与接线；BackHandler / Snackbar / 导航为 UI 豁免。
