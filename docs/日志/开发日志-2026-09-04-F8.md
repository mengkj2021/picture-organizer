# 开发日志 · 2026-09-04 · F8

## 摘要

**F8**：删除标签库标签时判断图片引用；无引用直接删；有引用二次确认后批量去掉 `tagsJson` + Exif，再删词表。**F20 实测**：先写 `removeTagName` / `countImagesWithTag` 单测再实现。

## 改动

- `ImageListItem.removeTagName` / `countImagesWithTag` + `ImageListItemTest` 增补
- `ImageRepository` / `RoomImageRepository` / `MockImageRepository`：`countImagesWithTag`、`removeTagFromAllImages`
- `TagManageViewModel`：注入 `ImageRepository`；`cascadeDeleteTag` 二层确认；Exif 失败汇总 Snackbar
- 文案与标签管理 / 式样 / 架构文档

## 验证

- `testDebugUnitTest`：**BUILD SUCCESSFUL**（含 F8 新增用例）

## 验收对照自评（F8）

| 验收项 | 结果 | 证据 |
|---|---|---|
| 删除无引用标签：确认后直接删除成功 | ☑ | `confirmDeleteTag`：`count==0` → `deleteTag`；无 cascade 状态 |
| 删除有引用：弹框张数；继续清 tagsJson+Exif 并删词表；取消不动 | ☑ | `cascadeDeleteTag` UI + `removeTagFromAllImages` 后 `deleteTag`；取消只清 cascade |
| 删除后全库无图片 tagsJson/Exif 含该名（抽样） | ☑ | `removeTagFromAllImages` 对命中图写 DB+`writeUserTags`；逻辑由 `removeTagName` 单测覆盖 |
| 文档对齐 README §8；开发日志带 F8 | ☑ | 标签管理 README / 标签列表已改；本文件 |

## F20 实测

本票含可测纯逻辑（精确去标 / 计数）→ 先补 `ImageListItemTest`，再接线 Repository / VM；收尾单测绿灯。
