# 开发日志 · 2026-08-28 导入画面与 Room 接入

## 完成内容

- 新增 route `import-images` 与 [导入画面](../画面/导入画面/README.md)
- `RoomImageRepository` 替换 Mock（Flow 观察三状态列表）
- `AppFileManager` / `ImageManager`：私有目录落盘；临时压缩阈值 2MB / 长边 1920 / JPEG 85
- 主画面「导入图片」导航至导入画面；导入中 Loading；完成后返回待处理列表
- **A8**：`images/{pending,confirmed,no_modify}/` 三子目录；「移动到」同步搬文件；不修改 Tab「删除」删文件+行；启动 `migrateFlatPathsIfNeeded`
- `assembleDebug` 通过

## 关联文档

- [架构设计.md](../设计/架构设计.md)
- [改动清单.md](../工程/改动清单.md) A1–A8
