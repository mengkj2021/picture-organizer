# 开发日志 2026-09-03 · S3

## 票

S3：主画面 Tab「不修改」改名「回收站」（仅显示层；行为与数据不变）

## 做了什么

- **F20 先测后写**：先改 `ImageListItemTest`（保留字断言换「回收站」；补「不修改」不再保留用例），再改 `STATUS_TAGS`
- 显示文案：`strings.xml` Tab / 空态 / 移动到 / 保留字提示 / 教程第 2 页
- Mock：`noModifyItems` 去掉把旧状态名当用户标签的示例
- 文档：式样书 §4.3 / §6、架构删除处、主画面与三一览、教程、测试用例；区域目录 `不修改一览` → `回收站一览`
- **不动**：`ImageStatus.NoModify`、`MainTab.NoModify`、目录 `no_modify/`、DB

## 验收对照自评（S3）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 文档先行同步完成（式样书「待定」→ 已确认或明确不适用） | ☑ | 式样书 §4.3 / §6、主画面 README + `回收站一览/`、教程、架构、测试用例已同步；票内待定（旧词是否仍禁）接票确认：不再保留 |
| 全 UI 用户可见处为「回收站」，无「不修改」残留（教程 / 空态 / 移动到 / 保留字提示） | ☑ | `strings.xml` 五处文案已换；`src` 内用户可见「不修改」仅剩注释说明改名与「旧词不再保留」测试 |
| 行为与现状一致：status / 目录 / 枚举不变；「回收站」内移动与删除语义不变；保留字新词生效（输入「回收站」作标签被拒） | ☑ | 枚举/目录未改；`STATUS_TAGS`=`待处理/已确认/回收站`；`ImageListItemTest` 全绿（含旧词不保留） |
| 开发日志（文件名带本票号）已写 | ☑ | 本文件 |

## F20 实测回证

- 本票含可测纯逻辑（`STATUS_TAGS` / `userTagsOf`）
- 顺序：先测 → 再实现；命令：`gradlew testDebugUnitTest --tests com.pictureorganizer.model.ImageListItemTest` → **BUILD SUCCESSFUL**

## 备注

- 关联提交：待用户验收后提交
