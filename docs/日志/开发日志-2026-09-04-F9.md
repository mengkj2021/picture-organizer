# 开发日志 · 2026-09-04 · F9

## 摘要

**F9**：详情库标签数 > 10 时主区只显示已选库标签 +「选择标签」`ModalBottomSheet`；面板内 `ToggleLibraryTag` 即时持久化（1A）；已选全部仍显示（2A）。**F20**：无纯逻辑可测，未加单测。

## 改动

- `ImageDetailScreen`：`LIBRARY_TAG_COLLAPSE_THRESHOLD = 10`；收起分支 + BottomSheet
- `strings.xml`：`detail_tag_pick_library` / `detail_tag_pick_library_title`
- 文档：`标签.md`、详情 README、式样、票与台账

## 验证

- `:app:compileDebugKotlin` + `testDebugUnitTest`：**BUILD SUCCESSFUL**

## 验收对照自评（F9）

| 验收项 | 结果 | 证据 |
|---|---|---|
| 库标签 ≤ 10：与现状一致 | ☑ | `collapseLibrary == false` 时 FlowRow 铺全库 Chip |
| 库 > 10：主区仅已选 + 按钮；面板全库可勾选 | ☑ | filter `name in userTags` + OutlinedButton + ModalBottomSheet |
| 面板勾选即时生效、与主区一致 | ☑ | 同 `ToggleLibraryTag`，无暂存状态 |
| 自定义标签输入/删除不受影响 | ☑ | 自定义区与 TextField 未改路径 |
| 标签.md 同步；日志带 F9 | ☑ | 本文件与画面文档 |

## F20

无 JVM 友好规则可抽（纯 Compose 分支），注明豁免。
