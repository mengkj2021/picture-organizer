# 开发日志 · 2026-08-27 主画面与数据层架子

> 记录当时里程碑。**后续以 [开发日志-2026-08-28.md](开发日志-2026-08-28.md) 为准**（Room 生产数据源、导入、三目录、删除等已落地）。

## 1. 背景

- 前置：2026-08-25 构建工具链升级完成；2026-08-26 建立路由与画面文档体系
- 目标：实现主画面 UI 与交互骨架，引入 MVVM 与 Room 持久化基础，为后续真实导入做准备

## 2. 完成内容

### 2.1 导航与启动

| 项 | 内容 |
|---|---|
| 路由 | `splash` 自动跳转 `main`（splash 不在 back stack 保留） |
| 依赖 | Navigation Compose 2.9.0 |

### 2.2 主画面（main）

| 项 | 内容 |
|---|---|
| 布局 | TopAppBar + 三 Tab NavigationBar + LazyColumn 列表 |
| Tab | 待处理 / 已确认 / 不修改 |
| 列表 | 各 Tab 约 12 条假数据；列表项：假缩略图 + 日期 + 描述 + 标签 |
| 操作栏 | TabActionBar：编辑 / 完成、全选、移动到（下拉） |
| 编辑模式 | Checkbox 多选、批量改状态（内存假数据跨 Tab 移动） |
| Tab 专属 | 待处理「导入图片」、不修改「删除」（本阶段 Snackbar「功能开发中」） |

### 2.3 MVVM 分层

| 层 | 主要文件 |
|---|---|
| UI | `MainScreen`、`ui/main/tab/*` |
| ViewModel | `MainViewModel`、`MainUiState`、`MainUiEvent`、`MainUiEffect` |
| Repository | `ImageRepository`、`MockImageRepository` |
| 假数据 | `MockImageListData` |

### 2.4 Room 与包结构

| 项 | 内容 |
|---|---|
| 依赖 | Room 2.7.2 + KSP 2.3.11 |
| Application | `PictureOrganizerApplication` 懒加载 `AppDatabase` |
| 数据库 | `picture_organizer.db`，version 1 |
| Entity | `ImageEntity`（images 表） |
| DAO | `observeByStatus`、`insert`（占位，未接入 Repository） |
| 包结构 | `model/`、`data/local/`、`util/file/`、`util/image/` |

### 2.5 构建验证

- `gradlew.bat assembleDebug` 通过
- UI 仍使用 Mock 数据，行为与引入 Room 前一致

## 3. 刻意未做

- `RoomImageRepository` 替换 Mock
- 真实导入 / 删除 / 压缩 / 缩略图
- Hilt / Koin 等 DI 框架

## 4. 关联文档

- [架构设计.md](../设计/架构设计.md)
- [主画面](../画面/主画面/README.md)
- [项目式样说明书.md](../式样/项目式样说明书.md) v0.7

## 5. 下一步

1. 实现 `RoomImageRepository`，ViewModel 注入切换
2. `AppFileManager.importFromUri()` — 复制到私有目录
3. 导入流程画面（文档先行）
