# 开发日志 · 2026-09-01 D1 筛选匹配抽取

## 完成内容

- **D1 筛选匹配抽取**：标签筛选匹配逻辑收口到 `model` 包唯一实现
- 新增 `model/ImageTagFilter.kt`：顶层扩展 `ImageListItem.matchesTagFilter(selectedTagNames, includeUntagged)`（原 `MainViewModel.matchesTagFilter` 平移，行为不变）
- `MainViewModel`：删除 companion `matchesTagFilter`，改调扩展函数
- `ExportZipViewModel`：删除跨包依赖 `import com.pictureorganizer.ui.main.MainViewModel`（消除 `ui.exportzip → ui.main` 耦合），两处调用改扩展函数

## 关联文档

- [改动清单.md](../工程/改动清单.md)（D1 ✅，已从 D 表移除）
- [架构设计.md](../设计/架构设计.md)（§列表行为描述不变，未涉及函数名，无需改动）
