# 开发日志 2026-09-03 · F23 本地质量门禁

## 范围

- 票：F23（pre-push 自动 `ktlintCheck`）
- 类型：功能 / 体验（工程质量）
- F20：无纯业务逻辑，未加单测

## 改动

1. `ktlintFormat`：清 main 既有违规（`TagFilterCriteria`、`PictureOrganizerNavHost`、`ImageDetailScreen`/`ViewModel`、`ImportUiState`/`ViewModel`）
2. 新增 `.githooks/pre-push`（入仓）；本机 `git config core.hooksPath .githooks`
3. `shared/rules/git-convention.md` 增「push 前自动门禁」→ 同步 `.cursor/`、`.codebuddy/`
4. 根 `README.md` 开发约定补启用一句

## 启用

```
git config core.hooksPath .githooks
```

## 验证

- `gradlew ktlintCheck`：全绿
- 直接跑 hook（干净树）：exit 0，提示「ktlintCheck 通过」
- 临时向 `TagFilterCriteria.kt` 注入违规后再跑 hook：exit 1 + 修复提示；还原后 `ktlintCheck` 再绿

## 验收对照自评（F23）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| push 前自动跑 ktlintCheck；违规时 push 被拒并给出可读提示 | ☑ | `.githooks/pre-push`；制造违规后 hook exit 1，打印修复示例与 `--no-verify` 提示 |
| 正常代码 push 不受影响（门禁通过） | ☑ | 干净树上 hook exit 0；`ktlintCheck` BUILD SUCCESSFUL |
| 启用方式已写入 git-convention（三处同步）；克隆后一条命令可启用 | ☑ | `shared/rules/git-convention.md` + `.cursor/rules/git-convention.mdc` + `.codebuddy/rules/git-convention/RULE.mdc`；命令 `git config core.hooksPath .githooks`；README 亦有一句 |
| 开发日志（文件名带本票号）已写 | ☑ | 本文件 |

## 关联提交

见票开发记录（待用户决定 commit）。
