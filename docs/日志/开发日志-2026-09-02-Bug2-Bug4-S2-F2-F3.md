# 开发日志 · 2026-09-02 Bug2–4 / S2 / F2 / F3

## 范围

按起票顺序落地：**Bug2、Bug3、Bug4、S2、F2、F3**（跳过需确认的 F1 与调查票 T1–T3）。

## 完成内容

### Bug2 详情软键盘顶栏消失
- `MainActivity`：`windowSoftInputMode=adjustResize`
- `ImageDetailScreen`：`contentWindowInsets` 排除 IME；内容区 `imePadding()`，顶栏不再被顶出可视区

### Bug3 筛选底栏被切
- `FilterScreen` bottomBar：`navigationBarsPadding()`（自定义 bottomBar 需自行消费 inset）

### Bug4 导入返回不可点
- `ImportScreen`：`Scaffold` `fillMaxSize`；`BackHandler(enabled = !isImporting)`；顶栏返回在 Idle 可点（导入中点击无效）

### S2 自定义标签自动入库
- `SaveTag` 成功后 `ensureTagInLibrary`：库无同名则 `insertTag`，已存在跳过

### F2 重命名后缀锁定与空名恢复
- 输入仅主名；扩展名只读 suffix；保存/套模板强制原扩展名
- 主名清空后失焦 → 恢复原主名

### F3 导入重复提示
- 新增文案 `import_duplicate_hint`，展示于导入画面顶部

## 验收

- `compileDebugKotlin` 通过

## 关联

- 票：Bug2 / Bug3 / Bug4 / S2 / F2 / F3（均 ✅）
- 画面：详情重命名/标签、筛选、导入来源选择；式样书 §4.3（S2）
