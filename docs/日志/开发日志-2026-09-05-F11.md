# 开发日志 · 2026-09-05 · F11

## 票

F11 导入重名图片逐对询问（记录原图名 + 询问开关）

## 做了什么

1. **文档**：导入画面 / 进度与结果、设置画面、式样 §4.1、架构（Room v4、`originalName`、Prefs、`ImportDuplicateLogic`）
2. **数据层**：`ImageEntity.originalName`；`MIGRATION_3_4`；Dao `getStoredOriginalNames`；mapper / Repository / Mock 贯通；schema `4.json`
3. **F20 先测后写**：`ImportDuplicateLogic` + `ImportDuplicateLogicTest`（大小写、批内、空白名）
4. **导入流**：开关开时 partition → 先 auto → 冲突 `CompletableDeferred` 询问；写 `originalName` 入库
5. **设置**：`SettingsViewModel` + Switch「导入重名询问」（`import_duplicate_ask_enabled`，默认 true）
6. 顺带 `ktlintFormat` 修 `PendingOrphanCleanerTest` 换行（否则挡 push 门禁）

## 验证

- `gradlew testDebugUnitTest --tests ImportDuplicateLogicTest` ✅
- `gradlew ktlintCheck` ✅
- 真机：同名询问 / 跳过 / 本次不再询问 / 关开关直导 → **待用户验收**

### 验收对照自评（F11）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 导入时原图名入库；新导入的同名原图可被判出 | ☑ | `ImageEntity.originalName` + insert 写 DISPLAY_NAME；`getStoredOriginalNames` + `ImportDuplicateLogic.partition`；单测 caseInsensitive / batchInternal |
| 一批含重名：无冲突的先导入，冲突的逐张弹窗询问；导入 → 新增另一条记录，跳过 → 不导入 | ☑ | `ImportViewModel.importUris` 分流；弹窗三按钮；Skip 只 bumpProgress 不 insert |
| 弹窗含「本次不再询问」：点后本批剩余重名图不弹窗、照常导入，且不改变全局开关 | ☑ | `DuplicateAskDecision.SkipAskingRest` 置 `skipAsking`；不写 Prefs |
| 开关默认开；设置画面关闭后导入不再询问（照常导入） | ☑ | Prefs 默认 true；`isImportDuplicateAskEnabled()==false` 时整批 `runOne` |
| 弹窗内含「可在设置中关闭」提示 | ☑ | `import_duplicate_settings_hint` |
| 文档与代码对齐；开发日志（文件名带 F11）已写 | ☑ | 画面/式样/架构已改；本日志 |

## 分支

`ticket/F11-导入重名询问`
