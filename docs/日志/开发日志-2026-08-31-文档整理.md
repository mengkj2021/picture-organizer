# 开发日志 2026-08-31（文档整理）

## 范围

对照 `docs/` 与 `app/src/main`（不改业务代码），做同步与结构优化。

## 同步要点

### 式样书 → v0.9
- §1 三步进度改为已完成（主功能）
- §4 补默认标签、压缩开关、exports + FileProvider
- §7 勾掉已落地的画面规划项；指向改动清单 B / P
- §8 / 页脚补 08-31 记录

### 架构设计
- 包树补全 settings / exportzip / renametemplate 等
- Preferences 三键；`rename_templates` 表；ZipExporter / RenamePatternApplier / FileProvider
- 导入 / 打包数据流；§6 仅余 B1/B2 + P

### 路由设计
- route 列与 `Routes.kt` 模板字符串一致
- §5 splash / tutorial（含 fromSettings）行为

### 画面
- 教程：设置重看、fromSettings 分支、文案与 strings 一致
- 主画面 / 筛选 / 导入 / 详情：交叉入口与数据依赖
- 标签区域去掉过时 R1 措辞

### 改动清单
- 追加 **P1–P8**（体验增强）、**D1**（筛选匹配抽取）
- 建议顺序更新

## 未改代码

- 教程第 3 页 `strings.xml` 仍含「后续…打包」（记入 **P6**）
- 其它 P 项仅文档登记，待排期
