# 开发日志 · 2026-09-01 P5 双击重置缩放

## 完成内容

- **P5 双击重置缩放**：图片详细画面主图双击恢复 1x 并居中
- `ZoomableImage` 在 `detectTransformGestures` 之外新增 `pointerInput(contentKey)` + `detectTapGestures(onDoubleTap)`，双击将 `scale` 重置为 1f、`offset` 归零
- 两个手势共存：transform 只消费移动事件不吞 tap；本区域无单击行为，无副作用
- 切换缩略条图片时的重置缩放行为保持不变（`remember(contentKey)` 原有机制）

## 关联文档

- [改动清单.md](../工程/改动清单.md)（P5 ✅，已从 P 表移除）
- [主图缩放.md](../画面/图片详细画面/主图缩放.md)（功能清单 / 用户操作 / 待定事项同步）
