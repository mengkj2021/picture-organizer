# 开发日志 2026-09-03 · Bug5 详情放大隐藏不生效

## 范围

- 票：Bug5（详情放大时重命名/标签未按 F4 隐藏；手势怪异）
- 类型：Bug
- F20：无纯逻辑（Compose 手势竞态 + UI 状态同步），未加单测

## 改动

1. `ImageDetailScreen.kt` / `ZoomableImage`
   - 变换后 350ms 内忽略伪单击（抬指被当成 tap）
   - `onScaledChanged` / `onTransform` 清 `forceShowEditor`
   - 回调 `rememberUpdatedState`；变换时间戳用 `longArray` 可变引用避免双 `pointerInput` 闭包过期
2. `docs/画面/图片详细画面/主图缩放.md`：补 Bug5 行为说明

## 验证

- 静态对照：捏合路径必清强制显示；伪 tap 短时窗抑制；`contentKey`（缩略条换图）重置 scale / 强制显示 remember 键
- 真机手势手感请用户验收（单击唤出、双击复位、捏合再藏）

## 验收对照自评（Bug5）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 放大（任意路径到 >1x）即隐藏文件名/标签；回到 1x 恢复 | ☑ | `onScaledChanged` 进/出缩放均清 `forceShowEditor`；`showEditor = !imageScaled \|\| forceShowEditor`；回到 1x 时 `imageScaled=false` |
| 单击唤出 / 双击复位 / 捏合缩放互不干扰，动作自然 | ☑ | 变换后 `TAP_SUPPRESS_AFTER_TRANSFORM_MS` 抑伪 tap；真单击仍 `onSingleTap` 切换；双击重置 scale；`onTransform` 在强制显示后再捏合会再藏 |
| 缩略条切换重置缩放后状态一致 | ☑ | `remember(contentKey)` / `remember(state.currentId)` 换图重置 scale、offset、forceShow、imageScaled |
| 开发日志（文件名带 Bug5）已写 | ☑ | 本文件 |

## 关联提交

见票开发记录（待用户决定 commit）。
