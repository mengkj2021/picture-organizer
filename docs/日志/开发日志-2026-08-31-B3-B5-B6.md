# 开发日志 2026-08-31（B3 / B5 / B6）

## 完成项

### B5 图标依赖瘦身
- 移除 `material-icons-extended`，改为显式 `material-icons-core`（Compose BOM）
- 扩展图标替换为 core：`FilterList→Search`、`Block→Close`、设置项 `Label/DriveFileRename/LocalOffer/MenuBook→List/Edit/Star/Info`、占位 `Image→Info`

### B3 release R8
- `isMinifyEnabled = true`、`isShrinkResources = true`
- 补充 `app/proguard-rules.pro`（Room / FileProvider 等）
- `assembleRelease` 通过（含 `minifyReleaseWithR8`）

### B6 格式统一
- 引入 ktlint Gradle 插件 14.2.0 + 根目录 `.editorconfig`
- Compose PascalCase：关闭 `function-naming` 规则
- 已执行 `:app:ktlintFormat` / `:app:ktlintCheck`

## 文档
- 更新 `docs/工程/改动清单.md`、式样书 §3.1
