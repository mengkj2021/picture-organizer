# 开发日志 · 2026-09-05 · F26

## 票

F26 设置新增 OssLicenses 画面（第三方开源许可一览）

## 做了什么

1. **锁定**：方案 A（aboutlibraries）+ route `oss-licenses` + 设置「查看教程」下方
2. **文档**：新建开源许可画面；设置 / 画面索引 / 路由 / 式样 §5 + §3.1 / 架构包树
3. **Gradle**：`aboutlibraries` 15.0.4；`plugin.android` + `aboutlibraries-compose-m3`
4. **代码**：`OssLicensesScreen`（`produceLibraries` 读 `R.raw.aboutlibraries` + `LibrariesContainer`）；`Routes` / NavHost / 设置入口 / strings
5. 分支：`ticket/F26-开源许可画面`

## 验证

- `gradlew :app:compileDebugKotlin` ✅
- `gradlew :app:ktlintCheck` ✅
- 生成 `aboutlibraries.json` 含 coil / room-runtime / datastore / exifinterface / navigation-compose / lifecycle / core-ktx ✅
- 真机：设置入口 → 列表 → 许可详情 → 返回 → **待用户验收**

### 验收对照自评（F26）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 设置画面可见「开源许可」入口，点击进入对应画面 | ☑ | `SettingsScreen` 教程下行 + `Routes.OSS_LICENSES`；NavHost 注册 `OssLicensesScreen` |
| 画面列出全部 runtime 第三方库（androidx 系、Room、Coil、ExifInterface、DataStore 等）及许可证，无遗漏 | ☑ | 插件生成 `aboutlibraries.json`；抽样匹配 coil/room/datastore/exif/navigation/lifecycle/core-ktx |
| 返回设置画面正常 | ☑ | `OssLicensesScreen(onBack = popBackStack)` |
| 文档与代码对齐（画面 README、路由表、式样书） | ☑ | `docs/画面/开源许可画面/`、路由、式样 §5/§3.1、架构 `ui/osslicenses/` |
| 开发日志（文件名带本票号 F26）已写 | ☑ | 本日志 |

## 分支

`ticket/F26-开源许可画面`
