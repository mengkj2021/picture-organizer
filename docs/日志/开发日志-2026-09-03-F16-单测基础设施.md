# 开发日志 2026-09-03 · F16 单测基础设施

## 范围

- 票：F16（单元测试基础设施 + 首批易碎纯逻辑）
- 类型：功能 / 体验（工程质量）

## 改动

1. `android/gradle/libs.versions.toml`：`junit` 4.13.2、`kotlinx-coroutines-test` 1.10.2
2. `android/app/build.gradle.kts`：`testImplementation` 上述依赖
3. 首批测试（`android/app/src/test/java/com/pictureorganizer/`）：
   - `util/file/RenamePatternApplierTest.kt`
   - `model/ImageListItemTest.kt`
   - `model/ImageTagFilterTest.kt`
4. 式样书 §3.1 同步测试依赖版本

## 怎么跑 / 怎么加（亦写入票开发记录）

```bat
cd android
set JAVA_HOME=C:\Program Files\Android\Android Studio\jbr
gradlew.bat testDebugUnitTest
gradlew.bat ktlintTestSourceSetCheck
```

新测试：与被测类同包，放在 `app/src/test/java/com/pictureorganizer/...`，类名 `*Test`，JUnit4。

## 验证

- `testDebugUnitTest`：**16** 用例全绿（Rename 5 + ImageListItem 5 + ImageTagFilter 6）
- `ktlintTestSourceSetCheck`：通过
- `ktlintCheck`（含 main）：**未全绿**——`ktlintMainSourceSetCheck` 为既有违规（ImageDetail / Import 等），本票未改主业务源码；遗留见票复盘

## 关联提交

见本票开发记录。
