# 开发日志 2026-09-03 · F18 交叉审查提示词

## 范围

- 票：F18（双 AI 交叉审查提示词）
- 类型：功能 / 体验（工程质量 · AI 协作流程）
- F20：无纯逻辑，未加单测

## 改动

1. 新建 `shared/prompts/交叉审查.md`：固定审查清单 ①–⑤ + 输出格式 +「本次审查」填写区；默认只审不改；可选 `审完可改`
2. `shared/prompts/README.md`：模板表登记该文件
3. `shared/README.md`：目录树补 `起票分析.md`、`交叉审查.md`

## 交叉审查演练（对 F23）

按新提示词流程，审查对象为 F23 已落地资产（本机 Cursor 作审查方；F23 由 Cursor 会话实现——同工具自审限制仍在，但清单格式与可读性问题抓取可验证提示词可用性）。

### 演练填写区等价内容

```
票号：F23
实现方：Cursor
审查方：Cursor（演练；正式使用时应换工具）
审查对象：.githooks/pre-push；shared/rules/git-convention.md（及 .cursor / .codebuddy 同步）；根 README 门禁一句；docs/日志/开发日志-2026-09-03-F23-本地质量门禁.md
```

### 演练结果摘要

| # | 审查项 | 结论 | 说明 |
|---|---|---|---|
| ① | 多余抽象 / 造轮子 | 通过 | hook 为单文件 shell，无多余层 |
| ② | 包结构与现有模式 | 通过 | `.githooks/` 入仓 + `core.hooksPath` 文档化；git-convention 三处已同步 |
| ③ | 边界与错误分支 | 问题（建议） | `.githooks/pre-push` 硬编码 `jbr-17.0.9` 目录名；用户升级/换名 JDK 文件夹时可能 miss，仍可依赖已设 `JAVA_HOME` 或 Android Studio JBR 回退——**非验收阻断**，记遗留即可 |
| ④ | 对照验收标准 | 通过 | 拒推提示、干净通过、启用命令、开发日志四项与票/日志一致 |
| ⑤ | 改动漂移 | 通过 | 未做 CI/单测并入；`ktlintFormat` 清债属门禁前置，票方案已写明 |

总评：有问题待改（低优先建议项）；**提示词清单与输出格式可用**。正式互审应换另一工具再跑一遍以体现「换脑」。

## 验收对照自评（F18）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| `shared/prompts/交叉审查.md` 就位：审查对象填写区 + 固定审查清单 + 输出格式 | ☑ | 文件含「本次审查」填写区；步骤 3 清单 ①–⑤；步骤 4 强制输出表 |
| `shared/prompts/README.md` 现有模板表补登记该文件 | ☑ | README「现有模板」表新增交叉审查行 |
| 至少一次真实交叉审查演练（日志记录审出/未审出的问题） | ☑ | 上文对 F23 演练；审出 ③ JDK 路径硬编码建议；①②④⑤ 通过 |
| 开发日志（文件名带本票号）已写 | ☑ | 本文件 |

## 关联提交

92a184d
