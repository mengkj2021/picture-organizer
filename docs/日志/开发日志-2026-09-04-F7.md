# 开发日志 · 2026-09-04 · F7

## 摘要

**F7**：导入时从 Exif `UserComment` 回读用户标签，与默认标签并集去重写入 `tagsJson`，并进标签词表。**F20 实测**：先写 `mergeImportTags` 单测再实现。

## 改动

- `ImageTagMetadata`：`parseUserCommentJson` / `mergeImportTags` / `readUserTags(InputStream)`
- `ImageManager.readUserTagsFromUri`：压缩前读源
- `ImportViewModel`：注入 `TagRepository`；合并标签 insert + 词表 ensure
- 单测：`ImageTagMetadataTest`（5 用例，全绿）
- 文档：标签.md §6、详情/导入 README、式样 §4.1、架构 §3.2、票与台账

## 验证

```
cd android
# 本机缺本地 gradle zip 时用已解压的 9.6.1：
$GRADLE_9_6_1/bin/gradle testDebugUnitTest
```

- `testDebugUnitTest`：**BUILD SUCCESSFUL**
- `ImageTagMetadataTest`：5/5 通过（并集保序、状态过滤、空白、仅 Exif、双空）

## 验收对照自评（F7）

| 验收项 | 结果 | 证据（改了哪 / 验证了什么） |
|---|---|---|
| 导入含 Exif 标签的 JPEG：`tagsJson` 含回读标签且与默认标签去重 | ☑ | `ImportViewModel.importSingle`：`readUserTagsFromUri` → `mergeImportTags` → `insert`；单测 `mergeImportTags_union_*` / `stripsStatus*` 绿灯 |
| 非 JPEG / 无标签 / 坏 JSON：导入照常，不报错 | ☑ | `readUserTags` / `readUserTagsFromUri` / `parseUserCommentJson` 均 `runCatching` → 空列表；不阻断 `prepareForImport` |
| 文档与代码对齐：勾选标签.md §6 与详情 README §9「导入时从 Exif 回读」待办 | ☑ | 两处已勾选；导入 README / 式样 / 架构已同步 |
| 开发日志（文件名带 F7）已写 | ☑ | 本文件 |

## F20 实测

本票含可测纯逻辑（导入标签并集去重）→ 先写 `ImageTagMetadataTest`，再实现 `mergeImportTags` 与接线；收尾 `testDebugUnitTest` 绿灯。`parseUserCommentJson` 依赖 `org.json`，按 F16 裁剪未做 JVM 测。
