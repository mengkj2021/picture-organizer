# 项目式样说明书（v0.5 草稿）

> 本文档记录「图片整理」项目的式样定义。**只记录已确认的内容，未决定项一律标注「待定」，后续逐项讨论补充。**

## 1. 项目概述与定位

- 目标：开发一款**图片管理软件**（App 或桌面软件），用于浏览、分类并整理图片文件。
- 用户场景：把收集 / 拍摄的图片按规则归类，**直接操作文件夹**整理真实文件。
- 三步规划进度：

| 步骤 | 内容 | 状态 |
|------|------|------|
| 1 | AI 开发目录搭建 | ✅ 已完成 |
| 2 | 项目式样准备 | 🔄 进行中（本文档） |
| 3 | 基础项目搭建与开发 | 🔄 进行中 |

## 2. 应用形态（已确认）

- **Android App**
- 桌面端：不在当前计划内

## 3. 技术栈 / UI（已确认）

- 语言：**Kotlin**
- UI 框架：**Jetpack Compose**
- 平台：Android

### 3.1 构建工具链版本（已确认）

| 组件 | 版本 |
|------|------|
| Gradle | 9.7.1 |
| Android Gradle Plugin（AGP） | 9.2.1 |
| Kotlin（compose 编译器插件） | 2.4.10 |
| Compose BOM | 2026.08.00 |
| androidx.core:core-ktx | 1.19.0 |
| androidx.lifecycle:lifecycle-runtime-ktx | 2.11.0 |
| androidx.activity:activity-compose | 1.13.0 |
| compileSdk / targetSdk | 37 |
| minSdk | 26 |
| Java 兼容性 | 17（AGP 9 默认 JDK 17+） |

- 构建机制：AGP 9 启用 **Built-in Kotlin**，app 模块不再应用 `org.jetbrains.kotlin.android` 插件；Compose 编译器由 `org.jetbrains.kotlin.plugin.compose` 插件管理。
- 首次导入：Android Studio Sync 时会自动下载 Gradle 发行包、依赖及 android-37 平台（一次性），按提示确认即可。

### 3.2 开发方式约定（已确认）

- 构建配置与文档由 AI 协助维护；**功能代码由开发者纯手动编写**。
- 调整依赖 / 工具链版本时，需同步更新 3.1 版本矩阵。

## 4. 核心功能（已确认）

- 图片处理流程：**导入 → 压缩保存 → 单张图片操作 → 按标签分类导出**

### 4.1 导入

- 图片需用户**手动导入**到 app 私有目录（复制，非移动）
- 导入来源支持两种：**系统相册 / 图库** 与 **文件选择器**

### 4.2 导入时压缩

- 判断标准：**文件大小或像素分辨率任一超过阈值即压缩**（两者结合判断），未超阈值保留原样
- 压缩阈值具体数值：待定（后续细调）

### 4.3 单张图片操作（当前阶段）

- 支持：**重命名**、**加标签**、**确认设置完成**（确认完成也是一个标签）
- 剪切：不纳入当前阶段，列为以后扩展

### 4.4 按标签分类导出

- 根据标签将图片分开放入**用户选择的不同目录**
- **每个标签单独生成一个压缩包（zip）**，放入 app 私有目录

## 5. 页面 / 界面框架

- 待定

## 6. 文件整理方案（已确认）

- 整理方式：**直接操作文件夹** —— 移动 / 重命名真实文件，按规则归类。
- 图片统一存放在 **app 私有目录**，由用户手动导入（复制）进入
- 导入时按需压缩后保存到私有目录
- 按标签分类后，每个标签单独打包为 zip，移入 app 私有目录
- 全程本地运行，**不联网**，无网络权限需求

## 7. 未决事项清单

- [ ] 页面结构与交互流程
- [ ] 压缩阈值具体数值（文件大小 / 分辨率）
- [ ] 整理规则细节（标签体系定义等）

## 8. 开发记录

- 2026-08-25：构建工具链整体升级至 2026-08 最新稳定版，解决 Android Studio 首次 Sync / 构建的系列问题，最终构建安装成功。详细步骤、问题与经验见 [开发日志 · 2026-08-25](开发日志-2026-08-25.md)

---

> v0.5 草稿 · 2026-08-25 · 未决定项一律「待定」，确认后逐步填充
