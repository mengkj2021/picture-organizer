# 图片打包画面 · 测试用例

| 项 | 内容 |
|---|---|
| 画面 | 图片打包画面 |
| route | `export-zip` |
| 规格 | [图片打包画面/README.md](../画面/图片打包画面/README.md) |

| 编号 | 标题 | 前置 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|---|
| TC-export-01 | 无筛选按标签分包 | 已确认含多用户标签 + 未打标签 | 1. 不筛 → 开始打包 | 每用户标签一 zip；有未打标签则多一包；输出 `exports/` | ☐ |
| TC-export-02 | 有筛选只打所选 | 已确认足够数据 | 1. 筛选某标签 → 打包 | 仅生成对应标签包 | ☐ |
| TC-export-03 | 分享 | 打包成功有结果 | 1. 点分享 | 系统分享面板可发出 zip（FileProvider） | ☐ |
| TC-export-04 | 打包中不可返回 | 打包进行中 | 1. 按返回 | 拦截，不退出 | ☐ |
| TC-export-05 | Idle 返回 | 未在打包 | 1. 返回 | 回主画面已确认 Tab | ☐ |
| TC-export-06 | 仅已确认 | 待处理也有同标签图 | 1. 打包 | zip 内不含待处理/回收站图片 | ☐ |
