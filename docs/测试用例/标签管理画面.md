# 标签管理画面 · 测试用例

| 项 | 内容 |
|---|---|
| 画面 | 标签管理画面 |
| route | `tag-manage` |
| 规格 | [标签管理画面/README.md](../画面/标签管理画面/README.md) |

| 编号 | 标题 | 前置 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|---|
| TC-tag-01 | 新增库标签 | 标签列表 Tab | 1. 添加标签名保存 | 列表出现；筛选/详情库 Chip 可见 | ☐ |
| TC-tag-02 | 重命名库标签 | 已有标签 | 1. 编辑名称 | 库名更新（图片已贴旧名是否跟随以实现为准，记结果） | ☐ |
| TC-tag-03 | 删除库标签 | 已有标签；部分图已贴该名 | 1. 删除库标签 | 库中消失；**图片 tagsJson 不因删库被清空**（规格） | ☐ |
| TC-tag-04 | 标签模板 CRUD | 模板 Tab | 1. 新增含多标签名的模板 → 编辑 → 删除 | 列表与详情「套用模板」菜单一致 | ☐ |
| TC-tag-05 | 无状态标签 | 标签列表 | 1. 查看全部库标签 | 不出现「待处理/已确认/不修改」作为库标签（S1） | ☐ |
| TC-tag-06 | 返回设置 | 本画面 | 1. 返回 | 回设置 | ☐ |
