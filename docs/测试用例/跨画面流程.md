# 跨画面流程 · 测试用例

| 项 | 内容 |
|---|---|
| 范围 | 主路径冒烟（多 route） |
| 规格 | [式样书 §4](../式样/项目式样说明书.md)、[路由设计](../设计/路由设计.md) |

| 编号 | 标题 | 前置 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|---|
| TC-flow-01 | 首次安装主路径 | 清数据 | 1. 启动 → 教程完成 → 主画面 → 导入 → 详情打标签 → 移到已确认 → 筛选 → 打包 → 分享 | 全链路无崩溃；数据与文件一致；无状态标签混入用户标签 | ☐ |
| TC-flow-02 | 二次启动 | 已完成教程且有数据 | 1. 冷启动 | splash → 主画面；列表数据仍在 | ☐ |
| TC-flow-03 | 设置系闭环 | 在主画面 | 1. 设置 → 建库标签与模板 → 默认标签 → 导入验证 → 详情套用模板/重命名模板 → 返回 | 各入口可达；偏好与库变更反映到导入/详情 | ☐ |
| TC-flow-04 | 三状态文件落盘 | 待处理有图 | 1. 分别移到已确认、回收站 | 文件出现在对应子目录；三 Tab 互斥展示 | ☐ |
| TC-flow-05 | S1 回归 | 任意整理过的图 | 1. 列表 Chip、详情标签、筛选库、打包分组 | 任何处均不以「待处理/已确认/回收站」作为用户标签 | ☐ |
