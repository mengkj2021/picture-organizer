# 默认标签画面 · 测试用例

| 项 | 内容 |
|---|---|
| 画面 | 默认标签画面 |
| route | `default-tags` |
| 规格 | [默认标签画面/README.md](../画面/默认标签画面/README.md) |

| 编号 | 标题 | 前置 | 步骤 | 预期 | 结果 |
|---|---|---|---|---|---|
| TC-default-tags-01 | 保存多选 | 库有多个标签 | 1. 勾选若干 → 保存 → 返回 | 再次进入勾选状态保留 | ☐ |
| TC-default-tags-02 | 仅新导入生效 | 已勾选默认标签；库中已有旧图 | 1. 再导入新图 | 新图带默认标签；旧图不变 | ☐ |
| TC-default-tags-03 | 未保存不写入 | 勾选后不点保存（若 UI 区分） | 1. 直接返回 | 偏好未改；或以「须点保存」为准记录实际行为 | ☐ |
| TC-default-tags-04 | 返回设置 | 本画面 | 1. 返回 | 回设置 | ☐ |
