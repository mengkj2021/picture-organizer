# F12 票：教程画面内容扩充

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F12 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | 教程画面内容扩充（操作引导细化；用户注明：不着急改，低优先） |
| 状态 | ☐ 待实施 |
| 起票日期 | 2026-09-03 |
| 提出 | 用户 |
| 关联 | [教程画面 README](../../../画面/教程画面/README.md)（3 页结构、文案与 `strings.xml` 一致、§9 待定「配图（可选）」未勾）；[strings.xml](../../../../android/app/src/main/res/values/strings.xml)（`tutorial_page*_title/body` 各一句短文案） |

## 用户诉求 / 场景

- 要解决的问题或场景：教程画面现为 **3 页纯文字、每页一句话**（导入图片 / 整理分类 / 筛选与打包），对首次用户可讲的东西没讲透（导入的文件选择方式、压缩开关与默认标签、状态三 Tab 语义、重命名模板、打标签与自动入库、筛选、打包导出、设置里可重看等均未出现）；用户希望内容更丰富。
- 预期效果：教程信息量扩充，能引导用户完成主要操作闭环；具体形态（页数 / 章节结构 / 是否配图）未定。
- 是否已有式样依据：否（现状文档 §9 仅有「配图（可选）」待办；属体验增强需求待确认）

## 需求确认（接票前若为「待定」先在此澄清）

- 优先级：**低**（用户明示"不着急改"，排在近期测试找 bug 与高优先票之后）。
- 取舍 / 范围界定（接票前澄清）：
  1. 扩充到什么粒度：仅文案扩写／逐功能加页／结构化"分步引导+图文"？
  2. 是否引入配图（画面截图或示意）——涉及测试设备出图与仓库体积；
  3. 页面载体是否沿用 `HorizontalPager` 3 页 + 指示（还是改可滚动长文 / 分节列表），与现有 P6/Bug1 形态的关系；
  4. 文案与 `strings.xml` 同步维护约定是否继续。

## 方案（接票后填）

- 文档先行（画面文档 / 式样书 / 架构 / 路由，按需）：
- 代码改动点：
- 验证方式：

## 验收标准

- [ ] 教程内容扩充达成用户认可的信息量；结构与画面文档、strings.xml 对齐
- [ ] 首次流程 / 设置重看两条路径回归正常（含 Bug1 inset 修复不回退）
- [ ] 开发日志（文件名带 F12）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
|  |  |  |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**（达成了什么 / 与预期差距）：
- **代价与遗留**（新增复杂度、未覆盖场景 → 转新票或备注）：
- **错题本登记**：已登记 [docs/工程/错题本.md](../../../错题本.md) ／ 不适用
