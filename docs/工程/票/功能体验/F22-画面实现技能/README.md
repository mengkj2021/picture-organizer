# F22 票：技能 implement-screen（画面实现 SOP）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F22 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 工程质量（AI 技能落地） |
| 标题 | 落地技能 `implement-screen`：读画面文档 → 实现 Kotlin + 路由注册 |
| 状态 | ☐ 待实施 |
| 起票日期 | 2026-09-03 |
| 关联 | `shared/skills/`；F21（create-screen-doc，姊妹技能，各自独立）；dev-convention 文档先行 |

## 用户诉求 / 场景

- 要解决的问题或场景：画面实现（读画面文档 → 按包结构写 Kotlin → 路由注册 → 对照文档收尾）每次依赖 prompt 重述，易漏：UI 壳层 inset 消费（Bug1/Bug2 教训）、路由表与代码同步、数据依赖对齐画面文档。业界把"实现"也编码为技能，与 `create-screen-doc` 配对，保证文档产物与代码产物用同一套规范。
- 预期效果：调用 `implement-screen` 技能 → 以画面 README 为唯一输入，按固定 SOP 实现 + 注册路由 + 收尾对照，UI 壳层约束自动带上。
- 是否已有式样依据：否（工程资产）。

## 需求确认（接票前若为「待定」先在此澄清）

- 范围：只做 `implement-screen`（F21 是 create-screen-doc，两者拆票独立实施）。
- 落地形态：按 `shared/skills/README.md` SKILL.md 规范；同步工具目录；README 表补登记。
- 技能内容须覆盖历史 UI 教训：M3 Scaffold/inset 消费、IME 顶栏（Bug2）、底栏 inset（Bug3）、系统返回键（Bug4）、F4/F5 可见性联动——SOP 里固化为"壳层与交互对照画面文档逐项核对"。
- 输入约定：本技能以画面文档为准，文档与代码冲突时先回查（docs/README 对齐约定），不擅自改文档。

## 方案（接票后填）

- 文档先行（画面文档 / 式样书 / 架构 / 路由，按需）：无
- 代码改动点：新建 `shared/skills/implement-screen/SKILL.md`（+assets 按需）；同步工具技能目录；`shared/skills/README.md` 补登记
- 验证方式：接一张画面相关票（如 F10 导入流程体验）时用技能实施，核对是否自动带上 UI 壳层约束

## 验收标准

- [ ] `implement-screen` SKILL.md 就位（输入约定 + SOP + 壳层/交互核对清单）
- [ ] `shared/skills/README.md` 登记；工具目录同步可用
- [ ] 至少一次真实画面票实施走技能流程（日志记录，UI 约束无回归）
- [ ] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
|  |  |  |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**（达成了什么 / 与预期差距）：
- **代价与遗留**（新增复杂度、未覆盖场景 → 转新票或备注）：
- **错题本登记**：已登记 [docs/工程/错题本.md](../../../错题本.md) ／ 不适用
