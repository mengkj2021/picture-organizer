# F24 票：规则瘦身（常驻 vs 按需）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F24 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 工程质量（规则组织） |
| 标题 | `dev-convention.md` 瘦身：拆分「常驻高频」与「按需低频」，防上下文膨胀 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 关联 | `shared/rules/dev-convention.md`；`shared/README.md`（frontmatter 说明）；.codebuddy/.cursor 三处同步 |

## 用户诉求 / 场景

- 要解决的问题或场景：`dev-convention.md` 为 alwaysApply（每会话常驻注入），目前较长、包结构等细节全量载入。业界（CLAUDE.md best practices）共识：常驻文件只放高频真理、低频细节按需加载（globs / 技能 / 链接），否则每会话白白烧上下文，规则越长 AI 遵守率越低。
- 预期效果：`dev-convention.md` 收敛为高频核心约定（文档先行、一票一会话、先测后写引用等）；包结构、构建细节等低频内容挪到按需文档（或已有权威文档引用），需要时 AI 现查，不常驻。
- 是否已有式样依据：否（工程整理）。

## 需求确认（接票前若为「待定」先在此澄清）

- 范围：只重组 dev-convention 的**内容分布**（哪些留常驻、哪些移走/改引用），**不新增约束**、不改语义；其他 rules 文件不在范围（各自按需另评）。
- 原则：保留引用链（移到哪就在原位置留一句"见 xxx"），避免移走后找不到；所有内容仍可被 AI 定位到，只是不再全量注入。
- 动规则必须三处同步（shared / .codebuddy / .cursor），与 F19 等新纪律合并排布避免互相冲突——建议本票在 F19（一票一会话）之后实施，一并归位。
- 性能收益可量化：统计瘦身前后的规则正文字数，验收时记录。

## 方案（接票后填）

- 文档先行：无新画面；权威源改为架构 §2
- 代码改动点：无 Android 代码；改 `shared/rules/dev-convention.md` → 同步三处；架构 §2 补「放置约定」
- 验证方式：字数对比；三处 hash 一致；包结构从架构 §2 可定位

## 验收标准

- [x] dev-convention 常驻部分显著变短（记录前后字数），核心纪律无缺失
- [x] 移出的低频内容可从原位置引用/权威文档定位（不丢）
- [x] 三处同步完成；新会话注入验证通过（规则文件已同步；**需用户新建对话确认注入**）
- [x] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-03 | 包结构迁架构 §2；规则 2086→1604 字符；三处同步 | 2b29de9 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：常驻规则约 **-23%**（2086→1604 字符，含 frontmatter）；包结构细则进架构 §2.1/§2.2，规则第 7 条只留引用。核心票驱动/文档先行/route/依赖/票号仍常驻。
- **代价与遗留**：F19/F17/F20 等后续规则仍会加长常驻文；宜继续「先引用后展开」。新会话注入需人工确认一次。
- **错题本登记**：不适用（工程整理，无踩坑）
