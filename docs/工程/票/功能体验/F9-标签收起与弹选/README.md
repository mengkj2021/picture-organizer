# F9 票：标签可选数量超阈值时收起未选项、弹出面板选择

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F9 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | 图片详细画面：库标签可选项超过 10 个时只显示已选择标签，未选择标签经按钮弹出面板选择 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 关联 | [ImageDetailScreen.kt](../../../../android/app/src/main/java/com/pictureorganizer/ui/imagedetail/ImageDetailScreen.kt)；[标签.md](../../../画面/图片详细画面/标签.md) |

## 用户诉求 / 场景

- **现状**：图片详细画面标签区把**标签库全部标签**平铺为 `FilterChip`（已选高亮、未选可点即加）；自定义标签区独立显示（只含已选）。
- **要解决的问题**：标签库标签数量多（如超过十个）时，未选项全铺在画面里，标签区过长、视觉噪音大，找已选项与编辑区被挤远。
- **预期效果**：
  1. 库标签可选项**超过 10 个**时进入「收起」形态：详细画面标签区**只显示已选择的标签**；
  2. 未选择的库标签收起，通过**按钮**（如「选择标签」）弹出**面板/画面**，内含全部标签供勾选；
  3. 库标签数量不超过 10 时维持现状（直接铺开点选）。
- **是否已有式样依据**：否，属体验增强，需求以本票为准；实施时同步更新 [标签.md](../../../画面/图片详细画面/标签.md)。

## 需求确认（接票前若为「待定」先在此澄清）

- **触发阈值口径**：按**库可选项总数**（`state.libraryTags.size`）> 10 判断。
- **「只显示已选择」的范围**：库已选 FilterChip 可点取消；自定义区与自由输入保留。
- **弹出面板提交时机（用户拍板 1A）**：`ModalBottomSheet` 内点选沿用 `ToggleLibraryTag` **即时持久化**。
- **已选 > 10 主区显示（用户拍板 2A）**：全部已选仍显示 +「选择标签」按钮。

## 方案（接票后填）

- 文档先行：`标签.md`、详情 README、式样 §4.3
- 代码：`ImageDetailScreen` 阈值分支 + `ModalBottomSheet`；文案 `detail_tag_pick_library*`
- F20：无 JVM 友好纯逻辑，未加单测
- 验证：编译 + `testDebugUnitTest`；真机 ≤10 / >10 / 面板勾选即时同步

## 验收标准

- [x] 库标签 ≤ 10：标签区与现状一致
- [x] 库标签 > 10：主画面只显示已选择标签（+ 按钮）；点按钮弹出面板可见全部库标签并可勾选
- [x] 面板内选择与取消即时生效，与主画面已选状态一致（无两套状态）
- [x] 自定义标签输入 / 删除不受影响
- [x] 文档与代码对齐：[标签.md](../../../画面/图片详细画面/标签.md) 同步；开发日志（文件名带 F9）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-04 | 1A/2A；收起 + BottomSheet 即时 Toggle；文档 | 20b26a4 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：库标签多时主区噪音下降；面板即时同步已选，无暂存双状态。
- **代价与遗留**：阈值写死 10（常量可调）；未做面板内搜索/排序。
- **错题本登记**：不适用
