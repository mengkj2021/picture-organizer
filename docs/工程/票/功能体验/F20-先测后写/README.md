# F20 票：纯逻辑票「先测后写」（依赖 F16）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F20 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 工程质量（测试流程） |
| 标题 | 涉及纯逻辑的票改为「先写测试、再实现」；依赖 F16 单测就绪 |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 关联 | F16（前置依赖，验收后生效）；`shared/prompts/功能开发.md` |

## 用户诉求 / 场景

- 要解决的问题或场景：单测若不接进开发流程，就只是"有"而非"用"。业界（Nimbalyst 等）直白警告：测试覆盖要与 AI 生成速度同步提升，否则只会更快制造技术债；标准做法是 **spec 写清验收 → 先让 AI 生成测试 → 再实现 → 迭代到绿灯**（Agent 极擅长"迭代到通过"这一循环）。
- 预期效果：F16 落地后，凡接票实施中**涉及纯逻辑改动**（如 `RenamePatternApplier`、filter/sort、mapper 规则），流程要求先补/先写该逻辑的单测，再实现或改实现，实现完成跑测试自证绿灯。让"行为正确"可被自动证明，防修 A 坏 B。
- 是否已有式样依据：否（流程改造）。

## 需求确认（接票前若为「待定」先在此澄清）

- **前置依赖：F16 必须验收完成**（`testDebugUnitTest` 可跑、首批测试在库），本票才实施——建议 F16 接在 F20 前。
- 范围：改 `shared/prompts/功能开发.md`：接票流程加判断"本票改动是否含可测纯逻辑？"→ 是则先写/先看测试，收尾须跑相关测试绿灯；测试与实现同票提交（不单独起测试票，除非测试改动大）。
- 触发示例（可写进 prompt 作判断提示）：重命名模板、标签/状态规则、筛选排序、日期解析、文件名规则、任何 `model/` / `util/` / `data` mapper 的规则改动。
- 不适用于：纯 UI 壳层 / inset / 仅 Compose 结构改动（无逻辑可测时写明"无纯逻辑，未加测试"即可，不必硬造）。
- **验收第 3 条（路线 A，2026-09-03 用户确认）**：本票先 ✅；「先测后写」实测挂下一张**含可测纯逻辑**的票，该票开发日志回证「F20 实测」。

## 方案（接票后填）

- 文档先行（画面文档 / 式样书 / 架构 / 路由，按需）：无
- 代码改动点：`shared/prompts/功能开发.md` 流程加「先测后写」判断与步骤（含"无纯逻辑则注明"豁免）
- 验证方式：流程写入 prompt；纯逻辑实测挂后续票（建议 F13/F14，或任何先碰到纯逻辑的票）

## 验收标准

- [x] F16 已 ✅（前置条件）
- [x] `功能开发.md` 含「先测后写」流程步骤 + 豁免说明
- [x] 「先测后写」实测挂下一张含可测纯逻辑的票：流程已写入；该票日志须回证「F20 实测」（路线 A）
- [x] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-03 | `功能开发.md` 第 5 步加先测后写；实测挂后续纯逻辑票 | 见本提交 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**：接票流程多了「可测纯逻辑？」判断；是则先测后写、收尾绿灯；否则注明豁免。与 F16 基础设施衔接。
- **代价与遗留**：本票无纯逻辑可自证；实测挂下一张含纯逻辑的票（日志写「F20 实测」）。未改 `dev-convention`（按范围）。
- **错题本登记**：不适用
