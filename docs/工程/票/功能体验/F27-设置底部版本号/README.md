# F27 票：设置画面底部显示版本号（versionName）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F27 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 体验增强 |
| 标题 | 设置画面底部加 versionName（版本号）显示 |
| 状态 | ☐ 待实施 |
| 起票日期 | 2026-09-03 |
| 关联 | 画面文档（设置画面）；[改动清单](../改动清单.md) F25 版本矩阵（发布 / 公开配套） |

## 用户诉求 / 场景

- 要解决的问题或场景：`android/app/build.gradle.kts` 已定义 `versionCode = 1`、`versionName = "0.0.1"`，但 UI 无从查看当前版本，用户 / 测试 / 发版对照不便。
- 预期效果：设置画面底部显示 app 版本号（如「版本 0.0.1」），与 build 配置一致。
- 是否已有式样依据：否（体验增强，位置与样式需确认）

## 需求确认（接票前若为「待定」先在此澄清）

- **读取方式（待定）**：`buildFeatures.buildConfig` 当前未开启（`app/build.gradle.kts` 仅 `compose = true`）。可选：
  - A. 开启 buildConfig，用 `BuildConfig.VERSION_NAME`
  - B. `build.gradle.kts` 写 `resValue`，`getString(R.string.app_version)`
- **显示样式 / 位置（待定）**：设置列表底部一行小字（如 caption 灰字居中）即可，还是要加「点击复制」/「长按看 versionCode」等附加能力？倾向简单先行。
- **与 F25 关系**：F25（统一版本矩阵：文档号 ↔ versionName ↔ versionCode）为低优先待办；本票先做 UI 显示，不依赖 F25，但接票时注意别与 F25 冲突（文本只读来源，不硬编码）。

## 方案（接票后填）

- 文档先行：设置画面 README 功能清单 / UI 描述补「底部版本号」。
- 代码改动点：
- 验证方式：

## 验收标准

- [ ] 设置画面底部显示版本号，与 `build.gradle.kts` 的 versionName 一致
- [ ] 文档与代码对齐（设置画面 README）
- [ ] 开发日志（文件名带本票号 F27）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
|  |  |  |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**（达成了什么 / 与预期差距）：
- **代价与遗留**（新增复杂度、未覆盖场景 → 转新票或备注）：
- **错题本登记**：已登记 [docs/工程/错题本.md](../../../错题本.md) ／ 不适用
