# F19 票：一票一会话纪律

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | F19 |
| 类型 | 功能 / 体验票 |
| 类型细分 | 工程质量（AI 协作流程） |
| 标题 | 「一票一会话」纪律：每张票干净会话实施，收尾清上下文 |
| 状态 | ☐ 待实施 |
| 起票日期 | 2026-09-03 |
| 关联 | `shared/rules/dev-convention.md`；prompts/功能开发.md |

## 用户诉求 / 场景

- 要解决的问题或场景：待实施票多（F7–F16 一排），若在同一会话连续接票，上下文越滚越胀、前面票的残留状态干扰后续票（业界：干净会话边界是上下文管理第一原则，"一次聚焦的干净会话优于拖沓的庞大会话"）。当前无"一票一会话"的明示纪律。
- 预期效果：规则层写明——每张票独立开新会话实施；本票收尾（回写票 + 日志 + 用户验收）完成才允许接下一张；会话过长时先回写状态再新开会话续接。
- 是否已有式样依据：否（流程纪律）。

## 需求确认（接票前若为「待定」先在此澄清）

- 范围：在 `shared/rules/dev-convention.md` 加一条强制约定「一票一会话」；同步 `.codebuddy/rules/dev-convention/RULE.mdc` 与 `.cursor/rules/dev-convention.mdc`（sync-convention 强制 2）。
- 明确例外：同一票的「文档先行 → 实现 → 收尾」若因长度拆多会话，**允许续接同一票**，但必须先回写票状态再开新会话。
- 与 F16 无关（纯纪律，不依赖单测）。

## 方案（接票后填）

- 文档先行（画面文档 / 式样书 / 架构 / 路由，按需）：无
- 代码改动点：`shared/rules/dev-convention.md` 新增强制约定 + 同步两工具目录三份文件
- 验证方式：接下一张票时按新纪律开新会话，日志确认会话边界

## 验收标准

- [ ] dev-convention 三处（shared / .codebuddy / .cursor）同步含「一票一会话」约定
- [ ] 后续接票按此纪律执行（日志可证会话边界干净）
- [ ] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
|  |  |  |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **落地效果**（达成了什么 / 与预期差距）：
- **代价与遗留**（新增复杂度、未覆盖场景 → 转新票或备注）：
- **错题本登记**：已登记 [docs/工程/错题本.md](../../../错题本.md) ／ 不适用
