# S2 票：自定义标签成功后自动入库

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | S2 |
| 类型 | 式样变更票 |
| 标题 | 详情添加标签成功后写入标签库（已存在则跳过） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-02 |
| 关联 | [图片详细画面 · 标签](../../../../画面/图片详细画面/标签.md)；[标签管理画面](../../../../画面/标签管理画面/README.md)；式样书 §4.3 |

## 变更内容（已确认的式样）

- 现状：详情可自由输入自定义标签并写入图片 `tagsJson`；**不**因此自动写入标签库 `tags` 表
- 变更为：在详情**添加标签成功**后，若该标签名尚不在标签库，则加入标签库；已存在则不加
- 变更理由：减少「先去标签管理再建库标签」的往返；库与详情用过的标签逐步对齐
- 是否涉及「待定」项澄清：否（行为已由用户确认要做）

## 落地范围（接票后填）

- 式样书 / 画面文档章节：
- 涉及的画面 / route / 数据模型：`image-detail`；`TagRepository` / `tags` 表
- 代码改动点：
- 旧数据 / 迁移影响（若有）：历史仅存在于图片上的自定义名，本票可不回溯批量入库（接票时确认）

## 验收标准

- [ ] 文档先行同步完成（式样 / 标签区域说明）
- [ ] 新加不存在的标签名 → 标签库出现；已存在 → 不重复插入
- [ ] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-02 | SaveTag 成功后 ensureTagInLibrary | 见开发日志 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **变更的好处**（已达成 / 预期）：自由输入标签可直接进入库，减少往返标签管理
- **坏处与代价**（迁移成本、回归面、使用习惯变化等）：库标签可能增多；历史自定义标签未回溯入库
- **若重新选择，是否仍会做**（理由）：会
- **错题本登记**：不适用
