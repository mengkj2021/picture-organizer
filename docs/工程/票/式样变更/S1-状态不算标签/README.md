# S1 票：状态不算标签（归档）

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | S1 |
| 类型 | 式样变更票 |
| 标题 | 状态不算标签：状态仅由 `status` + 主画面三 Tab 表达，不写入标签体系 |
| 状态 | ✅ 已完成（归档） |
| 完成日期 | 2026-09-02 |
| 补建归档目录 | 2026-09-03（本票在票库机制落地前完成，为补全分区目录而补建） |
| 关联 | [开发日志 2026-09-02 S1](../../../日志/开发日志-2026-09-02-S1-状态不算标签.md)；[改动清单](../../改动清单.md)（S1 ✅，已入总台账） |

## 变更内容（已确认的式样）

- 现状：原实现把状态词（待处理 / 已确认 / 不修改）作为「状态标签」注入 `ImageListItem.tags` 并写入 Room `tagsJson`（详情保存、移动、导入时都会拼进标签首位），与式样书旧条款「确认完成也是一个标签」冲突
- 变更为：**状态仅由 `status` 字段 + 主画面三 Tab 分类表达，不作为标签出现在标签库 / `tagsJson` / 筛选 / 打包用户标签中**；`tagsJson` 仅存用户标签
- 保留行为：详情仍把状态名作为保留字禁止输入为用户标签；三 Tab、移动、删除、打包、Exif 双写、重命名 `{tag}`、筛选「未打标签」行为不变（读端早已按用户标签处理）
- 历史数据：存量 Room `tagsJson` 由启动迁移幂等剔除残留状态词；存量 Exif 不回写（读端 `readUserTags` 已过滤）

## 落地范围

- 代码 7 文件：`model/ImageListItem.kt`、`data/mapper/ImageMappers.kt`、`data/repository/RoomImageRepository.kt`、`MockImageRepository.kt`（Preview）、`ImageDetailViewModel.kt`、`ImageListItemRow.kt`、`MockImageListData.kt`、`strings.xml`（文案「状态名不能用作标签」）
- 文档：式样书 §4.3 / §8；架构设计 §3.3 / §3.4 / §3.6 / §4.2；主画面 README §9（待定第 1 条定稿打勾）/ §10

## 验收标准（详见开发日志）

1. 新导入 → DB `tagsJson` 无状态词；主画面 chip 只显示用户标签
2. 移动后 `status` 更新、`tagsJson` 不含任何状态词，用户标签原样保留
3. 详情增删改标签 / 库多选 / 套模板 → `tagsJson` 无状态词；输入状态名仍被拒并提示
4. 打包分组、筛选、重命名 `{tag}`、Exif 回读均不出现状态词
5. 升级自旧版本：列表 chip 不再显示状态词，任一次写操作后 DB 即干净

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-02 | S1 状态不算标签定稿并落地（代码 + 文档同步） | 见开发日志 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **变更的好处**（已达成 / 预期）：消除状态标签注入的漏点与脏数据；标签体系纯化为仅用户标签，与读端既有过滤行为对齐
- **坏处与代价**（迁移成本、回归面、使用习惯变化等）：存量 Room 需启动迁移一次性清洗；存量 Exif 不回写（读端过滤兜底）；「确认完成也是一个标签」旧式样删除
- **若重新选择，是否仍会做**（理由）：会
- **错题本登记**：不适用
