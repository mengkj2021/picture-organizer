# S3 票：主画面 Tab「不修改」改名「回收站」

## 票信息

| 项 | 内容 |
|---|---|
| 票号 | S3 |
| 类型 | 式样变更票 |
| 标题 | 「不修改」Tab 改名「回收站」（仅显示层文案；行为与数据不变） |
| 状态 | ✅ 已完成 |
| 起票日期 | 2026-09-03 |
| 关联 | [式样书](../../../../式样/项目式样说明书.md) 状态整理段 / 目录结构；[主画面](../../../../画面/主画面/README.md)（三 Tab）；[回收站一览](../../../../画面/主画面/回收站一览/一览.md)；[教程画面](../../../../画面/教程画面/README.md)；图片详细画面（保留字提示） |

## 变更内容（已确认的式样）

- 现状：主画面三 Tab「待处理 / 已确认 / **不修改**」；`ImageStatus.NoModify`、目录 `no_modify/`、枚举内部命名
- 变更为：**显示层改名「回收站」**——NavigationBar Tab、空态文案、工具栏「移动到 → 回收站」、教程引导文案、保留字提示文案等用户可见「不修改」字样全部换为「回收站」
- 变更理由：该 Tab 实际用途即存放「决定不再修改 / 不需要」的图片，可删除（真删）、可移出恢复（移到待处理 / 已确认），用户认知等同回收站，原名误导（"不修改"像未处理）
- **行为不变**：移动 / 删除 / 筛选语义、`ImageStatus` 枚举值、`status` 字段、目录名 `no_modify/`、DB 数据一律不动，仅换显示文案
- 是否涉及「待定」项澄清：是——保留字集合 `STATUS_TAGS` 更新为 `setOf("待处理", "已确认", "回收站")`；旧词「不修改」**不再保留**为禁用词（2026-09-03 接票确认）

## 落地范围（接票后填）

- 式样书 / 画面文档章节：式样书「状态整理」与目录结构说明；主画面 README + 三一览（区域目录改名 `回收站一览`）；教程；测试用例状态名同步
- 涉及的画面 / route / 数据模型：`main` 三 Tab 标签与空态；`ImageStatus.NoModify`（不动）；`ImageListItem.STATUS_TAGS` 换词
- 代码改动点：`strings.xml`；`ImageListItem.kt` / `ImageListItemTest.kt`；`MockImageListData.kt`；`ImageTagMetadata` 注释
- 旧数据 / 迁移影响（若有）：无（DB 不存状态中文词；目录 / 枚举 / status 值不变）

## 验收标准

- [x] 文档先行同步完成（式样书「待定」→ 已确认或明确不适用）
- [x] 全 UI 用户可见处为「回收站」，无「不修改」残留（教程 / 空态 / 移动到 / 保留字提示）
- [x] 行为与现状一致：status / 目录 / 枚举不变；「回收站」内移动与删除语义不变；保留字新词生效（输入「回收站」作标签被拒）
- [x] 开发日志（文件名带本票号）已写

## 开发记录

| 日期 | 内容 | 关联提交 |
|---|---|---|
| 2026-09-03 | 显示层改名 + STATUS_TAGS；F20 先测后写；文档目录改名 | 待提交 |

## 完结复盘（✅ / ❌ 后填；票与台账行保留，不删除）

- **变更的好处**（已达成 / 预期）：Tab 语义与真实用途（可删 / 可恢复）对齐，降低「像未处理」的误解
- **坏处与代价**（迁移成本、回归面、使用习惯变化等）：内部仍叫 `NoModify` / `no_modify/`，新人需对照显示名；旧词「不修改」若被用户手打成标签不再拦截（可接受）
- **若重新选择，是否仍会做**（理由）：会——纯文案 + 保留字，代价低、认知收益明确
- **错题本登记**：不适用（无新踩坑；显示名与内部标识可长期并存，不必再记）
